---
phase: 02-detection-parameter-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "M3 Hippocampus 20x 062926 3 plane/BraiAn.yml"
  - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json"
  - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json"
  - "scripts/qc_detection_gates.groovy"
  - "M3 Hippocampus 20x 062926 3 plane/scripts/qc_detection_gates.groovy"
  - "scripts/check_classifier_compartment.py"
autonomous: true
requirements: [SCRI-02, CLASS-01]

must_haves:
  truths:
    - "BraiAn.yml at project root has ONE channelDetections entry named DAPI-T4 (nucleus anchor, D-03), cellExpansionMicrons > 0 (5.0 seed), and a histogramThreshold block — NOT an absolute numeric threshold (D-01)"
    - "TdT_classifier.json reads 'Cytoplasm: AF568-T2 mean'; Fos_Classifier_20x.json reads 'Nucleus: AF488-T3 mean' (CLASS-01)"
    - "A measurement-QC helper (qc_detection_gates.groovy) can compute nucleus-area peak-bin and DAPI density/mm² from a detection run, and a static check script asserts classifier compartment strings"
  artifacts:
    - "M3 Hippocampus 20x 062926 3 plane/BraiAn.yml"
    - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json"
    - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json"
    - "scripts/qc_detection_gates.groovy (canonical) + project hard-copy"
    - "scripts/check_classifier_compartment.py"
  key_links:
    - "BraiAn.yml classifier `name` fields resolve to <project>/classifiers/object_classifiers/<name>.json (BraiAn.resolvePath, project base dir)"
    - "Channel names in BraiAn.yml exactly match server.json channels: AF568-T2, AF488-T3, DAPI-T4 — a mismatch silently yields zero cells"
    - "The 'Cytoplasm: AF568-T2 mean' measurement only exists because cellExpansionMicrons > 0 in BraiAn.yml"
---

<objective>
Author the complete scriptable half of the detection-parameter lock: one global `BraiAn.yml` (single DAPI-anchored `channelDetections` entry with a native `histogramThreshold` block per D-01, `cellExpansionMicrons: 5.0` per D-03 discretion), the two compartment-correct object classifiers (reuse Fos for CLASS-01, rebuild TdT to read the Cytoplasm ring), and the validation harness that reads the D-05 gate metrics from a detection run. This produces a runnable end-to-end detection slice; Plan 02-02 tunes it (human-in-the-loop) until the D-05 gates PASS and locks the values.

Purpose: SCRI-02 needs a channel-correct, histogram-relative `BraiAn.yml`; CLASS-01 needs a verified nuclear-compartment Fos classifier. This plan writes every executor-authorable artifact so the researcher only has to tune and lock in Plan 02-02.
Output: `BraiAn.yml`, `object_classifiers/{Fos_Classifier_20x,TdT_classifier}.json`, `qc_detection_gates.groovy` (canonical + project copy), `check_classifier_compartment.py`.
</objective>

## Phase Goal (MVP user story)

**As a** TRAP2 researcher, **I want to** run BraiAnDetect on one M3 section with a single channel-correct `BraiAn.yml` and two compartment-correct classifiers, **so that** I get biologically plausible TdT+/Fos+/Double+ nucleus counts that pass the D-05 detection-quality gates and can be trusted across the full series.

> Note (MVP fidelity): the ROADMAP `**Goal:**` line for Phase 2 is outcome-shaped, not user-story-shaped. The story above is mapped faithfully from that goal + the project core value (the researcher is the user); no new scope was invented. If a formally SPIDR-split story is wanted, run `/gsd mvp-phase 2`.

## Artifacts This Phase Produces (SCRI-02, CLASS-01)

| Artifact | Plan | Kind | Note |
|----------|------|------|------|
| `M3 Hippocampus 20x 062926 3 plane/BraiAn.yml` | 02-01 (author) / 02-02 (lock) | NEW config | Single DAPI-T4 entry, histogramThreshold, cellExpansionMicrons 5.0 |
| `.../classifiers/object_classifiers/` (directory) | 02-01 | NEW dir | Does not yet exist in target project |
| `.../object_classifiers/Fos_Classifier_20x.json` | 02-01 (copy) / 02-02 (threshold) | REUSE | Already reads `Nucleus: AF488-T3 mean` (CLASS-01) |
| `.../object_classifiers/TdT_classifier.json` | 02-01 (rebuild) / 02-02 (threshold) | REBUILD | `Cytoplasm: AF568-T2 mean` (was Nucleus — the bug being fixed) |
| `scripts/qc_detection_gates.groovy` (+ project copy) | 02-01 | NEW helper | Computes nucleus-area peak-bin + DAPI density/mm² for the D-05 gates |
| `scripts/check_classifier_compartment.py` | 02-01 | NEW helper | Static assert of classifier `function.measurement` compartment string |
| `.../02-LOCK-RECORD.md` | 02-02 | NEW record | Records D-05 PASS values + D-06 advisory ratio + D-07 skip |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detection-parameter-lock/02-CONTEXT.md
@.planning/phases/02-detection-parameter-lock/02-RESEARCH.md
@.planning/phases/02-detection-parameter-lock/02-PATTERNS.md
@CLAUDE.md
@.claude/CLAUDE.md

# Ground-truth source files (read the exact strings, do not paraphrase):
@M3 Hippocampus 20x 062226/M3 Hippocampus 20x 062226 Redo/classifiers/object_classifiers/Fos_Classifier_20x.json
@M3 Hippocampus 20x 062226/M3 Hippocampus 20x 062226 Redo/classifiers/object_classifiers/TRAP2TdT_Classifier_20x.json
@M3 Hippocampus 20x 062926 3 plane/data/1/server.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the D-05 validation harness (measurement-QC Groovy + static compartment check)</name>
  <files>scripts/qc_detection_gates.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/qc_detection_gates.groovy, scripts/check_classifier_compartment.py</files>
  <read_first>
    - .planning/phases/02-detection-parameter-lock/02-RESEARCH.md § "Validation Architecture" → "Wave 0 Gaps" (the two missing QC scripts this task creates) and § "Common Pitfalls" Pitfall 3 (DG bleed is visual, not just numeric).
    - M3 Hippocampus 20x 062926 3 plane/data/1/server.json (pixelWidth value 0.6905355 µm, channel names) — the density math needs pixel size in µm to convert annotation ROI area (px²) to mm².
    - M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy (established Groovy conventions: Javadoc header, `println` logging, `def` declarations — mirror them).
  </read_first>
  <action>
    Write `scripts/qc_detection_gates.groovy` as a QuPath "run in script editor" helper (the researcher runs it after a detection pass in Plan 02-02; this task only authors it). It must, against the current image data: (1) collect the per-cell nucleus-area measurement (measurement key `Nucleus: Area µm^2`, produced by WatershedCellDetection with `makeMeasurements: true`; fall back to the first measurement name containing `Area` if that exact key is absent, and `println` which key was used), bin areas into 10 µm² bins, and print the modal (peak) bin range so the researcher can check D-05 gate 1 (peak in 50–150 µm²); (2) compute DAPI nucleus density for D-05 gate 2 as detection-count-inside-annotation divided by annotation ROI area in mm², where mm² = `roi.getArea()` (px²) × (0.6905355e-3)^2 — read the pixel size from the image server rather than hard-coding when available, else use 0.6905355 µm; print whole-section density plus per-annotation density, and explicitly print the DG and CA1 region densities by matching annotation `getPathClass()`/name; (3) print the advisory D-06 ratio = count(Double+ or cells classed both TdT and Fos) / count(TdT+) with a note that this is ADVISORY per D-06, NOT a gate. Use `getCurrentImageData()`, `getDetectionObjects()`, `getAnnotationObjects()`; log everything via `println` to the QuPath log. Do NOT hard-code any threshold pass/fail — print the raw numbers and the D-05 target ranges (50–150 µm²; 500–2000/mm²) side by side for the human to judge. Hard-copy the finished file into the project `scripts/` dir per the Phase-1 canonical/hard-copy convention.
    Write `scripts/check_classifier_compartment.py` (stdlib only, no conda env needed): CLI `check_classifier_compartment.py <classifier.json> <expected_measurement>`; load the JSON, read `function.measurement`, exit 0 and print OK if it equals the expected string, else exit 1 and print the actual vs expected. Reusable for CLASS-01 now and CLASS-02 later.
  </action>
  <verify>
    <automated>test -s "scripts/qc_detection_gates.groovy" && grep -q "getDetectionObjects" "scripts/qc_detection_gates.groovy" && grep -q "getAnnotationObjects" "scripts/qc_detection_gates.groovy" && grep -q "0.6905355" "scripts/qc_detection_gates.groovy" && python3 scripts/check_classifier_compartment.py "M3 Hippocampus 20x 062226/M3 Hippocampus 20x 062226 Redo/classifiers/object_classifiers/Fos_Classifier_20x.json" "Nucleus: AF488-T3 mean"</automated>
  </verify>
  <done>Both helper scripts exist; the QC Groovy references detections/annotations and the µm pixel size; the static check exits 0 against the known-good source Fos classifier and would exit 1 on a wrong compartment string.</done>
  <acceptance_criteria>
    - `scripts/qc_detection_gates.groovy` prints (a) nucleus-area peak bin, (b) whole-section + DG + CA1 DAPI density/mm², (c) advisory Double+/TdT+ ratio, alongside the D-05 target ranges — with no baked-in pass/fail.
    - A project hard-copy of the Groovy exists under `M3 Hippocampus 20x 062926 3 plane/scripts/`.
    - `python3 scripts/check_classifier_compartment.py <file> <expected>` exits 0 on match, 1 on mismatch.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the two object classifiers (reuse Fos for CLASS-01, rebuild TdT to Cytoplasm)</name>
  <files>M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json, M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json</files>
  <read_first>
    - M3 Hippocampus 20x 062226/M3 Hippocampus 20x 062226 Redo/classifiers/object_classifiers/Fos_Classifier_20x.json (copy this verbatim; it already reads `Nucleus: AF488-T3 mean` — CLASS-01).
    - M3 Hippocampus 20x 062226/M3 Hippocampus 20x 062226 Redo/classifiers/object_classifiers/TRAP2TdT_Classifier_20x.json (the analog to rebuild; its `Nucleus: AF568-T2 mean` is the compartment BUG being fixed).
    - .planning/phases/02-detection-parameter-lock/02-PATTERNS.md § "Shared Patterns" → "Classifier JSON schema" (only measurement, class-name pair, and threshold ever vary).
    - .planning/phases/02-detection-parameter-lock/02-CONTEXT.md "Claude's Discretion" (reuse-vs-rebuild rule) and CLAUDE.md nucleus-anchored / cytosolic-TdTomato mandate.
  </read_first>
  <action>
    First create the directory `M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/` (it does not yet exist — target `classifiers/` currently holds only `classes.json`).
    Write `Fos_Classifier_20x.json` by copying the 062226-Redo source verbatim (SimpleClassifier / ClassifyByMeasurementFunction; `function.measurement` = `Nucleus: AF488-T3 mean`; pathClasses `["Negative","Positive"]`; filter `DETECTIONS_ALL`). Do NOT alter `function.measurement` — it already satisfies CLASS-01 (nuclear compartment). Leave `function.threshold` at the inherited `9341.31736526946` as a SEED ONLY; it will be re-derived histogram-relative on entry 1 in Plan 02-02 per D-02 (this file is not "locked" until then). Omit or keep the `timestamp` field; QuPath rewrites it on save.
    Write `TdT_classifier.json` by copying the TRAP2TdT analog's structure but changing ONLY the compartment: `function.measurement` MUST be `Cytoplasm: AF568-T2 mean` (per D-CLASS: cytosolic TdTomato measured on the expansion ring — this string only resolves because BraiAn.yml sets cellExpansionMicrons > 0). Standardize the class names to match the Fos classifier convention: `pathClassBelow` = `Negative`, `pathClassEquals` = `Positive`, `pathClassAbove` = `Positive`, `pathClasses` = `["Negative","Positive"]` (upgrade the analog's `"Other"` to `"Positive"` for BraiAnalyse naming consistency, per 02-PATTERNS.md). Leave `function.threshold` at the inherited `10200.8443` as a SEED ONLY, to be re-derived in Plan 02-02 (D-02). Keep `object_classifier_type` `SimpleClassifier`, `classifier_fun` `ClassifyByMeasurementFunction`, `filter` `DETECTIONS_ALL`.
  </action>
  <verify>
    <automated>test -f "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json" && test -f "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json" && jq -e '.function.measurement == "Nucleus: AF488-T3 mean"' "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json" && jq -e '.function.measurement == "Cytoplasm: AF568-T2 mean"' "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json" && jq -e '.pathClasses == ["Negative","Positive"]' "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json"</automated>
  </verify>
  <done>`object_classifiers/` dir created; Fos classifier reads `Nucleus: AF488-T3 mean` (CLASS-01); TdT classifier reads `Cytoplasm: AF568-T2 mean` with Negative/Positive classes; both are valid JSON.</done>
  <acceptance_criteria>
    - `jq` confirms Fos `function.measurement` == `Nucleus: AF488-T3 mean` and TdT == `Cytoplasm: AF568-T2 mean`.
    - TdT `pathClasses` are `["Negative","Positive"]` (no leftover `"Other"`).
    - Both files parse as JSON and thresholds are flagged in the SUMMARY as seed-only (locked in 02-02).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author the single-DAPI-anchored BraiAn.yml (SCRI-02, D-01/D-03)</name>
  <files>M3 Hippocampus 20x 062926 3 plane/BraiAn.yml</files>
  <read_first>
    - .planning/phases/02-detection-parameter-lock/02-RESEARCH.md § "Pattern 1: Single-Entry, Multi-Classifier channelDetections" (the exact field set, VERIFIED from the installed v1.1.0 jar) and § "Anti-Patterns to Avoid" (why NOT the per-marker/OverlappingDetections topology).
    - .planning/phases/02-detection-parameter-lock/02-PATTERNS.md § "BraiAn.yml" (topology note + confirmed pixel calibration 0.6905355 from server.json).
    - M3 Hippocampus 20x 062926 3 plane/data/1/server.json (channels block: AF568-T2, AF488-T3, DAPI-T4; pixelWidth 0.6905355 µm).
  </read_first>
  <action>
    Write `M3 Hippocampus 20x 062926 3 plane/BraiAn.yml` at the project root (sibling to `project.qpproj`) as ONE global config for the whole series (D-03), using the single-DAPI-anchored topology (D-RESEARCH Open Q#1 resolved to single entry — nucleus-anchored per CLAUDE.md; do NOT emit the upstream example's per-marker entries or OverlappingDetections).
    Top level: `classForDetections: null`; `detectionsCheck:` with `apply: false` (D-07 negative-control gate is skipped this phase) and `controlChannel: null`.
    `channelDetections:` a single list item with `name: "DAPI-T4"` (exact — the nuclear anchor). Under `parameters:` set: `requestedPixelSizeMicrons: 0.6905355` (match server.json PhysicalSizeX so WatershedCellDetection does not resample, per A4); `backgroundRadiusMicrons: 10`; `backgroundByReconstruction: true`; `medianRadiusMicrons: 0.0`; `sigmaMicrons: 2.0`; `minAreaMicrons: 20.0`; `maxAreaMicrons: 150.0`; `watershedPostProcess: true`; `cellExpansionMicrons: 5.0` (D-03 discretion seed — this is what creates the Cytoplasm compartment the TdT classifier reads); `includeNuclei: true`; `smoothBoundaries: true`; `makeMeasurements: true`. For the detection threshold use the native `histogramThreshold:` block (D-01: histogram-relative, NOT an absolute numeric `threshold` key) with `resolutionLevel: 0`, `smoothWindowSize: 15`, `peakProminence: 100`, `nPeak: 1`. Under `classifiers:` list two entries applied to the SAME DAPI-detected object set (SingleClassifier merges classes onto the same object — this is how Double+ arises, no geometric overlap): first `{channel: "AF488-T3", name: "Fos_Classifier_20x", annotationsToClassify: []}`, second `{channel: "AF568-T2", name: "TdT_classifier", annotationsToClassify: []}`. The `name` values must match the classifier filenames (without `.json`) authored in Task 2, since BraiAn resolves them under `<project>/classifiers/object_classifiers/`.
    IMPORTANT provenance note to carry into the SUMMARY: `sigmaMicrons`, `minAreaMicrons`, `maxAreaMicrons` are TRAP2-paper-seed values that are [ASSUMED] (primary source returned HTTP 403 in research, Assumptions A1) — they are STARTING SEEDS to be tuned against the D-05 empirical gates in Plan 02-02, not verified locked numbers. Do not present them as final.
  </action>
  <verify>
    <automated>/home/jflab/miniforge3/envs/braian/bin/python3 -c "import yaml,sys; d=yaml.safe_load(open('M3 Hippocampus 20x 062926 3 plane/BraiAn.yml')); e=d['channelDetections']; assert len(e)==1, 'must be a single channelDetections entry'; p=e[0]; assert p['name']=='DAPI-T4'; par=p['parameters']; assert 'histogramThreshold' in par and 'threshold' not in par, 'must use histogramThreshold not absolute threshold'; assert par['cellExpansionMicrons']>0; assert abs(par['requestedPixelSizeMicrons']-0.6905355)<1e-9; cls=[c['name'] for c in p['classifiers']]; assert cls==['Fos_Classifier_20x','TdT_classifier'], cls; chs=[c['channel'] for c in p['classifiers']]; assert set(chs)=={'AF488-T3','AF568-T2'}; assert d['detectionsCheck']['apply'] is False; print('BraiAn.yml OK')"</automated>
  </verify>
  <done>`BraiAn.yml` parses; exactly one DAPI-T4 channelDetections entry; histogramThreshold present and no absolute `threshold`; cellExpansionMicrons > 0; requestedPixelSizeMicrons = 0.6905355; two classifiers (Fos_Classifier_20x on AF488-T3, TdT_classifier on AF568-T2); detectionsCheck.apply false.</done>
  <acceptance_criteria>
    - The YAML parse-assert command prints `BraiAn.yml OK`.
    - Channel names in the file are exactly `DAPI-T4`, `AF488-T3`, `AF568-T2` (match server.json).
    - SUMMARY flags sigma/min/max area as [ASSUMED] seeds pending D-05 tuning in 02-02.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local files → QuPath/BraiAnDetect desktop session | `BraiAn.yml` + classifier JSON authored locally are read by the QuPath extension at detection time; only untrusted "input" is the microscopy image, all config is hand-authored by the researcher |
| (no network / no auth / no PII) | Single-user local scientific pipeline; no endpoints, no secrets, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (data correctness) | TdT classifier `function.measurement` compartment | high | mitigate | Rebuilt to `Cytoplasm: AF568-T2 mean` (Task 2); enforced by `jq` verify + `check_classifier_compartment.py`; visual overlay QC gated in 02-02. Wrong compartment silently mis-counts cytosolic TdTomato |
| T-02-02 | Tampering (data correctness) | Channel names in BraiAn.yml / classifiers vs OME-TIFF | high | mitigate | Exact `AF568-T2`/`AF488-T3`/`DAPI-T4` matched to server.json (Task 3 verify); non-zero-cell detection check in 02-02. Mismatch → silent zero-cell run (STATE.md Critical Risk) |
| T-02-03 | Tampering (data correctness) | Detection threshold drifting across series | medium | mitigate | `histogramThreshold` block instead of absolute `threshold` (D-01, Task 3); classifier thresholds re-derived histogram-relative in 02-02 (D-02) |
| T-02-04 | Tampering / Elevation | SnakeYAML deserialization of BraiAn.yml | low | accept | Already mitigated upstream: extension uses typed `Constructor(ProjectsConfig.class, LoaderOptions)` (verified in bytecode); YAML is authored locally, not accepted from untrusted source |
| T-02-SC | Supply chain | package installs | low | accept | No package installs this phase — QuPath 0.6.0 + qupath-extension-braian 1.1.0 pre-installed and catalog-pinned (RESEARCH "Package Legitimacy Audit: Not applicable") |
</threat_model>

<verification>
- Task 1: both helper scripts exist; static check exits 0 on the known-good source Fos classifier.
- Task 2: `jq` confirms Fos = `Nucleus: AF488-T3 mean`, TdT = `Cytoplasm: AF568-T2 mean`, TdT classes `["Negative","Positive"]`.
- Task 3: braian-env pyyaml parse-assert prints `BraiAn.yml OK` (single DAPI-T4 entry, histogramThreshold, cellExpansionMicrons>0, exact channels).
- Static file checks only — no detection run happens in this plan (that is the human step in 02-02).
</verification>

<success_criteria>
- `BraiAn.yml`, both classifier JSONs, `object_classifiers/` dir, and both helper scripts exist and pass their automated checks.
- Topology is the single DAPI-anchored entry (not per-marker); histogram-relative threshold (D-01); Fos nuclear (CLASS-01); TdT cytoplasmic.
- All numeric detection/classifier values are recorded as SEEDS pending the D-05 tuning + lock in Plan 02-02.
</success_criteria>

<output>
Create `.planning/phases/02-detection-parameter-lock/02-01-SUMMARY.md` when done. In it, list the exact seed values written, flag sigma/min/max-area as [ASSUMED] TRAP2 seeds, and state that thresholds (detection + both classifiers) are re-derived and locked in Plan 02-02.
</output>
