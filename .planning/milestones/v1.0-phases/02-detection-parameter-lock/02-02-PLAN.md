---
phase: 02-detection-parameter-lock
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "M3 Hippocampus 20x 062926 3 plane/BraiAn.yml"
  - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json"
  - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json"
  - ".planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md"
autonomous: false
requirements: [SCRI-02, CLASS-01]

must_haves:
  truths:
    - "GATE (D-05.1): detected nucleus-area distribution peaks in 50–150 µm² on entry 1 — must PASS on both DG and CA1 to lock"
    - "GATE (D-05.2): DAPI nucleus density is 500–2000/mm² on entry 1 — must PASS to lock"
    - "GATE (visual, success criterion 4): cytoplasmic expansion rings on dense DG do not bleed into adjacent nuclei; CA1 shows clean nucleus separability (D-04)"
    - "GATE (SCRI-02): a detection run with BraiAn.yml produces NON-ZERO cells in both the Fos (AF488-T3) and TdT (AF568-T2) channels — proves channel names match"
    - "GATE (CLASS-01): detection overlay shows Fos+ only in nuclei; Fos_Classifier_20x.json still reads Nucleus: AF488-T3 mean"
    - "NOT-A-GATE (D-06): Double+/TdT+ ratio is reported (advisory 10–40% target) but does NOT block the lock — gating on the expected biological result would be circular"
    - "NOT-A-GATE (D-07): Fos+ negative-control rate is SKIPPED this phase — a single hippocampal section has no trustworthy low-activity control region; deferred to the full series"
  artifacts:
    - "M3 Hippocampus 20x 062926 3 plane/BraiAn.yml (tuned + locked values written back)"
    - "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/{Fos_Classifier_20x,TdT_classifier}.json (histogram-relative thresholds re-derived, D-02)"
    - ".planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md (D-05 PASS values, D-06 advisory, D-07 skip)"
  key_links:
    - "The locked param values in BraiAn.yml are exactly the ones that made both D-05 gates PASS on DG AND CA1 (D-04)"
    - "Re-derived classifier thresholds (D-02) come from entry-1's own Fos/TdT measurement distributions, not the inherited 062226 absolute values"
---

<objective>
Human-in-the-loop tuning and lock. The researcher runs BraiAnDetect on M3 062926 entry 1 (D-04) using the `BraiAn.yml` and classifiers authored in Plan 02-01, tunes sigma / min-max area / histogramThreshold (and cellExpansionMicrons if DG rings bleed) until BOTH D-05 gates PASS on DG and CA1, and re-derives the two classifier thresholds histogram-relative from entry-1's measurement distributions (D-02). Then the executor writes those researcher-reported values back into the config files and records the lock.

Purpose: D-05 is the ONLY lock gate (detection-quality metrics), deliberately not the biological Double+ ratio (D-06 circularity) or the negative-control rate (D-07, no control region on one section). This plan closes SCRI-02 (tuned, channel-correct, non-zero-cell BraiAn.yml) and CLASS-01 (Fos nuclear overlay confirmed).
Output: locked `BraiAn.yml` + both classifier JSONs; `02-LOCK-RECORD.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detection-parameter-lock/02-CONTEXT.md
@.planning/phases/02-detection-parameter-lock/02-RESEARCH.md
@.planning/phases/02-detection-parameter-lock/02-VALIDATION.md
@CLAUDE.md
@.claude/CLAUDE.md
@M3 Hippocampus 20x 062926 3 plane/BraiAn.yml
@M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json
@M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json
@scripts/qc_detection_gates.groovy
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (HUMAN-IN-THE-LOOP): Tune detection in QuPath and report the D-05 gate measurements</name>
  <files>M3 Hippocampus 20x 062926 3 plane/ (live QuPath GUI session on entry 1 — no file is authored by this task; the executor writes files in Task 2 from the reported values)</files>
  <read_first>
    - .planning/phases/02-detection-parameter-lock/02-CONTEXT.md § Decisions (D-04 tuning substrate DG+CA1; D-05 hard gates; D-06 advisory; D-07 skipped).
    - .planning/phases/02-detection-parameter-lock/02-RESEARCH.md § "Common Pitfalls" Pitfall 2 (classifier threshold is a SEPARATE surface from BraiAn.yml histogramThreshold — D-02 must be re-derived by hand) and Pitfall 3 (DG bleed is a visual check, can hide behind a globally-passing area histogram).
    - CLAUDE.md "Agent behavior" (GUI tuning is human-only) and detection-correctness rules.
  </read_first>
  <action>
    HUMAN-ONLY (per CLAUDE.md "Agent behavior": QuPath GUI parameter tuning cannot be automated — the executor does NOT run QuPath). The researcher, at the monitor on DISPLAY=:0, runs BraiAnDetect on entry 1 with the 02-01 `BraiAn.yml`, iteratively adjusts `sigmaMicrons` / `minAreaMicrons` / `maxAreaMicrons` / `histogramThreshold` (and `cellExpansionMicrons` if DG rings bleed) until BOTH D-05 gates PASS on DG and CA1, re-derives the two classifier thresholds histogram-relative from entry-1's own measurement distributions (D-02), and reports the measured values back to the executor for write-back in Task 2. See how-to-verify for the exact steps.
  </action>
  <what-built>
    Plan 02-01 authored: `BraiAn.yml` (single DAPI-T4 entry, histogramThreshold, cellExpansionMicrons 5.0), `Fos_Classifier_20x.json` (nuclear), `TdT_classifier.json` (cytoplasmic), and `qc_detection_gates.groovy` (prints nucleus-area peak bin, DAPI density/mm², advisory Double+/TdT+ ratio). This checkpoint is the GUI parameter tuning that Claude cannot automate (per CLAUDE.md).
  </what-built>
  <how-to-verify>
    1. Launch QuPath 0.6.0 (`$HOME/section-pipeline/tools/QuPath/bin/QuPath`), open the `M3 Hippocampus 20x 062926 3 plane` project, entry 1 (atlas annotations already loaded from Phase 1).
    2. Run BraiAnDetect using `BraiAn.yml` (single-entry test on entry 1). Confirm it produces non-zero detections and that both classifiers apply (Fos+ and TdT+ classes appear).
    3. Run `scripts/qc_detection_gates.groovy` in the QuPath script editor; read the printed nucleus-area peak bin, the DG and CA1 DAPI densities, and the advisory Double+/TdT+ ratio.
    4. Zoom to dense DG: visually confirm cytoplasmic expansion rings do NOT bleed into adjacent nuclei (raise `sigmaMicrons`/adjust min area to separate merged nuclei first, or reduce `cellExpansionMicrons` below 5.0, if they do). Zoom to CA1: confirm clean nucleus separability.
    5. If either D-05 gate fails, adjust `sigmaMicrons` / `minAreaMicrons` / `maxAreaMicrons` / `histogramThreshold` (`peakProminence`, `nPeak`) and re-run until BOTH gates pass on BOTH DG and CA1.
    6. Re-derive each classifier threshold histogram-relative from entry-1's own measurement distribution (D-02): inspect the `Nucleus: AF488-T3 mean` distribution for the Fos cutoff and the `Cytoplasm: AF568-T2 mean` distribution for the TdT cutoff; record the chosen numeric cutoffs and the rule used (e.g. percentile or median+k·MAD).
    7. Confirm the Fos+ overlay lands only in nuclei (CLASS-01 visual).
  </how-to-verify>
  <acceptance_criteria>
    Researcher reports back, as observed/measured values (not a command output):
    - Nucleus-area histogram PEAK bin, and that it falls in 50–150 µm² on both DG and CA1 (D-05 gate 1 PASS).
    - DAPI nucleus density in /mm² for DG and CA1, both within 500–2000/mm² (D-05 gate 2 PASS).
    - DG bleed check: PASS (rings not crossing into neighbouring nuclei); CA1 separability: PASS.
    - Detection produced NON-ZERO Fos+ and TdT+ cells (channel names verified live).
    - Fos+ overlay confirmed in nuclei only (CLASS-01).
    - Final tuned param values: sigmaMicrons, minAreaMicrons, maxAreaMicrons, cellExpansionMicrons, and the histogramThreshold block that passed.
    - Re-derived histogram-relative thresholds for the Fos and TdT classifiers (numeric + derivation rule).
    - Advisory D-06 Double+/TdT+ ratio (reported, NOT gated). D-07 negative-control explicitly noted as skipped this phase.
  </acceptance_criteria>
  <verify>
    <human-check>Researcher confirms, as reported measured values (manual-only — no automated command; QuPath GUI per CLAUDE.md): nucleus-area peak in 50–150 µm² (DG + CA1), DAPI density 500–2000/mm² (DG + CA1), DG rings non-bleeding, non-zero Fos+ and TdT+ cells, Fos+ in nuclei only, plus the final tuned params and re-derived classifier thresholds.</human-check>
  </verify>
  <done>Both D-05 gates PASS on DG and CA1, DG bleed/CA1 separability confirmed, and the tuned params + re-derived thresholds are reported for write-back in Task 2.</done>
  <resume-signal>Type "approved" with the measured values above, or describe which gate failed and what was adjusted.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the tuned/re-derived values back into BraiAn.yml + classifiers and record the lock</name>
  <files>M3 Hippocampus 20x 062926 3 plane/BraiAn.yml, M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json, M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json, .planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md</files>
  <read_first>
    - The researcher's reported values from Task 1 (final tuned parameters + re-derived classifier thresholds).
    - M3 Hippocampus 20x 062926 3 plane/BraiAn.yml and both classifier JSONs (edit in place — do NOT rewrite structure, only the numeric values that were tuned).
    - .planning/phases/02-detection-parameter-lock/02-VALIDATION.md (record the lock outcome into the Manual-Only Verifications context).
  </read_first>
  <action>
    Edit `BraiAn.yml` in place: replace the seed `sigmaMicrons`, `minAreaMicrons`, `maxAreaMicrons`, `cellExpansionMicrons`, and the `histogramThreshold` sub-values with the exact tuned values the researcher reported in Task 1. Do NOT change the topology (single DAPI-T4 entry), the channel names, or swap `histogramThreshold` for an absolute `threshold`.
    Edit `Fos_Classifier_20x.json` and `TdT_classifier.json` in place: set each `function.threshold` to the researcher's re-derived histogram-relative cutoff (D-02). Do NOT touch `function.measurement` on either file (Fos stays `Nucleus: AF488-T3 mean`, TdT stays `Cytoplasm: AF568-T2 mean`).
    Write `.planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md` capturing: the locked BraiAn.yml parameter values; the two locked classifier thresholds + derivation rule; D-05 gate 1 (nucleus-area peak, DG + CA1) PASS values; D-05 gate 2 (DAPI density, DG + CA1) PASS values; the DG bleed / CA1 separability visual result; the advisory D-06 Double+/TdT+ ratio marked ADVISORY-NOT-GATE; and an explicit "D-07 Fos+ negative-control gate SKIPPED this phase (no control region on a single section) — deferred to full series" line. Cite SCRI-02 and CLASS-01 as satisfied.
  </action>
  <verify>
    <automated>/home/jflab/miniforge3/envs/braian/bin/python3 -c "import yaml; d=yaml.safe_load(open('M3 Hippocampus 20x 062926 3 plane/BraiAn.yml')); p=d['channelDetections'][0]; par=p['parameters']; assert p['name']=='DAPI-T4'; assert 'histogramThreshold' in par and 'threshold' not in par; assert par['cellExpansionMicrons']>0; print('yml locked OK')" && jq -e '.function.measurement == "Nucleus: AF488-T3 mean"' "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json" && jq -e '.function.measurement == "Cytoplasm: AF568-T2 mean" and (.function.threshold | type == "number")' "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/TdT_classifier.json" && python3 scripts/check_classifier_compartment.py "M3 Hippocampus 20x 062926 3 plane/classifiers/object_classifiers/Fos_Classifier_20x.json" "Nucleus: AF488-T3 mean" && grep -qi "D-05" ".planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md" && grep -qi "SKIPPED" ".planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md"</automated>
  </verify>
  <done>BraiAn.yml still parses with the DAPI-T4/histogramThreshold/cellExpansion invariants intact and the tuned values written; both classifier compartments unchanged; both thresholds are numbers; the lock record captures both D-05 PASS values, the D-06 advisory ratio, and the D-07 skip.</done>
  <acceptance_criteria>
    - BraiAn.yml parse-assert prints `yml locked OK`; topology/compartment invariants unchanged.
    - Fos measurement `Nucleus: AF488-T3 mean`, TdT measurement `Cytoplasm: AF568-T2 mean`, both thresholds numeric (re-derived, D-02).
    - `02-LOCK-RECORD.md` records D-05 gate 1 + gate 2 PASS values (DG + CA1), D-06 advisory ratio, D-07 skip; marks SCRI-02 + CLASS-01 satisfied.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local files → QuPath/BraiAnDetect desktop session | Tuned `BraiAn.yml` + classifier JSONs are read by the extension when the researcher runs detection; the only untrusted input is the local microscopy image |
| (no network / no auth / no PII) | Single-user local scientific pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (data correctness) | Params tuned toward the expected biological ratio (circularity) | high | mitigate | Lock gate is D-05 detection-quality metrics ONLY; D-06 Double+/TdT+ ratio is reported ADVISORY, explicitly excluded from the lock (CONTEXT D-06) |
| T-02-06 | Tampering (data correctness) | Locked absolute classifier threshold drifting across the series | medium | mitigate | Classifier thresholds re-derived histogram-relative from entry-1's distribution (D-02); detection threshold uses histogramThreshold (D-01). Series-drift monitored later via SERIES-02 |
| T-02-07 | Tampering (data correctness) | Write-back accidentally alters compartment/topology/channel names | high | mitigate | Task 2 verify re-asserts DAPI-T4 single entry, histogramThreshold (no absolute), Fos `Nucleus:`, TdT `Cytoplasm:` after the edit |
| T-02-01 | Tampering (data correctness) | Fos+ classified outside nuclei | high | mitigate | Visual overlay CLASS-01 check in Task 1; static `check_classifier_compartment.py` re-run in Task 2 verify |
| T-02-SC | Supply chain | package installs | low | accept | No package installs this phase — tooling pre-installed and version-pinned |
</threat_model>

<verification>
- Task 1 is a blocking human checkpoint; acceptance is the researcher's reported D-05 PASS values + visual DG/CA1 results (manual-only per CLAUDE.md GUI rule).
- Task 2 automated: BraiAn.yml parse-assert (`yml locked OK`), `jq` compartment + numeric-threshold asserts, static compartment check, and lock-record content greps.
- Phase gate: both D-05 gates PASS on entry 1 (DG + CA1) before BraiAn.yml + both classifiers are considered LOCKED. D-06 and D-07 are reported, not gating.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criteria 1–4 all satisfied: (1) BraiAn.yml has locked sigma/min-max area/histogram-relative threshold + cellExpansionMicrons > 0, tuned on one section; (2) channel names match and a run yields non-zero Fos + TdT cells; (3) Fos_classifier reads `Nucleus: AF488-T3 mean` (static + overlay); (4) DG overlay shows rings not bleeding.
- SCRI-02 and CLASS-01 complete; `02-LOCK-RECORD.md` documents the D-05 lock, the D-06 advisory ratio, and the D-07 skip.
</success_criteria>

<output>
Create `.planning/phases/02-detection-parameter-lock/02-02-SUMMARY.md` when done. Record the final locked values, the D-05 gate PASS numbers (DG + CA1), the advisory D-06 ratio, the D-07 skip, and confirm SCRI-02 + CLASS-01 satisfied. Flag for Phase 3 that the single-DAPI-anchored topology was chosen (SCRI-03's `OverlappingDetections` wording may need revision — RESEARCH Open Q#1).
</output>
