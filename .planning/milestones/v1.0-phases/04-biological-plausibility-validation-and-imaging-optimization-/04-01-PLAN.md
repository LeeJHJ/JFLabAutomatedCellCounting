---
phase: 04-biological-plausibility-validation-and-imaging-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/03_export_val01_metrics.groovy
  - "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy"
  - scripts/val01_metrics.py
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "scripts/03_export_val01_metrics.groovy reads the exact measurement keys 02_detect_classify.groovy writes (Nucleus: Area µm^2, Nucleus: AF488-T3 mean (bg-sub), Cytoplasm: AF568-T2 mean (bg-sub)) plus getPathClass() — per D-03"
    - "scripts/val01_metrics.py computes and prints all four VAL-01 metrics (Double+/TdT+ ratio, per-region DAPI density/mm², nucleus-area peak bin, Fos+ control/SSp rate) from a synthetic fixture"
    - "the Groovy export is hard-copied byte-identically into the QuPath project scripts/ dir (dual-location deploy)"
  artifacts:
    - scripts/03_export_val01_metrics.groovy
    - "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy"
    - scripts/val01_metrics.py
    - "area_histogram_mode (function in val01_metrics.py)"
  key_links:
    - "the export TSV column names written by the Groovy script must match exactly what val01_metrics.py parses (per-cell: class, region_label, nucleus_area_um2, centroid_x, centroid_y, fos_bgsub, tdt_bgsub; per-region: region_label, hemisphere, acronym, is_leaf, area_mm2)"
---

<objective>
Author the two-stage VAL-01 measurement pipeline (D-03): a QuPath/Groovy per-cell + per-region export script, and a Python metrics script in the `braian` env that computes the four VAL-01 bioplausibility metrics from the export TSVs. This is the tooling slice — the scripts are authored and (for the Python side) proven against a synthetic fixture; the live QuPath run and the written record happen in Plan 04-03 (Wave 2).

Purpose: Reproducible, re-runnable computation of the VAL-01 numbers so the findings record (Plan 04-03) is grounded in a script, not hand arithmetic. The GUI/scriptable split (export = human-run; analysis = Claude-scriptable) matches the established project convention.
Output: `scripts/03_export_val01_metrics.groovy` (+ byte-identical QuPath-project hard-copy), `scripts/val01_metrics.py`.

## Artifacts this phase produces (full phase inventory, for source-grounding exclusion)
- `scripts/03_export_val01_metrics.groovy` (+ `M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy`) — NEW (this plan)
- `scripts/val01_metrics.py` with function `area_histogram_mode` — NEW (this plan)
- `scripts/opt01_zplane_audit.py` — NEW (Plan 04-02)
- `M3 Hippocampus 20x 062926 3 plane/results/val01_percell_export.tsv` — NEW output (produced by the human run in Plan 04-03)
- `M3 Hippocampus 20x 062926 3 plane/results/val01_region_area.tsv` — NEW output (Plan 04-03)
- `.planning/phases/04-.../04-VALIDATION-RECORD.md` — NEW deliverable (Plan 04-03)
- `.planning/phases/04-.../04-IMAGING-NOTES.md` — NEW deliverable (Plan 04-02)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-CONTEXT.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-PATTERNS.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 03_export_val01_metrics.groovy (D-03/D-04 per-cell + per-region export)</name>
  <read_first>
    - scripts/export_region_dapi_reference.groovy (primary structural analog — region loop + CSV write + pixel calibration + hemisphere/acronym regex-split, whole file 84 lines)
    - scripts/02_detect_classify.groovy (secondary — exact bg-sub measurement keys at lines 230-241; regionOf/regionLabel closures at lines 441-461; ROLLUP_CLASSES + Excluded at lines 403-492)
    - scripts/qc_detection_gates.groovy (pixel-calibration + WARNING fallback lines 57-66; area key line 82; Gson JSON parse lines 36, 183-196)
    - .planning/phases/04-.../04-PATTERNS.md (section "scripts/03_export_val01_metrics.groovy" — the composite analog assignment)
  </read_first>
  <files>scripts/03_export_val01_metrics.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy</files>
  <action>
    Author a new QuPath "Run for project" Groovy export following the `export_region_dapi_reference.groovy` structure (D-03). Copy its doc-comment shape (`@author section-pipeline`, scope, output path) and state explicitly in the header that this script MUST run AFTER `02_detect_classify.groovy` (it depends on `getPathClass()` and the `(bg-sub)` measurements already being written and saved). Copy the pixel-calibration idiom verbatim (`server.getPixelCalibration()`, `getAveragedPixelSizeMicrons()`, fallback constant `0.6905355` with the WARNING println) and `pxToMm2 = (pixelUm * 1e-3) ** 2`.

    Emit TWO tab-delimited outputs into the QuPath project's `results/` dir (NOT the growing cross-image `reference/` CSV — this is a single-run Phase-4 snapshot, write fresh each run):
    1. Per-cell TSV `results/val01_percell_export.tsv` — one row per detection with columns: `class` (from `getPathClass()?.toString() ?: "Negative"` — yields Double+/Fos+/TdT+/Negative/Excluded), `region_label` (via the `regionOf`/`regionLabel` closures reused verbatim from 02_detect_classify.groovy lines 441-461), `nucleus_area_um2` (read key `Nucleus: Area µm^2`), `centroid_x`/`centroid_y` (`getROI().getCentroidX()`/`getCentroidY()`, pixel space — NOT Atlas_X/Y/Z, which is deferred to v2 EXP-01/03), `fos_bgsub` (read key `Nucleus: AF488-T3 mean (bg-sub)`), `tdt_bgsub` (read key `Cytoplasm: AF568-T2 mean (bg-sub)`). These three measurement-key strings are CRITICAL and must be the exact literals 02_detect_classify.groovy writes (RESEARCH Pitfall 4 — the Phase-3 all-Negative root cause was a key mismatch); read them via `d.getMeasurements().get(key)` and write empty/NaN-safe values, never crash on a null.
    2. Per-region area TSV `results/val01_region_area.tsv` (D-04) — reuse the `export_region_dapi_reference.groovy` region loop verbatim (skip detection-container annotations, skip `AllDetections`, hemisphere/acronym regex-split, `is_leaf` via child-annotation emptiness), one row per leaf atlas-region annotation with columns: `region_label`, `hemisphere`, `acronym`, `is_leaf`, `area_mm2` (`roi.getArea() * pxToMm2`). Per-region class counts are NOT written here — Python (Plan 04-01 Task 2) derives them by grouping the per-cell TSV.

    Use the append-with-header-guard write idiom from the analog (write header if file absent), but since this is a per-run snapshot, overwrite (truncate) each run rather than append. Deploy dual-location: author in canonical `scripts/`, then hard-copy byte-identically into `M3 Hippocampus 20x 062926 3 plane/scripts/` (established convention).
  </action>
  <verify>
    <automated>grep -F 'Nucleus: Area µm^2' scripts/03_export_val01_metrics.groovy && grep -F 'Nucleus: AF488-T3 mean (bg-sub)' scripts/03_export_val01_metrics.groovy && grep -F 'Cytoplasm: AF568-T2 mean (bg-sub)' scripts/03_export_val01_metrics.groovy && grep -F 'getPathClass' scripts/03_export_val01_metrics.groovy && grep -F 'val01_percell_export.tsv' scripts/03_export_val01_metrics.groovy && grep -F 'val01_region_area.tsv' scripts/03_export_val01_metrics.groovy && cmp scripts/03_export_val01_metrics.groovy "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/03_export_val01_metrics.groovy` exists and contains all three exact measurement-key literals (`Nucleus: Area µm^2`, `Nucleus: AF488-T3 mean (bg-sub)`, `Cytoplasm: AF568-T2 mean (bg-sub)`) plus `getPathClass`.
    - Both output paths (`results/val01_percell_export.tsv`, `results/val01_region_area.tsv`) appear as string literals in the script.
    - The `regionOf`/`regionLabel` closures are present (grep for `regionOf` and `regionLabel`).
    - `cmp` confirms the canonical and QuPath-project copies are byte-identical.
    - The script never dereferences a measurement without a null guard (no bare `.doubleValue()` on an unchecked `get(key)` result).
  </acceptance_criteria>
  <done>Export script authored, contains the exact bg-sub/area keys and both TSV output paths, reuses the region closures, and is deployed byte-identically to both script locations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author val01_metrics.py (four VAL-01 metrics + area_histogram_mode) proven on a synthetic fixture</name>
  <read_first>
    - scripts/build_dapi_reference.py (primary analog — docstring/argparse/Path-typing/sys.exit-guard/pandas groupby-agg skeleton, whole file 105 lines)
    - .planning/phases/04-.../04-RESEARCH.md (section "Nucleus-Area-Peak Estimation Method" — the exact `area_histogram_mode` function, 10 µm² bins; and the VAL-01 per-metric writeups for what each metric means)
    - .planning/phases/04-.../04-PATTERNS.md (section "scripts/val01_metrics.py")
  </read_first>
  <files>scripts/val01_metrics.py</files>
  <behavior>
    - area_histogram_mode([45,46,47,52,120], bin_width=10.0) → returns the (40.0, 50.0, 3) modal bin (three values in [40,50)).
    - Given a per-cell fixture with 4 Double+, 10 TdT+, computes Double+/TdT+ ratio = 0.40 and reports the co-expression fraction Double+/(Double++TdT+) = 0.286.
    - Given a region-area fixture (CA1 area 0.5 mm², 1000 nuclei) computes DAPI density = 2000/mm².
    - Given SSp-labelled per-cell rows, computes the SSp Fos+ rate = (n Fos+ + n Double+)/n(SSp cells).
    - Runs to completion and prints all four metric labels for the fixture; `sys.exit` with a clear message if either input TSV is missing or lacks an expected column.
  </behavior>
  <action>
    Author `scripts/val01_metrics.py` in the `braian` env following `build_dapi_reference.py`'s shebang/docstring/usage/import shape (`from __future__ import annotations`, `argparse`, `pathlib.Path`, `numpy`, `pandas`). CLI: `--percell-tsv` (default `M3 Hippocampus 20x 062926 3 plane/results/val01_percell_export.tsv`), `--region-tsv` (default `.../results/val01_region_area.tsv`), `--out` (optional JSON dump of computed metrics). Use `formatter_class=RawDescriptionHelpFormatter`, `epilog=__doc__`.

    Implement the RESEARCH-specified `area_histogram_mode(areas_um2, bin_width=10.0)` verbatim (floor-based 10 µm² bins matching qc_detection_gates.groovy Gate-1, `np.unique` + `argmax`, NaN-drop). Compute the four VAL-01 metrics from the two TSVs:
    1. Double+/TdT+ ratio — `value_counts()` on the `class` column; report BOTH `n(Double+)/n(TdT+)` (the Phase-3 reported convention, ≈0.45) AND `n(Double+)/(n(Double+)+n(TdT+))` (fraction of TdT+ cells co-expressing Fos); print the target band 10–40% next to each. Also break the ratio down per hippocampal subfield (group by `region_label`) since region_label is a per-cell column (RESEARCH VAL-01 §1 candidate #4).
    2. Per-region DAPI density/mm² — group the per-cell TSV by `region_label` for total-nucleus counts, join to the region-area TSV on region label, `density = n_nuclei / area_mm2`; print CA1/CA2/CA3/DG-mo/DG-po/DG-sg densities against the 500–2,000/mm² band.
    3. Nucleus-area peak — run `area_histogram_mode` over the `nucleus_area_um2` column; ALSO print median, IQR, and `scipy.stats.skew` as bin-independent cross-checks (RESEARCH pitfalls); print against the 50–150 µm² band.
    4. Fos+ control rate — filter per-cell rows whose `region_label` starts with `SSp`; Fos+ rate = `(n Fos+ + n Double+)/n(SSp)`; also scan for fiber-tract labels (`fiber tracts`, `cc`, `fx`, `or`, `ec`, `em`) if present; print against 1–3%. This is a corroboration anchor only — the record (Plan 04-03) documents the absence of a true negative control.

    Add a defensive input guard (T-04-01): `sys.exit` with a clear message if a TSV is missing or is missing an expected column, and if the per-cell TSV has zero rows. Print all metrics to stdout in a labeled block (build_dapi_reference.py print convention) so Plan 04-03 can transcribe them.
  </action>
  <verify>
    <automated>printf 'class\tregion_label\tnucleus_area_um2\tcentroid_x\tcentroid_y\tfos_bgsub\ttdt_bgsub\nDouble+\tCA1\t45\t1\t1\t5000\t4000\nTdT+\tCA1\t46\t2\t2\t100\t4000\nFos+\tSSp\t47\t3\t3\t5000\t100\nNegative\tCA1\t52\t4\t4\t50\t50\nTdT+\tDG-mo\t120\t5\t5\t80\t4000\nNegative\tSSp\t44\t6\t6\t40\t40\n' > /tmp/pc.tsv && printf 'region_label\themisphere\tacronym\tis_leaf\tarea_mm2\nCA1\tLeft\tCA1\ttrue\t0.5\nSSp\tLeft\tSSp\ttrue\t1.0\nDG-mo\tLeft\tDG-mo\ttrue\t0.25\n' > /tmp/rg.tsv && conda run -n braian python3 scripts/val01_metrics.py --percell-tsv /tmp/pc.tsv --region-tsv /tmp/rg.tsv | grep -Ei 'ratio|density|peak|Fos'</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n braian python3 scripts/val01_metrics.py --percell-tsv <fixture> --region-tsv <fixture>` runs to completion and prints lines matching ratio, density, peak, and Fos.
    - `area_histogram_mode` returns `(40.0, 50.0, 3)` for the fixture areas [45,46,47,52,44] (three in [40,50)).
    - The Double+/TdT+ ratio and the co-expression fraction are both printed with the 10–40% target band shown.
    - Per-region density is computed by joining per-cell counts to region areas (grep the source for a pandas merge/join on region label).
    - A missing or column-short TSV triggers `sys.exit` with a clear message (not an uncaught traceback).
  </acceptance_criteria>
  <done>val01_metrics.py authored, computes all four VAL-01 metrics + area_histogram_mode + bin-independent cross-checks, guards malformed input, and passes the synthetic-fixture smoke test printing all four metric labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script-authored TSV → val01_metrics.py | The Python metrics script parses a TSV this pipeline itself generates on a single-operator local machine; the only "untrusted input" is a locally-authored file. No network, auth, or user-supplied input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | val01_metrics.py TSV input parsing | low | mitigate | Validate expected columns + non-zero row count before computing; `sys.exit` on malformed input (defensive coding, not a security control) |
| T-04-02 | Information Disclosure | local script outputs | low | accept | Outputs are local research TSVs/metrics with no secrets/PII; single-operator machine |

*Security domain assessed not-applicable for this documentation/analysis phase (RESEARCH §Security Domain): no network-facing service, no auth/session/access-control surface, no package installs. No high-severity threats; no supply-chain (T-*-SC) row because this phase installs nothing.*
</threat_model>

<verification>
- Both scripts exist and pass their automated checks above.
- The Groovy export's three exact bg-sub/area measurement-key literals match what `02_detect_classify.groovy` writes (RESEARCH Pitfall 4 guard).
- The per-cell + per-region TSV column names authored in the Groovy export are exactly what `val01_metrics.py` parses (key_link).
- Dual-location deploy is byte-identical (`cmp`).
</verification>

<success_criteria>
- `scripts/03_export_val01_metrics.groovy` and its QuPath-project hard-copy exist and are byte-identical.
- `scripts/val01_metrics.py` runs against a synthetic fixture and prints all four VAL-01 metrics.
- No live QuPath run performed in this plan (deferred to Plan 04-03 human step, per the GUI/scriptable split).
</success_criteria>

<output>
Create `.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-01-SUMMARY.md` when done.
</output>