---
phase: 04-biological-plausibility-validation-and-imaging-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/opt01_zplane_audit.py
  - .planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03]
must_haves:
  truths:
    - "04-IMAGING-NOTES.md records the acquired Z-plane count (6), Z-step (2.0 µm), and a concrete target plane-count recommendation for the next session — OPT-01"
    - "04-IMAGING-NOTES.md records the CZI raw size (~9.0 GB) and MIP OME-TIFF size (~0.97 GB) and a quantitative store-raw-Z vs MIP-now tradeoff — OPT-02"
    - "04-IMAGING-NOTES.md records a per-subfield resolution assessment (which subfields need 20x Airyscan vs a lower-power survey) grounded in the Phase-2 CA1-separable / DG-sg-not-separable finding — OPT-03"
    - "every numeric claim in the notes is tagged [measured] or [inferred] (D-06)"
    - "the OPT-01 plane-count plateau argument is empirical where data exists (3-plane vs hybrid DAPI counts from the existing region-TSVs) and explicitly scope-limited where it is not (single-plane variant never detection-run)"
  artifacts:
    - scripts/opt01_zplane_audit.py
    - .planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md
  key_links:
    - "opt01_zplane_audit.py reads the raw CZI metadata (aicspylibczi) + filesystem sizes + the two existing results/*_regions.tsv DAPI counts; its printed values are the [measured] facts transcribed into 04-IMAGING-NOTES.md"
---

<objective>
Produce `04-IMAGING-NOTES.md` — the forward-looking acquisition-recommendations record for the full series (OPT-01 Z-plane audit, OPT-02 file-size/storage tradeoff, OPT-03 resolution/subfield assessment). This is a self-contained vertical slice: a small Python audit script measures the empirical facts (CZI Z-count/step, file sizes, DAPI-count plateau across the existing MIP variants) and the notes document interprets them, every claim tagged `[measured]` vs `[inferred]` (D-06). Kept a separate document from the VAL-01 record per D-05 (different audience and lifetime).

Purpose: Concrete imaging guidance (target plane count, keep-raw-vs-MIP policy, which subfields actually need Airyscan) so the next imaging session and the full-series run are informed by this single-section run.
Output: `scripts/opt01_zplane_audit.py`, `04-IMAGING-NOTES.md`.

## OPT-01 open-question resolution (explicit, per RESEARCH Open Question 2)
Only the 3-plane variant has a confirmed classified `data.qpdata`. The single-plane (Z2) variant has NO detection run on disk; the 3-plane and hybrid variants DO have BraiAnDetect region-TSVs (`Num DAPI-T4`: 3-plane=213100, hybrid=209888). Resolution: compute the plateau argument **empirically from the two available region-TSVs (3-plane vs hybrid DAPI counts, ~1.5% apart) plus the optical framing**, and **explicitly scope-limit** OPT-01 to "a partial (2-of-3) empirical comparison — the single-plane variant was never detection-run; a full three-way empirical confirmation is deferred as an optional GUI step." This is the decisive resolution (option b, documented scope-down) — do NOT silently assume a full three-way comparison exists.

## Artifacts this phase produces
See the inventory in 04-01-PLAN.md `<objective>`. This plan adds `scripts/opt01_zplane_audit.py` and `04-IMAGING-NOTES.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-CONTEXT.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-RESEARCH.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author opt01_zplane_audit.py (CZI Z-count/step + file sizes + DAPI-count plateau)</name>
  <read_first>
    - czi_mip.py (analog — aicspylibczi open/inspect block lines 19-31: CziFile, get_dims_shape())
    - .planning/phases/04-.../04-RESEARCH.md (section "Code Examples" — the Scaling/Items metadata read for Z-step and pixel size; and "OPT-01 Plane-Count / Nyquist-for-MIP Framing" measured values)
    - .planning/phases/04-.../04-PATTERNS.md (section "OPT-01 CZI Z-plane-count / pixel-size read")
    - M3 Hippocampus 20x 062926 3 plane/results/M3_20x_MIP_Z1-3.ome.tiff - czi_to_mip_regions.tsv (region-TSV column layout: Image Name, Name, Classification, Area um^2, Num Detections, Num DAPI-T4)
  </read_first>
  <files>scripts/opt01_zplane_audit.py</files>
  <action>
    Author `scripts/opt01_zplane_audit.py` in the `braian` env (D-06: measure, do not hardcode). Follow `build_dapi_reference.py`'s CLI/docstring conventions (`argparse`, `type=Path`, `RawDescriptionHelpFormatter`, `epilog=__doc__`). Three measured outputs printed in a labeled block:
    1. OPT-01 CZI acquisition facts — open the raw CZI with `aicspylibczi.CziFile`, read `get_dims_shape()[0]` for `Z` (expect 6), `C` (3), `M` (187); read `czi.meta.find('.//Scaling/Items')` for the `Z` Distance value (expect 2E-6 m → 2.0 µm/plane) and `X` Distance value (expect 6.90535E-7 m → 0.690535 µm/px). Print total acquired Z-range = (Z-1) × step. Default CZI path `Automated Cell Counting/M3 Hippocampus 20x 062026.czi`, overridable via `--czi`.
    2. OPT-02 file sizes — `stat`/`Path.stat().st_size` on the CZI and the primary MIP OME-TIFF (`M3 Hippocampus 20x 062226/M3_20x_MIP_Z1-3.ome.tiff`); print bytes, GB, and the raw:MIP ratio (expect ~9.3×).
    3. OPT-01 plateau data — read the two existing region-TSVs in `M3 Hippocampus 20x 062926 3 plane/results/` (`M3_20x_MIP_Z1-3...regions.tsv` and `M3_20x_hybrid...regions.tsv`), extract the `Root` row's `Num DAPI-T4` count for each (expect 213100 vs 209888), print both and their percent difference. Print a clear note that the single-plane (Z2) variant has no region-TSV on disk (plateau comparison is 2-of-3, per the scope-down resolution).

    Guard with `sys.exit` + clear message if the CZI or a region-TSV is absent (defensive, T-04-01). Metadata-only CZI read (no pixel load) — must complete in seconds even on the ~9 GB file.
  </action>
  <verify>
    <automated>conda run -n braian python3 scripts/opt01_zplane_audit.py 2>&1 | grep -Ei 'Z-plane|Z step|2\.0|9\.|ratio|DAPI|213100|209888|plateau'</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n braian python3 scripts/opt01_zplane_audit.py` prints Z-plane count 6, Z-step 2.0 µm, pixel size ~0.6905 µm/px.
    - Prints CZI size ~9.0 GB, MIP size ~0.97 GB, and the raw:MIP ratio (~9.3×).
    - Prints both region-TSV `Num DAPI-T4` Root counts (213100, 209888) and their percent difference, plus the single-plane-not-run note.
    - Missing CZI / region-TSV triggers `sys.exit` with a clear message, not a traceback.
  </acceptance_criteria>
  <done>opt01_zplane_audit.py authored and prints all OPT-01 measured acquisition facts, OPT-02 file sizes + ratio, and the 2-of-3 DAPI-count plateau data.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-IMAGING-NOTES.md (OPT-01/02/03, [measured]/[inferred] labeled)</name>
  <read_first>
    - .planning/phases/04-.../04-RESEARCH.md (sections "OPT-01 Plane-Count / Nyquist-for-MIP Framing", "OPT-02 File Size / Storage Tradeoff", "OPT-03 Optical Resolution Argument" — the reasoned content + the subfield separability table + the Assumptions Log A3/A4/A5 hedges)
    - .planning/phases/04-.../04-CONTEXT.md (D-05, D-06, and the OPT-02 ~9 GB vs ~1 GB specific-idea line)
    - .planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md (the CA1-separable / DG-sg-not-separable empirical finding that anchors OPT-03)
    - the stdout of `scripts/opt01_zplane_audit.py` from Task 1 (the [measured] facts to transcribe)
  </read_first>
  <files>.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md</files>
  <action>
    Author `04-IMAGING-NOTES.md` (D-05: separate document from the VAL-01 record). Three sections, every numeric claim tagged `[measured]` or `[inferred]` (D-06):
    - **OPT-01 Z-plane audit** — transcribe the Task-1 measured facts (6 planes `[measured]`, 2.0 µm step `[measured]`, 10 µm acquired range `[measured]`). Frame the recommendation as a plateau argument: report the 3-plane vs hybrid DAPI-count difference (~1.5%, `[measured]`) as evidence the counts are already near-plateaued across the sub-range variants, give a concrete target plane count for the next session, and clearly scope-limit that this is a 2-of-3 empirical comparison (single-plane variant not detection-run — running it is an optional deferred GUI step). Include the Nyquist-for-3D-reconstruction framing ONLY as background context (`[inferred]`), explicitly noting per RESEARCH Pitfall 2 that 3D reconstruction is out of scope (this project's "3D" = atlas-space point cloud) so ~0.3 µm Nyquist sampling is NOT the operative question. Add the section-thickness cross-check (10 µm range ≈ 20–33% of a typical 30–50 µm vibratome section, `[inferred]`, A4) noting the limiting factor is 20x/0.8-NA imaging depth, not Z-step choice; flag the actual section thickness as an open item to confirm from wet-lab notes.
    - **OPT-02 file-size / storage tradeoff** — CZI ~9.00 GB `[measured]`, MIP ~0.97 GB `[measured]`, ratio ~9.3× `[measured]`. Weigh store-raw-Z vs MIP-now quantitatively against a full-series section count and the 854 GB free NVMe (~90 sections raw vs ~800+ MIP-only, `[inferred]` projection). Recommendation: keep raw CZI for this validation section and any where the OPT-01 plateau is inconclusive; MIP-immediately-and-discard-raw for the bulk once the plane-count target is locked.
    - **OPT-03 resolution assessment** — NA=0.8 air `[measured]`; confocal-equivalent lateral ~230–290 nm and axial ~1.0–1.3 µm `[inferred, formula]`; Airyscan ~1.7× relative gain extrapolated to NA 0.8 `[inferred]` (per RESEARCH Pitfall 3 / A3, always pair a resolution number with "at NA=0.8"). Core answer grounded in the Phase-2 empirical finding `[measured]`: CA1 cleanly per-cell separable, DG-sg not per-cell separable even at 20x Airyscan (density-only, excluded). Reproduce the per-subfield table (CA1/CA2/CA3/DG-mo/DG-po/DG-sg/cortex) with the Airyscan-need column; conclude that classification (compartment-mean intensities on >7 µm nuclei) does not require Airyscan super-resolution in any subfield, that a lower-power confocal survey plausibly suffices for CA1/CA2/CA3/DG-mo/DG-po/cortex, and that DG-sg stays density-only regardless of resolution tier. Flag CA2/CA3 as extrapolated-from-CA1 (`[inferred]`, A5) needing a quick visual QC in the series, and flag the whole recommendation as needing a paired Airyscan-vs-confocal acquisition before being treated as locked.

    Cite the TRAP2 paper (bioRxiv 2024.09.16.611953 / F1000Research 15:410) provenance honestly (403-blocked this session; acquisition parameters not paper-verified — an open item). Keep prose consistent with the sibling `.planning/phases/*/` document register (measured vs interpreted framing, no code).
  </action>
  <verify>
    <automated>test -f ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md" && grep -qi 'OPT-01' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md" && grep -qi 'OPT-02' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md" && grep -qi 'OPT-03' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md" && grep -qF '[measured]' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md" && grep -qF '[inferred]' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-IMAGING-NOTES.md"</automated>
  </verify>
  <acceptance_criteria>
    - `04-IMAGING-NOTES.md` exists and has a dedicated section for each of OPT-01, OPT-02, OPT-03.
    - OPT-01 states 6 acquired planes, a concrete target plane count, the 2-of-3 plateau scope-limit, and separates Nyquist-for-3D from the operative MIP question.
    - OPT-02 states both file sizes, the ~9.3× ratio, and a full-series store-raw-vs-MIP recommendation.
    - OPT-03 states the per-subfield Airyscan-need assessment anchored on the Phase-2 CA1/DG-sg separability finding.
    - Both `[measured]` and `[inferred]` tags appear; every resolution number is paired with "at NA=0.8" or `[inferred]`.
    - No metric is framed as a pass/fail gate; the TRAP2-paper provenance is hedged honestly.
  </acceptance_criteria>
  <done>04-IMAGING-NOTES.md written with all three OPT items, each carrying measured facts + interpretation, D-06 claim labels applied throughout, and the OPT-01 plateau scope-limit stated explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CZI / region-TSV / filesystem → opt01_zplane_audit.py | The audit script reads local microscopy files and pipeline-authored TSVs on a single-operator machine; metadata-only, no network/auth/user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | opt01_zplane_audit.py file inputs | low | mitigate | `sys.exit` with a clear message if the CZI or a region-TSV is missing/short before parsing (defensive coding, not a security control) |
| T-04-04 | Information Disclosure | 04-IMAGING-NOTES.md local doc | low | accept | Forward-looking research recommendations, no secrets/PII; single-operator machine |

*Documentation/analysis phase — no network-facing service, no auth/access-control surface, no package installs (RESEARCH §Security Domain). No high-severity threats; no supply-chain row (nothing installed).*
</threat_model>

<verification>
- `scripts/opt01_zplane_audit.py` runs and prints all OPT-01/02 measured facts + the plateau data.
- `04-IMAGING-NOTES.md` covers all three OPT items with `[measured]`/`[inferred]` labels.
- OPT-01's plateau claim is empirical (2-of-3) with the single-plane scope-down stated.
</verification>

<success_criteria>
- All three OPT requirements documented with a value and an interpretation.
- The imaging notes are a separate document from the VAL-01 record (D-05).
- The Nyquist-for-3D vs MIP-adequacy distinction is drawn explicitly (RESEARCH Pitfall 2).
</success_criteria>

<output>
Create `.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-02-SUMMARY.md` when done.
</output>
