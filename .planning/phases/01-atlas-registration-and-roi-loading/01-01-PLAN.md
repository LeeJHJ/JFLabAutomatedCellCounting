---
phase: 01-atlas-registration-and-roi-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/01_load_abba_rois.groovy
  - M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy
autonomous: true
requirements: [SCRI-01]
must_haves:
  truths:
    - "The central scripts directory /home/jflab/Analysis/scripts/ exists"
    - "01_load_abba_rois.groovy exists in Analysis/scripts/ and is a valid QuPath Groovy script"
    - "The script is hard-copied into the target QuPath project's scripts/ directory"
    - "The script guards against missing ABBA files (early return if getAvailableAtlasRegistration is empty)"
    - "The script uses the verified 4-argument loadWarpedAtlasAnnotations form and calls resolveHierarchy()"
  artifacts:
    - "/home/jflab/Analysis/scripts/01_load_abba_rois.groovy"
    - "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy"
  key_links:
    - "atlasName variable 'allen_mouse_10um_java' matches the atlas key in ABBA export files (D-06)"
    - "clearAllObjects() guard comment warns re-run after Phase 3 wipes cell data (D-05)"
    - "QuPath Run for project resolves scripts only from <project>/scripts/ so the copy is required (D-11)"
---

## Phase Goal

**As a** TRAP2 section researcher, **I want to** load Allen CCFv3 atlas region annotations into the M3 hippocampus QuPath entry and visually verify they align to tissue, **so that** I can proceed to cell detection knowing every cell will be assigned to a correct, trusted brain region.

> Phase 1 delivers this goal across three vertical waves: (01) the ROI-loading script as a runnable artifact, (02) the ABBA registration files it consumes (human GUI in Fiji), (03) executing the script + capturing the QC overlay. After each wave the researcher can do something new: hold a runnable script → have registration files on disk → see verified atlas annotations over tissue.

## Artifacts This Phase Produces

| Artifact | Produced By | Requirement |
|----------|-------------|-------------|
| `/home/jflab/Analysis/scripts/` (directory) | Plan 01 Task 1 | SCRI-01 |
| `/home/jflab/Analysis/scripts/01_load_abba_rois.groovy` | Plan 01 Task 2 | SCRI-01 |
| `M3 Hippocampus 20x 062926 3 plane/scripts/` (directory) | Plan 01 Task 1 | SCRI-01 |
| `M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy` | Plan 01 Task 3 | SCRI-01 |
| `data/1/ABBA-Transform-allen_mouse_10um_java.json` | Plan 02 (Fiji GUI) | REG-01 |
| `data/1/ABBA-RoiSet-allen_mouse_10um_java.zip` | Plan 02 (Fiji GUI) | REG-01 |
| Populated `data/1/data.qpdata` + updated `data/1/summary.json` (>50 annotations) | Plan 03 Task 1 | SCRI-01 |
| `data/1/registration_QC.jpg` | Plan 03 Task 3 | REG-02 |

<objective>
Deliver the ROI-loading script as a working, deployable artifact: create the canonical scripts directory, author `01_load_abba_rois.groovy` using the JAR-verified ABBA API, and hard-copy it into the target QuPath project so it is runnable via "Run for project".

Purpose: This is the scriptable half of Phase 1 (SCRI-01). It can be authored before ABBA registration exists because the script self-guards on missing ABBA files — writing it first unblocks the human GUI work and gives the researcher a script that fails loudly rather than silently.
Output: `scripts/01_load_abba_rois.groovy` (canonical) and its hard-copy in `M3 Hippocampus 20x 062926 3 plane/scripts/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-atlas-registration-and-roi-loading/01-CONTEXT.md
@.planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md
@.planning/phases/01-atlas-registration-and-roi-loading/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts directories</name>
  <files>scripts/, M3 Hippocampus 20x 062926 3 plane/scripts/</files>
  <read_first>
    - .planning/phases/01-atlas-registration-and-roi-loading/01-CONTEXT.md (D-10, D-11: directory locations and copy workflow)
    - .planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md (Recommended Project Structure; Environment Availability: both dirs marked DOES NOT EXIST)
  </read_first>
  <action>
    Create the two directories that do not yet exist (per D-10 and D-11):
      mkdir -p /home/jflab/Analysis/scripts
      mkdir -p "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts"
    The first is the canonical central location for all pipeline Groovy scripts (D-10). The second is the QuPath project's own scripts directory, required because QuPath "Run for project" resolves scripts only from `<project>/scripts/` (D-11, RESEARCH Anti-Patterns). Do not touch the older `M3 Hippocampus 20x 062226/` project (D-04).
  </action>
  <verify>
    <automated>test -d /home/jflab/Analysis/scripts && test -d "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Directory /home/jflab/Analysis/scripts/ exists
    - Directory "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts/" exists
    - The older 062226 project directory is untouched
  </acceptance_criteria>
  <done>Both scripts directories exist on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Author 01_load_abba_rois.groovy in canonical scripts dir</name>
  <files>scripts/01_load_abba_rois.groovy</files>
  <read_first>
    - .planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md (Code Examples: "Complete 01_load_abba_rois.groovy Skeleton" — authoritative, extracted from installed JAR; Common Pitfalls 1-6)
    - .planning/phases/01-atlas-registration-and-roi-loading/01-PATTERNS.md (Imports at bottom; header/Javadoc; section divider style; guard pattern; core API pattern; summary print pattern)
    - Automated Cell Counting Test/scripts/Test 062026 1.groovy (structural analog: header block, println usage, imports-at-bottom convention)
  </read_first>
  <action>
    Write /home/jflab/Analysis/scripts/01_load_abba_rois.groovy implementing the JAR-verified ABBA ROI loading pattern. The script MUST contain, in order:

    1. A Javadoc header block (`/** ... */`) with a one-line description, a REQUIREMENTS section (QuPath ABBA extension v0.4.0 installed; image registered in Fiji ABBA; ABBA-Transform-allen_mouse_10um_java.json and ABBA-RoiSet-allen_mouse_10um_java.zip present in data/N/), a USAGE section (copy to project scripts/ and run via Automate then "Run for project"), and an @author tag placeholder.
    2. A Configuration section defining `def atlasName = 'allen_mouse_10um_java'` with an inline comment "D-06: must match ABBA export atlas name" (D-06).
    3. An entry-name print at the top: `println "Running on: " + getCurrentImageData().getServer().getMetadata().getName()` to catch the wrong-entry error (Pitfall 5).
    4. A guard block calling `qupath.ext.biop.abba.AtlasTools.getAvailableAtlasRegistration(getCurrentImageData())`; if the result `.isEmpty()`, print an ERROR message directing the researcher to run ABBA export in Fiji first and `return` early (Pitfall 1 — silent no-op).
    5. `clearAllObjects()` preceded by a multi-line comment warning that it removes ALL objects including cell detections, that it is safe in Phase 1 (no cells yet), and that re-running after Phase 3 detection wipes cell data — with the two documented remedies from RESEARCH Pitfall 6 (D-05). Do NOT reproduce any negative-grep literal as a bare token; the comment describes the risk in prose.
    6. The core load call using the verified 4-argument public form:
       `def atlasRoot = qupath.ext.biop.abba.AtlasTools.loadWarpedAtlasAnnotations(getCurrentImageData(), "acronym", true, true)`
       with an inline comment documenting the arguments (ImageData, namingProperty, splitLeftRight, overwrite). Do NOT use the internal 5-argument AtlasOntology overload (RESEARCH Anti-Patterns).
    7. `resolveHierarchy()` with a comment stating it establishes parent-child nesting required for BraiAnDetect region assignment (Claude's Discretion decision; Pitfall 3).
    8. A Summary section printing `getAnnotationObjects().size()` and a "Next step: File > Save project, then take QC screenshot." line.
    9. `import qupath.ext.biop.abba.AtlasTools` at the BOTTOM of the file (QuPath convention, PATTERNS "Imports at bottom").

    Use `def` for all declarations, `println` for output, and `// ── Section ──` dividers (PATTERNS Shared Patterns).
  </action>
  <verify>
    <automated>test -f /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && grep -q "allen_mouse_10um_java" /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && grep -q "loadWarpedAtlasAnnotations" /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && grep -q "getAvailableAtlasRegistration" /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && grep -q "resolveHierarchy" /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && grep -q "clearAllObjects" /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && grep -q "import qupath.ext.biop.abba.AtlasTools" /home/jflab/Analysis/scripts/01_load_abba_rois.groovy && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File /home/jflab/Analysis/scripts/01_load_abba_rois.groovy exists
    - Contains `def atlasName = 'allen_mouse_10um_java'`
    - Contains a guard on getAvailableAtlasRegistration with an early `return` when empty
    - Contains clearAllObjects() with a warning comment about wiping cell detections
    - Calls loadWarpedAtlasAnnotations with exactly 4 arguments (getCurrentImageData(), "acronym", true, true) — not the AtlasOntology overload
    - Calls resolveHierarchy() after the load
    - Import statement for qupath.ext.biop.abba.AtlasTools is at the bottom of the file
  </acceptance_criteria>
  <done>The script exists and contains the guard, clearAllObjects with warning, 4-arg load, resolveHierarchy, and bottom import.</done>
</task>

<task type="auto">
  <name>Task 3: Hard-copy script into target QuPath project scripts dir</name>
  <files>M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy</files>
  <read_first>
    - .planning/phases/01-atlas-registration-and-roi-loading/01-CONTEXT.md (D-11: scripts hard-copied into each QuPath project's scripts/ for "Run for project")
    - scripts/01_load_abba_rois.groovy (the canonical source authored in Task 2)
  </read_first>
  <action>
    Copy the canonical script into the target project so QuPath "Run for project" can find it (D-11):
      cp /home/jflab/Analysis/scripts/01_load_abba_rois.groovy "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy"
    This is a byte-identical hard copy, not a symlink — QuPath project scripts directories hold real files. Copy ONLY into the 062926 3 plane project (D-01/D-04); do not copy into the older 062226 project.
  </action>
  <verify>
    <automated>diff /home/jflab/Analysis/scripts/01_load_abba_rois.groovy "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy" exists
    - It is byte-identical to the canonical /home/jflab/Analysis/scripts/01_load_abba_rois.groovy (diff returns no differences)
    - No copy was made into the 062226 project
  </acceptance_criteria>
  <done>The project scripts/ directory contains a byte-identical copy of the canonical script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher → filesystem | Groovy script authored locally; no external/network input crosses into the script |
| Groovy script → QuPath project data | Script mutates data/1/ objects via clearAllObjects and loadWarpedAtlasAnnotations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | 01_load_abba_rois.groovy content | low | mitigate | Script authored by researcher only; no external input is interpolated into script logic; atlasName is a hardcoded literal (RESEARCH Security Domain V5) |
| T-01-02 | Denial of Service | clearAllObjects() accidental data loss | low | mitigate | Prominent multi-line warning comment (D-05); Phase 1 runs before any cell data exists so no data can be lost yet |
| T-01-03 | Tampering | Atlas file path traversal | low | accept | Paths are derived from QuPath project structure, not from user input (RESEARCH Security Domain: Very low risk, local single-user offline pipeline) |
</threat_model>

<verification>
- `/home/jflab/Analysis/scripts/` and the project `scripts/` directory both exist
- `01_load_abba_rois.groovy` present in both locations and byte-identical
- Script contains the guard, clearAllObjects warning, 4-arg load, resolveHierarchy, and bottom import (grep checks in each task)
- No modifications made to the older 062226 project
</verification>

<success_criteria>
- SCRI-01 script artifact authored and deployed (execution/running of it is verified in Plan 03 after ABBA files exist)
- Both required directories created
- Script is a valid QuPath Groovy file matching project conventions
</success_criteria>

<output>
Create `.planning/phases/01-atlas-registration-and-roi-loading/01-01-SUMMARY.md` when done
</output>
