---
phase: 01-atlas-registration-and-roi-loading
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - M3 Hippocampus 20x 062926 3 plane/data/1/data.qpdata
  - M3 Hippocampus 20x 062926 3 plane/data/1/summary.json
  - M3 Hippocampus 20x 062926 3 plane/data/1/registration_QC.jpg
autonomous: false
requirements: [SCRI-01, REG-02]
user_setup:
  - service: QuPath v0.6.0
    why: "Running the Groovy script and taking the QC screenshot require the QuPath GUI on the researcher's monitor"
    dashboard_config:
      - task: "Open project, select entry 1, run 01_load_abba_rois.groovy via Run for project, save, then screenshot the viewer"
        location: "QuPath: Automate > Run for project; File > Save; manual screenshot to data/1/registration_QC.jpg"
must_haves:
  truths:
    - "Running 01_load_abba_rois.groovy on entry 1 populates the annotation list with atlas regions (>50 annotations)"
    - "Re-running the script does not duplicate annotations (clearAllObjects guard works)"
    - "A QC screenshot shows CA1, CA3, DG subfields + surrounding cortex + ventral edge aligned to atlas outlines"
  artifacts:
    - "M3 Hippocampus 20x 062926 3 plane/data/1/registration_QC.jpg"
    - "Updated M3 Hippocampus 20x 062926 3 plane/data/1/summary.json (Annotations > 50)"
  key_links:
    - "Script runs on entry 1 (getCurrentImageData) so annotations land in data/1/, verified via summary.json (Pitfall 5)"
    - "resolveHierarchy() produced nested parent-child regions so BraiAnDetect can roll up counts (Pitfall 3)"
    - "QC image saved to data/1/registration_QC.jpg alongside ABBA files (D-09)"
---

<objective>
Execute the ROI-loading script on the registered section, confirm atlas annotations populate without duplication, and capture the registration-overlay QC image. This closes SCRI-01 (script runs cleanly, idempotent) and REG-02 (visual QC).

Purpose: This is the end-to-end payoff of Phase 1 — after this plan, the M3 QuPath entry carries the full Allen CCFv3 annotation hierarchy, visually verified against tissue, and is ready for Phase 2 detection. It depends on the script (Plan 01) and the ABBA files (Plan 02) both existing.
Output: populated annotation hierarchy in data/1/ (persisted to data.qpdata/summary.json) and `data/1/registration_QC.jpg`.

This plan is NOT autonomous — running the script and screenshotting are QuPath GUI actions performed by the researcher. Claude provides the automated filesystem verification of the results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-atlas-registration-and-roi-loading/01-CONTEXT.md
@.planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md
@M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Run 01_load_abba_rois.groovy on entry 1 and confirm annotations load</name>
  <files>M3 Hippocampus 20x 062926 3 plane/data/1/data.qpdata, M3 Hippocampus 20x 062926 3 plane/data/1/summary.json</files>
  <read_first>
    - .planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md (Code Examples: "Verifying ROI Load Succeeded" one-liner; Pitfalls 1,3,5; Open Question 3 on explicit save)
    - M3 Hippocampus 20x 062926 3 plane/scripts/01_load_abba_rois.groovy (the script being run)
  </read_first>
  <action>
    HUMAN-ONLY (QuPath GUI, not automatable). Hand off to the researcher: open the "M3 Hippocampus 20x 062926 3 plane" project, open entry 1 (M3_20x_MIP_Z1-3.ome.tiff), and run 01_load_abba_rois.groovy via Automate > Run for project on that entry (confirm the script's "Running on:" print names entry 1 — Pitfall 5), then File > Save so summary.json updates (Open Question 3). Follow the exact steps in how-to-verify. Claude then runs the annotation-count check against data/1/summary.json expecting >50 annotations. Claude cannot execute the GUI.
  </action>
  <what-built>
    The researcher runs the script in QuPath; Claude provides the run instructions and the automated post-run annotation-count check.
  </what-built>
  <how-to-verify>
    Researcher steps (QuPath GUI, DISPLAY=:0):
    1. Launch QuPath: /home/jflab/section-pipeline/tools/QuPath/bin/QuPath
    2. Open project "M3 Hippocampus 20x 062926 3 plane/project.qpproj".
    3. Select and open entry 1 (M3_20x_MIP_Z1-3.ome.tiff). Confirm the script's "Running on:" print shows this entry name (Pitfall 5).
    4. Automate > Run for project (or Script Editor > Run) with 01_load_abba_rois.groovy on entry 1.
    5. Confirm the log prints "Found ABBA registrations", "Loaded atlas annotations", "Hierarchy resolved", and a total annotation count.
    6. File > Save the project (summary.json only updates on explicit save — Open Question 3).

    Automated verification (run after save):
      python3 -c "import json; d=json.load(open('/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/summary.json')); a=d['hierarchy']['objectTypeCounts'].get('Annotations',0); assert a>50, f'Only {a} annotations'; print('PASS annotations:', a)"
    Expected: roughly 250-270 annotations (varies by section).
  </how-to-verify>
  <verify>
    <human-check>Researcher confirms the script log shows the load succeeded on entry 1 and the project was saved</human-check>
    <automated>python3 -c "import json; d=json.load(open('/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/summary.json')); a=d['hierarchy']['objectTypeCounts'].get('Annotations',0); assert a>50, f'Only {a} annotations loaded'; print('PASS annotations:', a)"</automated>
  </verify>
  <acceptance_criteria>
    - summary.json for data/1 reports more than 50 annotations
    - The script ran on entry 1 (log "Running on:" confirms the M3_20x_MIP_Z1-3.ome.tiff entry)
    - Log shows resolveHierarchy() completed (nested regions, not a flat list)
  </acceptance_criteria>
  <resume-signal>Type "loaded" once summary.json shows >50 annotations on entry 1, or describe the error</resume-signal>
  <done>Atlas annotations populate entry 1; summary.json reports >50 annotations.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Confirm idempotency — re-run does not duplicate annotations</name>
  <files>M3 Hippocampus 20x 062926 3 plane/data/1/summary.json</files>
  <read_first>
    - .planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md (Pitfall 2: duplicate annotation loading; Test Map idempotency row)
  </read_first>
  <action>
    HUMAN-ONLY (QuPath GUI, not automatable). Hand off to the researcher: re-run 01_load_abba_rois.groovy on entry 1 a second time via Automate > Run for project, then File > Save. This exercises the clearAllObjects guard (Pitfall 2). Claude then checks data/1/summary.json: the annotation count must remain a single load (50-400 range), NOT roughly double (~520). Claude cannot execute the GUI.
  </action>
  <what-built>
    The researcher re-runs the same script a second time; Claude checks the annotation count did not roughly double (proves the clearAllObjects guard works).
  </what-built>
  <how-to-verify>
    Researcher steps:
    1. Note the annotation count from Task 1 (call it N).
    2. Re-run 01_load_abba_rois.groovy on entry 1 (Automate > Run for project).
    3. File > Save.

    Automated verification: the count must stay at N (within a small tolerance), NOT approximately 2N. Compare against the Task 1 value:
      python3 -c "import json; d=json.load(open('/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/summary.json')); a=d['hierarchy']['objectTypeCounts'].get('Annotations',0); print('annotations after re-run:', a); assert a<400, f'Count {a} suggests duplication (2x ~260)'"
    A count near ~260 = PASS (guard worked). A count near ~520 = FAIL (clearAllObjects not clearing — investigate).
  </how-to-verify>
  <verify>
    <human-check>Researcher confirms annotation count after the second run is roughly the same as the first (not doubled)</human-check>
    <automated>python3 -c "import json; d=json.load(open('/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/summary.json')); a=d['hierarchy']['objectTypeCounts'].get('Annotations',0); assert 50<a<400, f'Count {a} indicates duplication or load failure'; print('PASS idempotent annotations:', a)"</automated>
  </verify>
  <acceptance_criteria>
    - Annotation count after the second run is between 50 and 400 (single load, not doubled)
    - No approximate 2x growth (would indicate clearAllObjects failed)
  </acceptance_criteria>
  <resume-signal>Type "idempotent" once the re-run count is confirmed not doubled</resume-signal>
  <done>Re-running the script leaves the annotation count essentially unchanged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Capture registration QC overlay screenshot</name>
  <files>M3 Hippocampus 20x 062926 3 plane/data/1/registration_QC.jpg</files>
  <read_first>
    - .planning/phases/01-atlas-registration-and-roi-loading/01-CONTEXT.md (D-07, D-08, D-09: manual screenshot, required anatomy, save path)
    - .planning/phases/01-atlas-registration-and-roi-loading/01-RESEARCH.md (REG-02 Test Map row)
  </read_first>
  <action>
    HUMAN-ONLY (QuPath GUI + screenshot, not automatable). Hand off to the researcher: with entry 1 open and annotations loaded, frame the viewer so CA1, CA3, DG subfields + surrounding cortex + ventral brain edge are all visible and confirm atlas outlines follow tissue boundaries (D-08 — high accuracy bar; if misaligned, return to Plan 02 and re-register). Save the screenshot to data/1/registration_QC.jpg (D-09). Claude then verifies the file exists at that path. Claude cannot execute the GUI or take the screenshot.
  </action>
  <what-built>
    The researcher screenshots the QuPath viewer showing atlas region outlines over tissue; Claude verifies the file exists at the expected path.
  </what-built>
  <how-to-verify>
    Researcher steps (QuPath GUI):
    1. With entry 1 open and annotations loaded, zoom the viewer so ALL of the following are visible at once: CA1, CA3, DG hippocampal subfield boundaries, surrounding cortex, and the ventral brain edge (D-08).
    2. Visually confirm the atlas region outlines follow the tissue boundaries. If the ventral edge or any subfield boundary is off, the registration is inadequate — go back to Plan 02 and re-register before accepting.
    3. Take a screenshot and save it as:
       /home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/registration_QC.jpg (D-09)

    Automated verification:
      test -f "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/registration_QC.jpg" && echo "PASS QC image present"
  </how-to-verify>
  <verify>
    <human-check>Researcher confirms the QC image shows CA1/CA3/DG + cortex + ventral edge all aligned to atlas outlines; if misaligned, re-registration is required before accepting</human-check>
    <automated>test -f "/home/jflab/Analysis/M3 Hippocampus 20x 062926 3 plane/data/1/registration_QC.jpg" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File data/1/registration_QC.jpg exists (non-zero size)
    - The image shows CA1, CA3, DG subfields + surrounding cortex + ventral brain edge (per researcher visual confirmation)
    - Atlas outlines visually align to tissue boundaries (researcher's high-accuracy bar met, D-08)
  </acceptance_criteria>
  <resume-signal>Type "QC approved" once the aligned overlay screenshot is saved, or "re-register" if alignment fails</resume-signal>
  <done>registration_QC.jpg exists in data/1/ and shows the required anatomy aligned to atlas outlines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher (QuPath GUI) → project data | Running the script mutates data/1/ objects; screenshot writes an image file |
| Groovy script → data.qpdata | clearAllObjects + loadWarpedAtlasAnnotations rewrite the annotation store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Denial of Service | Duplicate annotation loads | low | mitigate | Idempotency check (Task 2) confirms clearAllObjects guard prevents 2x growth (Pitfall 2) |
| T-01-07 | Denial of Service | Script run on wrong entry | low | mitigate | Script prints entry name at start; annotation count verified in entry 1's summary.json (Pitfall 5) |
| T-01-08 | Repudiation | Unverified registration quality | low | mitigate | registration_QC.jpg is the durable visual audit record (REG-02, D-08/D-09) |
</threat_model>

<verification>
- summary.json for data/1 reports >50 annotations after first run (SCRI-01 runs cleanly)
- Second run does not double the annotation count (SCRI-01 idempotency)
- registration_QC.jpg exists and shows required anatomy aligned (REG-02)
</verification>

<success_criteria>
- SCRI-01 fully satisfied: script runs cleanly on entry 1, annotations populate, re-run does not duplicate
- REG-02 satisfied: registration overlay QC image produced and visually verified
- Phase 1 goal met: atlas annotations loaded, verified against tissue, ready for Phase 2 detection
</success_criteria>

<output>
Create `.planning/phases/01-atlas-registration-and-roi-loading/01-03-SUMMARY.md` when done
</output>
