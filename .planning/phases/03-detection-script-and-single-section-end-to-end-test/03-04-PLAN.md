---
phase: 03-detection-script-and-single-section-end-to-end-test
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: [SCRI-03]
must_haves:
  truths:
    - "A human runs 02_detect_classify.groovy in QuPath on the M3 entry and confirms all four Phase-3 success criteria pass on the final bg-sub-based script"
  artifacts: []
  key_links:
    - "This is the phase gate: SC1 (runs+writes data.qpdata), SC2 (4 classes + atlas labels), SC3 (Atlas_X 5,000–10,000 µm), SC4 (CA1/CA2/CA3/DG count table) must all hold together"
---

## Phase Goal (MVP user story)

**As a** TRAP2 neuroscience researcher, **I want to** run the finished `02_detect_classify.groovy` once end-to-end in QuPath and see all four success criteria hold together, **so that** I can trust the classified M3 section and lock the classifier as series-ready.

<objective>
Human-in-the-loop phase gate. The script is authored autonomously across Plans 01–03; this plan is the single consolidated verification that a human runs it in QuPath on M3 entry 1 and confirms all four Phase-3 success criteria pass on the FINAL (bg-sub-based) script. There is no automated test framework for this GUI pipeline (03-VALIDATION.md), so this manual run is the authoritative phase gate.

Purpose: Confirm SCRI-03 end-to-end on the real M3 entry before Phase 3 is considered done.
Output: verified pass (or a recorded gap list for gap-closure planning).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-detection-script-and-single-section-end-to-end-test/03-VALIDATION.md
@.planning/phases/03-detection-script-and-single-section-end-to-end-test/03-CONTEXT.md
</context>

## Artifacts this phase produces (source-grounding reference)

No new source symbols — this plan runs and verifies the artifacts created in Plans 01–03. (Full artifact list is in 03-01-PLAN.md and 03-03-PLAN.md.)

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run 02_detect_classify.groovy end-to-end and confirm all four success criteria</name>
  <files>none (GUI verification run — no files modified)</files>
  <read_first>
    - .planning/phases/03-detection-script-and-single-section-end-to-end-test/03-VALIDATION.md (Manual-Only Verifications table — the four SC instructions)
    - scripts/classify_markers.groovy (base reference for the class-breakdown printout the human reads)
    - scripts/02_detect_classify.groovy (the final script under test)
  </read_first>
  <what-built>
    A complete `02_detect_classify.groovy` (deployed in both `scripts/` and `M3 Hippocampus 20x 062926 3 plane/scripts/`) that: guards zero detections (D-02); computes a compartment-agnostic local-background-subtracted measure for Fos and TdT (D-04); re-derives positive thresholds on that measure via ChannelHistogram (D-05); classifies each nucleus into TdT+/Fos+/Double+/Negative (+ Excluded for DG-sg/VS); attaches an ABBA atlas region label per cell; rolls per-class counts onto CA1/CA2/CA3/DG annotation measurements; and prints an Atlas_X micron sanity check. New bgsub classifier JSONs hold the re-derived thresholds.
  </what-built>
  <action>
    Human runs the finished script in QuPath and confirms all four Phase-3 success criteria on the M3 entry. No code changes in this task — it is the phase gate. Steps:
    1. Open QuPath 0.6.0 and the `M3 Hippocampus 20x 062926 3 plane` project. Ensure `run_braian_detection.groovy` has already produced DAPI-T4 detections on entry 1 (if not, run it first — D-01: detection lives there).
    2. Run `02_detect_classify.groovy` via "Run for project" (or single-entry run on entry 1). Watch the log.
    3. SC1 — confirm no red error in the QuPath log and that entry 1's `data.qpdata` mtime updates (file saved with classified cells).
    4. SC2 — confirm the class-breakdown printout shows all four of TdT+, Fos+, Double+, Negative present (non-zero), and the sample region-label printout shows classified cells carrying ABBA region acronyms (e.g. CA1, CA3, DG-mo).
    5. SC3 — read the Atlas_X sanity print: values should be in [5000, 10000] µm. If they read ~500–1000, apply the documented ×10 fallback (10 µm/voxel) and re-confirm the corrected range.
    6. SC4 — open the Annotations tab / measurement table and confirm CA1, CA2, CA3, and DG (subfield) annotations show populated `Count: Negative/Fos+/TdT+/Double+` columns matching the console table.
    7. D-05 self-check — confirm the raw-measure re-derivation printout lands near 13000.4538 (Fos) / 16766.4671 (TdT); and that reclassifying is idempotent (re-run leaves counts stable, D-02).
    8. D-03/D-04 spot-check (advisory, not a hard SC gate) — if SSp is present, confirm its Fos+ false-positive fraction is visibly lower than the Phase-2 raw-threshold run.
  </action>
  <how-to-verify>
    Follow the numbered steps in the action; each maps to one success criterion (steps 3→SC1, 4→SC2, 5→SC3, 6→SC4) plus the D-05 self-check (step 7) and the D-03/D-04 advisory spot-check (step 8).
  </how-to-verify>
  <verify>
    <human-check>All four success criteria observed in a single QuPath run: no error + data.qpdata saved (SC1); four classes + atlas labels (SC2); Atlas_X in 5,000–10,000 µm (SC3); CA1/CA2/CA3/DG Count: * columns in the annotation pane (SC4). Raw-measure self-check lands near the locked absolute thresholds.</human-check>
  </verify>
  <acceptance_criteria>
    - SC1: script runs with no error and updates `data.qpdata`.
    - SC2: all four classes present and non-zero; sample cells carry ABBA region acronyms.
    - SC3: printed Atlas_X values fall in 5,000–10,000 µm (after the documented ×10 fallback if needed).
    - SC4: CA1/CA2/CA3/DG annotations show populated `Count: *` columns in the measurement table.
    - D-05 self-check: raw-measure re-derivation lands near 13000.4538 / 16766.4671.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all four success criteria pass, or describe which criterion failed (and the observed values/log) so a gap-closure plan can be created.</resume-signal>
  <done>All four Phase-3 success criteria pass on the M3 entry in one QuPath run; phase gate cleared (or gaps recorded for `/gsd-plan-phase --gaps`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator → QuPath GUI | Manual verification run; no new code, no external input, no automation surface introduced |

Single-user, local, CPU-only pipeline: no auth, session, network, or crypto surface. This plan runs and inspects existing artifacts only; no package installs (no npm/pip/cargo), so no supply-chain (T-*-SC) threat applies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Repudiation (local record-keeping) | manual verification outcome | low | accept | Outcome is recorded in the plan SUMMARY / STATE; single-user machine, no audit-trail requirement |

No `high`- or `critical`-severity threats exist for this plan's surface; none were fabricated.
</threat_model>

<verification>
- The human confirms all four success criteria in one QuPath run; any failure is recorded as a gap for `/gsd-plan-phase --gaps`.
</verification>

<success_criteria>
- SC1: `02_detect_classify.groovy` runs via "Run for project" without errors and writes `data.qpdata` with classified cells.
- SC2: all four classes (TdT+/Fos+/Double+/Negative) present; each cell carries an atlas region label.
- SC3: printed Atlas_X values fall in 5,000–10,000 µm.
- SC4: per-region count table for CA1/CA2/CA3/DG is readable in the QuPath annotation pane.
</success_criteria>

<output>
Create `.planning/phases/03-detection-script-and-single-section-end-to-end-test/03-04-SUMMARY.md` when done.
</output>
