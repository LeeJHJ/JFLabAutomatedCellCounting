---
phase: 04-biological-plausibility-validation-and-imaging-optimization
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md
autonomous: false
requirements: [VAL-01]
must_haves:
  truths:
    - "04-VALIDATION-RECORD.md records a computed value AND a written interpretation for the Double+/TdT+ ratio, flagging it OUT (≈0.45 vs 10–40%) with the four D-02 candidate explanations weighed on paper"
    - "04-VALIDATION-RECORD.md records the per-region DAPI density/mm² values and interprets the above-seed reading by citing the prior Phase-2 mis-calibration finding (not re-litigating the seed)"
    - "04-VALIDATION-RECORD.md records the nucleus-area peak bin (+ median/IQR cross-check) and interprets the marginal below-seed reading via the accepted Phase-2 sigma=2.5 tradeoff"
    - "04-VALIDATION-RECORD.md documents the absence of a true negative-control region and reports the SSp Fos+ rate as a corroboration anchor (D-06 discretion)"
    - "no metric is written as FAILED / must-fix — each is a findings record with interpretation (D-01)"
  artifacts:
    - .planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md
    - "M3 Hippocampus 20x 062926 3 plane/results/val01_percell_export.tsv (produced by the human export run)"
    - "M3 Hippocampus 20x 062926 3 plane/results/val01_region_area.tsv (produced by the human export run)"
  key_links:
    - "the human QuPath export must run AFTER 02_detect_classify.groovy on entry 1 so the (bg-sub) measurements + classes exist in data.qpdata; val01_metrics.py then consumes the two TSVs it writes"
---

<objective>
Complete the VAL-01 vertical slice: the operator runs the Plan-04-01 Groovy export in QuPath ("Run for project") on the classified M3 entry 1 to produce the per-cell + per-region TSVs, then `val01_metrics.py` computes the four metrics on the real data and those numbers + their interpretations are written into `04-VALIDATION-RECORD.md`. Per D-01 this is a findings record with interpretation, NOT a pass/fail gate — the phase completes even with metrics out of range; out-of-range values are interpreted (n=1 caveat, biological explanation, threshold-sensitivity note) and become flagged notes for the full series.

Purpose: A reproducible, honest scientific record of the M3-entry-1 bioplausibility check, closing out the single-section validation run.
Output: `M3 Hippocampus 20x 062926 3 plane/results/val01_percell_export.tsv`, `.../results/val01_region_area.tsv`, and `04-VALIDATION-RECORD.md`.

## Filename note (collision avoidance)
CONTEXT/D-05 name this deliverable `04-VALIDATION.md`, but that filename is already occupied by the GSD Nyquist validation-strategy contract in this phase dir. To avoid clobbering it, the scientific record is written to **`04-VALIDATION-RECORD.md`**. Same content intent as D-05; different filename purely to preserve the existing GSD strategy file.

## Artifacts this phase produces
See the inventory in 04-01-PLAN.md `<objective>`. This plan adds `04-VALIDATION-RECORD.md` and the two export TSVs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-CONTEXT.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-RESEARCH.md
@.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-01-SUMMARY.md
@.planning/phases/03-detection-script-and-single-section-end-to-end-test/03-VERIFICATION.md
@.planning/phases/02-detection-parameter-lock/02-LOCK-RECORD.md
@reference/dapi_region_reference.csv
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human runs the Groovy export in QuPath and confirms populated TSVs</name>
  <read_first>
    - scripts/03_export_val01_metrics.groovy (the export the operator runs — from Plan 04-01)
    - .planning/phases/04-.../04-RESEARCH.md (Pitfall 4 — the all-null-column symptom to catch at this gate)
  </read_first>
  <action>
    Hand off to the operator (GUI-only per CLAUDE.md): run the Plan-04-01 export script in QuPath "Run for project" on the classified M3 entry 1 to write the two `results/` TSVs, then confirm the per-cell area/intensity columns are populated before the metrics run. This is a human-in-the-loop gate; Claude does not drive QuPath. See how-to-verify for the exact steps.
  </action>
  <what-built>
    `scripts/03_export_val01_metrics.groovy` (from Plan 04-01) is authored and deployed to the QuPath project scripts/ dir. It exports per-cell (class, region label, nucleus area, centroid, bg-sub intensities) and per-region area (mm²) TSVs from the classified `data.qpdata`.
  </what-built>
  <how-to-verify>
    1. Launch QuPath: `$HOME/section-pipeline/tools/QuPath/bin/QuPath` and open the `M3 Hippocampus 20x 062926 3 plane` project.
    2. Confirm entry 1 already has classified cells from `02_detect_classify.groovy` (Double+/Fos+/TdT+/Negative present — this was verified in Phase 3; re-run `02_detect_classify.groovy` first only if `data.qpdata` was reset).
    3. Run `03_export_val01_metrics.groovy` via "Run for project" (or Run on entry 1).
    4. Confirm two files appear under `M3 Hippocampus 20x 062926 3 plane/results/`: `val01_percell_export.tsv` and `val01_region_area.tsv`.
    5. Spot-check the per-cell TSV: the `nucleus_area_um2`, `fos_bgsub`, `tdt_bgsub` columns must be non-null/non-empty on the majority of rows (RESEARCH Pitfall 4 — all-null columns are the Phase-3 key-mismatch symptom and must be caught here, not downstream).
  </how-to-verify>
  <verify>
    <human-check>Two TSVs exist in results/; per-cell TSV has non-null area + bg-sub intensity on the majority of rows.</human-check>
    <automated>awk -F'\t' 'NR==1{for(i=1;i<=NF;i++)h[$i]=i; next} {t++; if($h["nucleus_area_um2"]!=""&&$h["nucleus_area_um2"]!="NaN")a++} END{print "rows="t" area_nonnull="a; exit (t>0 && a>=0.9*t)?0:1}' "M3 Hippocampus 20x 062926 3 plane/results/val01_percell_export.tsv"</automated>
  </verify>
  <resume-signal>Type "approved" once both TSVs exist and the per-cell area/intensity columns are populated, or describe the issue (e.g. all-null columns → the export read a wrong measurement key).</resume-signal>
  <acceptance_criteria>
    - `M3 Hippocampus 20x 062926 3 plane/results/val01_percell_export.tsv` and `val01_region_area.tsv` both exist.
    - The per-cell TSV `nucleus_area_um2`, `fos_bgsub`, `tdt_bgsub` columns are non-null on ≥90% of rows (the automated awk check exits 0).
    - The four classes (Double+/Fos+/TdT+/Negative) appear in the per-cell `class` column.
  </acceptance_criteria>
  <done>Both export TSVs exist with populated per-cell area/intensity columns; operator approved. Ready for the metrics run.</done>
</task>

<task type="auto">
  <name>Task 2: Compute VAL-01 metrics on real data and write 04-VALIDATION-RECORD.md</name>
  <read_first>
    - scripts/val01_metrics.py (from Plan 04-01 — the metrics it computes and its stdout format)
    - .planning/phases/04-.../04-RESEARCH.md (VAL-01 Published-Range Provenance: the four D-02 candidate interpretations; the Phase-2 density/area mis-calibration cross-reference; the SSp corroboration reasoning; the 0.40→0.45 methodology-shift data point)
    - .planning/phases/04-.../04-CONTEXT.md (D-01, D-02, D-05, D-06 and the SSp-corroboration + per-subfield-ratio specifics)
    - .planning/phases/03-.../03-VERIFICATION.md (the human-attested Phase-3 counts: Fos+ ~20%, TdT+ ~3.5%, Double+/TdT+ ~0.45, SSp suppressed)
    - reference/dapi_region_reference.csv (the Phase-2 per-region density reference — 2,500–5,500/mm² cluster, the cited prior finding)
  </read_first>
  <files>.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md</files>
  <action>
    Run `conda run -n braian python3 scripts/val01_metrics.py` against the real export TSVs (defaults point at the two `results/` files). Capture the printed four-metric block, then author `04-VALIDATION-RECORD.md` (D-01 findings-record register — never "FAILED"/"must fix"; use "flagged out of range, interpreted as follows"). Document each metric with: its target range, the measured value, an in/out-of-range flag, and a written interpretation:
    1. **Double+/TdT+ ratio** — report the computed value (expected ≈0.45, flagged OUT vs 10–40%) using both conventions the script prints; weigh all four D-02 candidate explanations on paper (strong-recall session; n=1 sampling; robust-threshold k=3 sensitivity — noting the k=4-vs-k=3 comparison was NOT run so this is a candidate not a verified cause; hippocampus CA1-vs-DG reactivation asymmetry). Include the [measured] methodology-shift data point (Phase-2 ≈0.40 → Phase-3 bg-sub+robust ≈0.45) as direct evidence for the threshold-sensitivity candidate. Report the per-subfield ratio breakdown from the script.
    2. **DAPI density/mm²** — report the measured hippocampal-subfield densities; flag above the 500–2,000/mm² seed; interpret by CITING the prior Phase-2 internal finding (`02-LOCK-RECORD.md` / `dapi_region_reference.csv`: densities cluster 2,500–5,500/mm² across nearly all regions, seed already judged mis-calibrated for 20x Airyscan MIP DAPI, MIP-inflates-2D-density) — do NOT re-litigate the seed.
    3. **Nucleus-area peak** — report the modal 10 µm² bin plus the median/IQR/skew cross-check; if marginally below the 50–150 µm² seed (~[40,50) expected), interpret via the accepted Phase-2 sigma=2.5 tradeoff (sigma=3.0 hit the seed but merged cortical nuclei) and the area↔diameter geometric sanity check.
    4. **Fos+ control rate** — document the absence of a true negative-control region (VAL-01's explicit "or absence documented" allowance, D-06 discretion); report the SSp Fos+ rate from the script as a corroboration anchor (post-Phase-3-fix SSp should read low — evidence the classifier behaves), explicitly noting SSp is not an unstimulated control; report any fiber-tract reference rate if the export has classified cells there.

    Tag empirical values `[measured]` and literature/reasoned claims `[inferred]`/`[ASSUMED]` (D-06), and state the TRAP2-paper provenance honestly (ranges carried as `[ASSUMED]` since Phase 2; primary source 403-blocked). Close with an explicit statement that per D-01 the phase completes with these findings recorded; out-of-range items are flagged notes for the full series, not blockers.
  </action>
  <verify>
    <automated>test -f ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md" && grep -qiE 'Double\+|ratio' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md" && grep -qi 'density' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md" && grep -qi 'area' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md" && grep -qi 'SSp' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md" && grep -qF '[measured]' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md" && ! grep -qiE '\bFAILED\b|must fix' ".planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-VALIDATION-RECORD.md"</automated>
  </verify>
  <acceptance_criteria>
    - `04-VALIDATION-RECORD.md` exists and carries a value + interpretation for all four VAL-01 metrics.
    - The Double+/TdT+ ratio section weighs all four D-02 candidate explanations and cites the 0.40→0.45 methodology-shift data point.
    - The density section cites the Phase-2 mis-calibration finding rather than re-deriving the seed.
    - The Fos+ section documents the negative-control absence and reports the SSp corroboration rate.
    - `[measured]` tags present; no metric is written as "FAILED" or "must fix" (D-01 findings-record register).
    - The record states the phase completes with findings recorded (D-01).
  </acceptance_criteria>
  <done>04-VALIDATION-RECORD.md written from the real computed metrics, all four VAL-01 items carry a value + interpretation, out-of-range values interpreted per D-01/D-02, SSp corroboration + negative-control absence documented, D-06 claim labels applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script-authored TSV → val01_metrics.py | The metrics run consumes the two TSVs the human-run Groovy export authored; single-operator local machine, no network/auth/user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | export TSV consumed by the metrics run | low | mitigate | The human-verify gate confirms the TSVs exist with populated columns before the metrics run; val01_metrics.py's own input guard (T-04-01) rejects a malformed/short TSV |
| T-04-06 | Information Disclosure | 04-VALIDATION-RECORD.md local doc | low | accept | Local scientific record, no secrets/PII; single-operator machine |

*Documentation/analysis phase — no network-facing service, no auth/access-control surface, no package installs (RESEARCH §Security Domain). No high-severity threats; no supply-chain row.*
</threat_model>

<verification>
- The human export run produces both TSVs with populated per-cell area/intensity columns (Pitfall 4 caught at the gate).
- `04-VALIDATION-RECORD.md` records a value + interpretation for all four VAL-01 metrics.
- D-01 findings-record register honored (no FAILED/must-fix); D-02 four-candidate interpretation present; D-06 claim labels applied.
</verification>

<success_criteria>
- All four VAL-01 metrics documented with computed value + interpretation.
- Out-of-range metrics (ratio, density, area) interpreted, not gated (D-01).
- Negative-control absence + SSp corroboration documented.
</success_criteria>

<output>
Create `.planning/phases/04-biological-plausibility-validation-and-imaging-optimization-/04-03-SUMMARY.md` when done.
</output>
