---
phase: 05-series-scaffolding-multi-scene-mip-batch-export-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/03_export_val01_metrics.groovy
  - "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy"
  - scripts/verify_export_integrity.py
autonomous: true
requirements: [EXP-02]
user_setup: []

must_haves:
  truths:
    - "03_export_val01_metrics.groovy derives BOTH output filenames from the sanitized QuPath entry name (getProjectEntry().getImageName() + invalidChars regex + buildPathInProject), so 'Run for project' across N entries writes 2*N distinct files with no cross-entry clobbering (EXP-02, D-06)"
    - "Both TSVs get the per-entry stem: results/<stem>__val01_percell_export.tsv and results/<stem>__val01_region_area.tsv; the truncate-per-run write semantics stay but are now scoped to a unique per-entry filename (D-07)"
    - "The emitted TSV column headers are byte-identical to before the fix — only the path changes, never the header row (D-07; val01_metrics.py PERCELL_EXPECTED_COLS/REGION_EXPECTED_COLS contract preserved)"
    - "The canonical scripts/ copy and the QuPath project scripts/ copy are byte-identical (dual-location deploy convention)"
    - "verify_export_integrity.py asserts: percell/region files pair up, total file count is even and == 2 * distinct stems, and (when >=2 entries) per-entry row counts are not all identical (clobbering guard)"
  artifacts:
    - "scripts/03_export_val01_metrics.groovy (fixed, per-entry output path)"
    - "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy (byte-identical copy)"
    - "scripts/verify_export_integrity.py (export-integrity checker)"
  key_links:
    - "Output path is a function of getProjectEntry() evaluated per-entry INSIDE the project run — never a fixed filename (RESEARCH Pitfall 5, EXP-02 root cause)"
    - "invalidChars regex reused verbatim from run_braian_detection.groovy (lines 79-81) — strips / and \\ before path construction, preventing directory traversal in the stem"
    - "Everything downstream of the two File-construction lines (row-building, headers, .text truncate-write) is UNCHANGED (D-07)"
---

<objective>
Fix `03_export_val01_metrics.groovy` so a QuPath "Run for project" across all 5 section
entries writes 5 distinct per-entry output pairs into `results/` with no TSV truncation —
removing the blocking prerequisite for AGG-01 (Phase 10). Add a reusable
`scripts/verify_export_integrity.py` that codifies the non-clobbering integrity assertion.

Purpose: Establish the output half of the series data-integrity gate (EXP-02). The current
script writes to two hardcoded filenames (`results/val01_percell_export.tsv`,
`results/val01_region_area.tsv`) that get overwritten on every entry during "Run for
project," so only the last-processed entry survives on disk.

Output: fixed `03_export_val01_metrics.groovy` (both copies, byte-identical), and
`scripts/verify_export_integrity.py`.

Sequencing note: This fix is a self-contained 2-line path change plus a small checker; it has
NO data dependency on the converter (plan 05-01) and runs in parallel in Wave 1. The full
5-classified-section proof (10 non-empty TSVs with differing row counts) naturally completes
in Phase 8/10 when 5 registered+classified entries exist; in Phase 5 the mechanism is proven
scriptably (grep + byte-identical cmp + integrity checker against available results) and,
optionally, by an operator "Run for project" on the existing single-entry M3 v1.0 project —
which produces a stem-named pair (not the old fixed filename), proving the mechanism end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-CONTEXT.md
@.planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-RESEARCH.md
@.planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Newly-created symbols across Phase 5 (exclude these from source-drift verification):

**Export fix (this plan):**
- `scripts/verify_export_integrity.py` — CLI checker; expects a `--results <dir>` arg; asserts pairing, even file count == 2 * distinct stems, and non-identical per-entry row counts
- Per-entry TSV stem patterns in `results/`: `<sanitized-entry-name>__val01_percell_export.tsv`, `<sanitized-entry-name>__val01_region_area.tsv` (e.g. `wBA1-3_s1__val01_percell_export.tsv`)
- New Groovy locals in `03_export_val01_metrics.groovy`: `invalidChars`, `entry`, `stem` (replacing the fixed `resultsDir`/filename construction at current lines 145-148, 184)

**Converter (plan 05-01):** `czi_mip.py` CLI flags `--czi`/`--outdir`/`--channels`/`--pixel-um`/`--animal-prefix`/`--check-scenes`; functions `_build_ome_xml`, `_preflight_scenes`, `_scene_identity_record`, `_save_identity_thumbnail`; outputs `wBA1-3_s{N}_MIP.ome.tiff`, `wBA1-3_s{N}_identity.png`

**Human-verify (plan 05-03):** no new symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Derive per-entry output filenames in 03_export_val01_metrics.groovy (EXP-02, D-06/D-07) + byte-identical dual-location deploy</name>
  <read_first>
    - scripts/03_export_val01_metrics.groovy (edit target — current path construction at lines 145-148 and 184; pixel-calibration block lines 41-51 is OFF-LIMITS)
    - scripts/run_braian_detection.groovy (primary analog, lines 79-81: invalidChars + getProjectEntry().getImageName() + buildPathInProject)
    - scripts/export_region_dapi_reference.groovy (secondary analog, lines 41-42: null-fallback entry-name idiom)
    - scripts/val01_metrics.py (column contract — PERCELL_EXPECTED_COLS/REGION_EXPECTED_COLS lines 53-57, _load_tsv lines 78-87; MUST NOT change the header the Groovy emits)
    - .planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-RESEARCH.md (Pattern 3; Pitfall 5; Code Examples "Per-region Groovy path fix")
    - .planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-PATTERNS.md (EXP-02 exact edit target + Shared Patterns)
  </read_first>
  <files>scripts/03_export_val01_metrics.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy</files>
  <action>
    In `scripts/03_export_val01_metrics.groovy`, replace the fixed results-dir/filename
    construction (current lines 145-148 for the per-cell file and line 184 for the region file)
    with the established per-entry idiom. Define `invalidChars` (the `['<','>',':','"','/','\\','|','?','*']`
    quoted-and-joined regex from `run_braian_detection.groovy` line 79). Resolve `entry =
    getProjectEntry()` and build `stem` from `entry.getImageName()` with the defensive null
    fallback to `getCurrentImageData().getServer().getMetadata().getName()` (as
    `export_region_dapi_reference.groovy` does), then `.replaceAll(invalidChars, '')`. Construct
    both output files with `new File(buildPathInProject("results", "${stem}__val01_percell_export.tsv"))`
    and `new File(buildPathInProject("results", "${stem}__val01_region_area.tsv"))`. Remove the
    `getProject().getBaseDirectory()` string-gluing and the `resultsDir.mkdirs()` call
    (`buildPathInProject` resolves against the project base dir; sibling scripts never mkdir
    `results/`). Change NOTHING downstream of these two lines — the `percellHeader`/`regionHeader`
    definitions, row-building loops, `String.format` templates, and `.text =` truncate-write
    stay exactly as-is (D-07 locks the column contract). Leave the pixel-calibration block
    (lines 41-51) untouched.

    Add a data-integrity guard: after resolving `stem`, if the sanitized stem is empty, abort
    with a clear message rather than writing an unnamed file. (Entry names are unique by
    construction per D-04, so no two entries collide post-sanitization; this guard makes that
    assumption explicit instead of silent — see threat T-05-02-02.)

    Then hard-copy the edited file byte-identically into
    `M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy` (dual-location
    deploy convention). Use a file copy, not a re-type, so the two copies are guaranteed identical.
  </action>
  <verify>
    <automated>test $(grep -c 'buildPathInProject' scripts/03_export_val01_metrics.groovy) -eq 2 && grep -q 'getProjectEntry' scripts/03_export_val01_metrics.groovy && echo PASS</automated>
    <automated>cmp scripts/03_export_val01_metrics.groovy "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy" && echo IDENTICAL</automated>
    <automated>grep -q 'nucleus_area_um2' scripts/03_export_val01_metrics.groovy && grep -q 'area_mm2' scripts/03_export_val01_metrics.groovy && echo HEADERS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'buildPathInProject' scripts/03_export_val01_metrics.groovy` == 2 (both output paths now use the project-relative helper)
    - `grep -q 'getProjectEntry' scripts/03_export_val01_metrics.groovy` matches (path is a function of the running entry)
    - `cmp scripts/03_export_val01_metrics.groovy "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy"` exits 0 (byte-identical dual-location copy)
    - The two header lists (percell 7 cols, region 5 cols) parsed by `val01_metrics.py` are still present verbatim in the Groovy — `git diff scripts/03_export_val01_metrics.groovy` shows changes ONLY in the results-path construction, not in `percellHeader`/`regionHeader`, row-building, or the pixel-calibration block
    - Output filename template contains the literal per-entry suffixes `__val01_percell_export.tsv` and `__val01_region_area.tsv` interpolated after `${stem}`
  </acceptance_criteria>
  <done>Both output paths derive from the sanitized running-entry name via buildPathInProject; the two script copies are byte-identical; column headers and downstream write logic are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add scripts/verify_export_integrity.py — file-count + non-clobbering assertion (EXP-02 Wave 0 gap)</name>
  <read_first>
    - scripts/val01_metrics.py (project Python style: argparse/Path, sys.exit on failure, TSV column contract PERCELL_EXPECTED_COLS/REGION_EXPECTED_COLS lines 53-57)
    - scripts/03_export_val01_metrics.groovy (the writer this checker validates — confirms the two per-entry suffixes)
    - .planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-RESEARCH.md (Validation Architecture — EXP-02 row: file count == 10, row counts differ)
    - .planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-VALIDATION.md (Wave 0 Requirements — post-export integrity check)
  </read_first>
  <files>scripts/verify_export_integrity.py</files>
  <action>
    Create `scripts/verify_export_integrity.py` following the project's script conventions
    (argparse with `type=Path`, `sys.exit(msg)` on failure, no pytest — printed assertions per
    project convention). Accept `--results <dir>` (the QuPath project `results/` directory).
    Glob `*__val01_percell_export.tsv` and `*__val01_region_area.tsv`. Assert: (1) every percell
    stem has a matching region stem and vice versa (pairing); (2) total matched file count is
    even and equals `2 * <distinct stems>`; (3) when >= 2 distinct stems exist, the per-entry
    percell row counts (data rows, excluding header) are NOT all identical — a clobbering
    regression would collapse them to a single surviving file or identical counts. Print a
    per-stem summary line (stem, percell rows, region rows) and a final PASS/FAIL. Exit non-zero
    on any failed assertion. When fewer than 2 stems are present (e.g. run against the current
    single-entry M3 project), print that the multi-entry clobbering assertion is skipped for
    lack of >=2 entries but still verify pairing + naming — so the checker is runnable in
    Phase 5 and again in Phase 8/10 when the 5 classified entries exist.
  </action>
  <verify>
    <automated>conda run -n braian python3 scripts/verify_export_integrity.py --help 2>&1 | grep -q -- '--results' && echo CLI_OK</automated>
    <automated>conda run -n braian python3 scripts/verify_export_integrity.py --results "M3 Hippocampus 20x 062926 3 plane/results" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `verify_export_integrity.py --help` shows a `--results` argument (CLI scaffold present)
    - Run against a directory with a valid single-entry or multi-entry result set exits 0 and prints a per-stem row-count summary + PASS
    - Run against a synthetic directory containing only ONE percell file with no region pair (or two identical-row-count files simulating a clobber) exits non-zero with a clear FAIL message
    - The checker never mutates any file in `--results` (read-only)
  </acceptance_criteria>
  <done>A reusable integrity checker asserts per-entry pairing, even file count, and non-identical row counts; runnable now against the single-entry M3 results and again on the 5-entry series in Phase 8/10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuPath entry name -> filesystem path | The running entry's `getImageName()` is interpolated into a `results/` output path — the one place untrusted-shaped string data crosses into a filesystem operation |
| verify_export_integrity.py -> filesystem | Read-only glob + line-count over an operator-supplied `--results` dir |

This half is still a **local, single-operator pipeline** (no auth/session/network/crypto),
but it contains the phase's one genuine input-validation item: a QuPath entry name used to
build an output path (RESEARCH.md § Security Domain, V5 Input Validation).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | Path traversal via unsanitized entry name (e.g. an entry named `../../etc/passwd`) used directly in `new File(...)` | high | mitigate | Reuse the existing `invalidChars` regex from `run_braian_detection.groovy` (strips `/ \ : .. `-adjacent chars `< > : " / \ | ? *`) BEFORE constructing the path with `buildPathInProject("results", ...)`; the sanitized stem cannot contain a path separator, so it cannot escape `results/`. Mitigated -> does not block (block_on=high applies only to unmitigated high) |
| T-05-02-02 | Tampering (data integrity) | Two entries sanitizing to the same stem -> silent clobber | medium | mitigate | Entry names are unique by construction (D-04: `wBA1-3_s1..s5`); the empty-stem guard aborts rather than writing an unnamed file, and `verify_export_integrity.py` asserts distinct-stem count so a collision surfaces as a file-count mismatch instead of silent data loss |
| T-05-02-SC | Tampering | package installs | n/a | n/a | No `pip install` — checker uses only stdlib + the already-present `braian` env (RESEARCH § Package Legitimacy Audit: Not applicable) |
</threat_model>

<verification>
- SC#3 (mechanism, scripted): `grep -c buildPathInProject == 2`, `getProjectEntry` present, byte-identical dual copy, column headers preserved.
- SC#3 (integrity): `verify_export_integrity.py` asserts pairing + even count + non-identical row counts; runnable now against the single-entry M3 project and again on the 5-entry series (Phase 8/10).
- Full 5-classified-section "Run for project" proof (10 non-empty TSVs, differing row counts) completes in Phase 8/10 when the registered+classified entries exist — sequenced there, not fabricated here.
</verification>

<success_criteria>
- `03_export_val01_metrics.groovy` writes per-entry output pairs (stem from sanitized entry name) with no truncation on "Run for project" (EXP-02, D-06/D-07).
- Column contract (`val01_metrics.py` expected columns) unchanged; only the path changed.
- Both script copies byte-identical; `verify_export_integrity.py` codifies the non-clobbering assertion.
</success_criteria>

<output>
Create `.planning/phases/05-series-scaffolding-multi-scene-mip-batch-export-integrity/05-02-SUMMARY.md` when done.
</output>