---
phase: 06-registration-speedup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-registration-speedup/06-REG03-SOP.md
  - scripts/bigwarp_effort_log.csv
  - .planning/phases/06-registration-speedup/06-REG05-FINDINGS.md
autonomous: true
requirements: [REG-03, REG-04, REG-05]
user_setup: []

must_haves:
  truths:
    - "06-REG03-SOP.md pins the native ABBA 'DeepSlice Registration (Local)' command parameters verbatim (channels=2 DAPI NOT 0, model=mouse, section_numbers=true, post_processing=KEEP_ORDER_SET_SPACING + operator spacing, propagate_angles=true, ensemble=true) plus the one-time DeepSlice env path set and the percentile-B&C-on-DAPI-first step (REG-03, D-02/D-04/D-05)"
    - "bigwarp_effort_log.csv has the exact 6-column schema (section,start_time,end_time,elapsed_min,landmark_count,notes) with one example row naming the amygdala anchors, ready for the operator to record ≤~5 min/section wall-clock evidence (REG-04)"
    - "06-REG05-FINDINGS.md states the D-07 a-priori keep/reject rule (quality-only, time irrelevant) BEFORE the trial and leaves a bolded Decision: KEEP/REJECT line to be filled either way (REG-05, D-06/D-07)"
  artifacts:
    - ".planning/phases/06-registration-speedup/06-REG03-SOP.md (REG-03 operator SOP scaffold)"
    - "scripts/bigwarp_effort_log.csv (REG-04 operator effort-log template)"
    - ".planning/phases/06-registration-speedup/06-REG05-FINDINGS.md (REG-05 a-priori-rule + keep/reject findings scaffold)"
  key_links:
    - "The SOP's channels=2 (DAPI) is the documented silent-failure guard — index 0 throws ABBA's 'Missing channel in selected slice(s)' error on these AF568-T2/AF488-T3/DAPI-T4 MIPs (RESEARCH Pitfall 3, feedback-abba-channel-index)"
    - "These three files are scaffolds authored now; the operator plans 06-03 (SOP), 06-04 (effort log), 06-05 (findings) fill in the recorded outcomes — that is why 06-03/04/05 depend on this plan"
    - "The a-priori D-07 rule must be written down before the trial so the keep/reject cannot be a post-hoc 'looks fine' rationalisation (threat T-06-03 data integrity)"
---

<objective>
Author the three operator-facing record scaffolds the human-in-the-loop registration tracks fill
in: (1) `06-REG03-SOP.md`, a parameter-pinned standard operating procedure for the native ABBA
"DeepSlice Registration (Local)" command (satisfying D-01's reproducibility intent as a committed
record, NOT a `run_deepslice.py` script); (2) `scripts/bigwarp_effort_log.csv`, the 6-column
per-section wall-clock template that turns REG-04's "materially less effort" claim into evidence;
and (3) `06-REG05-FINDINGS.md`, which states the D-07 a-priori keep/reject rule up front and leaves
a Decision line to be recorded either way.

Purpose: These artifacts are the executor-authored half of REG-03/REG-04/REG-05. Pinning every
parameter and the a-priori rule now — before any GUI work — means the operator plans (06-03/04/05)
are pure execute-and-record, and the "reproducible/offline/evidenced" goals of the three
requirements are met by committed records rather than by re-deciding parameters at the microscope.

Output: `.planning/phases/06-registration-speedup/06-REG03-SOP.md`,
`scripts/bigwarp_effort_log.csv`, `.planning/phases/06-registration-speedup/06-REG05-FINDINGS.md`.

Decision (make explicit): 06-CONTEXT ⟳ RESOLUTION supersedes RESEARCH's "Recommended Project
Structure" — `scripts/run_deepslice.py` is NOT authored. D-01's scriptable/reproducible goal is met
by the SOP markdown record here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-registration-speedup/06-CONTEXT.md
@.planning/phases/06-registration-speedup/06-RESEARCH.md
@.planning/phases/06-registration-speedup/06-PATTERNS.md
@.planning/phases/06-registration-speedup/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Full Phase 6 artifact set (`scripts/run_deepslice.py` explicitly NOT built — 06-CONTEXT ⟳ RESOLUTION).

**This plan (06-02, operator-record scaffolds):**
- `.planning/phases/06-registration-speedup/06-REG03-SOP.md` (REG-03 SOP scaffold)
- `scripts/bigwarp_effort_log.csv` (REG-04 effort-log template; schema `section,start_time,end_time,elapsed_min,landmark_count,notes`)
- `.planning/phases/06-registration-speedup/06-REG05-FINDINGS.md` (REG-05 findings scaffold)

**Other Phase 6 plans:** `scripts/extract_atlas_plate.py`, `scripts/elastix_params/Par_Affine.txt`,
`scripts/elastix_params/Par_BSpline.txt`, `scripts/elastix_trial_harness.py` (all in 06-01, REG-05).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 06-REG03-SOP.md — parameter-pinned SOP for the native ABBA "DeepSlice Registration (Local)" command (REG-03, D-02/D-04/D-05)</name>
  <read_first>
    - .planning/phases/06-registration-speedup/06-CONTEXT.md (⟳ RESOLUTION lines 30-34: the finalized native-command mechanism + exact parameters; D-04 compare-angle; D-05 outlier rule)
    - .planning/phases/06-registration-speedup/06-RESEARCH.md ("Pattern 2: native DeepSlice Registration (Local)" lines 222-236 parameter table; Pitfalls 2 & 3 saturation + channel index)
    - .planning/phases/06-registration-speedup/06-PATTERNS.md ("REG-03 operator SOP record" lines 148-156: feedback_abba_tilt.md numbered-step + dated-decision shape)
    - $HOME/.claude/projects/-home-jflab-Analysis/memory/feedback_abba_tilt.md (numbered fix-steps + "Decision:" block convention to copy)
  </read_first>
  <files>.planning/phases/06-registration-speedup/06-REG03-SOP.md</files>
  <action>
    Write `06-REG03-SOP.md` copying feedback_abba_tilt.md's shape: short context statement, then
    NUMBERED procedure steps, then a dated per-section outcome/decision block with blanks the operator
    fills in plan 06-03.

    Context statement: REG-03 registers the 5 wBA1-3 MIPs to Allen CCFv3 using ABBA's native
    "DeepSlice Registration (Local)" Fiji command (NOT a standalone run_deepslice.py — per 06-CONTEXT
    ⟳ RESOLUTION). Data: `Automated Cell Counting/wBA Sungmo/wBA1-3_s1..s5_MIP.ome.tiff` (0.69 µm/px,
    channels AF568-T2 / AF488-T3 / DAPI-T4, DAPI = index 2). Do NOT use the 32 GB `..._Merged.ome.tiff`.

    Numbered procedure (pin every value verbatim):
    1. One-time setup: `Edit > Configuration > Set DeepSlice Env Path` →
       `/home/jflab/miniforge3/envs/deepslice`.
    2. Load the 5 MIPs into ABBA's "Multi Image To Atlas" window as full-res slices (names already
       contain `_s1`..`_s5`).
    3. Before running DeepSlice, set a PERCENTILE-based B&C (auto-contrast) on the DAPI channel to
       avoid ABBA's documented ~50%-over-saturation failure mode (RESEARCH Pitfall 2).
    4. Select all 5 slices; run `Plugins > BIOP > Atlas > Multi Image To Atlas > Align > ABBA -
       DeepSlice Registration (Local)` with: `channels=2` (DAPI — NOT 0; index 0 throws "Missing
       channel in selected slice(s)"), `model=mouse`, `section_numbers=true` (the `_sN` filenames
       already satisfy DeepSlice's `_s\d+` regex — zero renaming), `post_processing=KEEP_ORDER_SET_SPACING`
       with the operator's known section spacing (D-02 — record the spacing value used),
       `propagate_angles=true`, `ensemble=true`. Leave "Allow change of atlas slicing angle" UNCHECKED
       initially (D-04).
    5. D-04 belt-and-suspenders angle check: note the propagated candidate DV/ML angle; independently
       find a good DV/ML tilt manually in Review Mode on one well-fitting section; compare both
       overlays and ADOPT WHICHEVER FITS BETTER. Record which won.
    6. D-05 outlier rule: apply the adopted shared angle to all 5, but permit a documented per-section
       DV/ML override in Review Mode for any poorly-fitting section (fold/damage/oblique cut). Record
       which sections deviated and why.
    7. Confirm the atlas overlay tracks tissue on each of the 5 sections before export.

    Then a "Per-section run record (filled in plan 06-03)" section: a table with columns
    `section | DeepSlice AP (mm) | shared angle source (propagated / manual) | per-section override? | overlay fit OK? | notes`, one blank row per s1..s5, and a dated **Decision:** line placeholder for the
    D-04 angle-source outcome. Make NO anterior/posterior claim in the scaffold prose beyond the
    per-section AP the operator will record. Cross-reference `[[feedback-abba-tilt]]` and
    `[[feedback-abba-channel-index]]` by name as the memory convention does.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-registration-speedup/06-REG03-SOP.md && grep -c 'KEEP_ORDER_SET_SPACING' .planning/phases/06-registration-speedup/06-REG03-SOP.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and `grep -c 'KEEP_ORDER_SET_SPACING' 06-REG03-SOP.md` >= 1
    - `grep -c 'channels=2' .planning/phases/06-registration-speedup/06-REG03-SOP.md` >= 1 AND the file explicitly warns index 0 is wrong (`grep -c 'Missing channel' ...` >= 1)
    - `grep -c 'propagate_angles=true' ...` >= 1 AND `grep -c 'ensemble=true' ...` >= 1 AND `grep -c 'deepslice' ...` >= 1 (env path pinned)
    - The per-section run record table has a row per s1..s5 and a bolded `Decision:` placeholder for the D-04 angle-source outcome (`grep -c 'Decision:' ...` >= 1)
  </acceptance_criteria>
  <done>06-REG03-SOP.md pins the native-command parameters, the D-04 compare-angle and D-05 outlier steps, and a blank per-section run-record table + Decision line for plan 06-03 to fill.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/bigwarp_effort_log.csv — REG-04 per-section wall-clock effort template (REG-04)</name>
  <read_first>
    - .planning/phases/06-registration-speedup/06-VALIDATION.md (Wave 0 Requirements: the exact column schema `section, start_time, end_time, elapsed_min, landmark_count, notes`)
    - .planning/phases/06-registration-speedup/06-PATTERNS.md ("scripts/bigwarp_effort_log.csv" lines 138-144: header-only CSV + one example row with amygdala anchors in notes)
    - .planning/phases/06-registration-speedup/06-CONTEXT.md (Claude's Discretion lines 46-49: reduced ~4-landmark amygdala anchors, target ≈ ≤5 min/section, evidenced not asserted)
  </read_first>
  <files>scripts/bigwarp_effort_log.csv</files>
  <action>
    Write `scripts/bigwarp_effort_log.csv` as a header-only CSV with EXACTLY these 6 columns in this
    order: `section,start_time,end_time,elapsed_min,landmark_count,notes`. Add ONE example/template row
    for `wBA1-3_s1` with empty time/count cells and a `notes` value naming the reduced amygdala-relevant
    anchor set: `"anchors: LA/BA lateral boundary, external capsule, optic tract, ventral brain edge"`.
    Do NOT pre-fill s2..s5 — the operator appends one row per section during plan 06-04, recording
    real wall-clock so REG-04's ≤~5 min/section (below v1.0's 5–15 min) claim is evidenced. The
    landmarks are amygdala-relevant, NOT v1.0's hippocampal CA1/CA3/DG set.
  </action>
  <verify>
    <automated>head -1 scripts/bigwarp_effort_log.csv</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 scripts/bigwarp_effort_log.csv` is exactly `section,start_time,end_time,elapsed_min,landmark_count,notes`
    - `grep -c 'LA/BA' scripts/bigwarp_effort_log.csv` >= 1 AND `grep -c 'optic tract' scripts/bigwarp_effort_log.csv` >= 1 (amygdala anchors documented, not hippocampal)
    - The file has a header + exactly one example row (`wc -l` reports 2), leaving s2..s5 for the operator
  </acceptance_criteria>
  <done>bigwarp_effort_log.csv has the pinned 6-column schema and one amygdala-anchor example row, ready for the operator to record per-section wall-clock evidence.</done>
</task>

<task type="auto">
  <name>Task 3: 06-REG05-FINDINGS.md — REG-05 a-priori D-07 keep/reject scaffold (REG-05, D-06/D-07)</name>
  <read_first>
    - .planning/phases/06-registration-speedup/06-PATTERNS.md ("REG-05 keep/reject findings record" lines 160-171: the exact a-priori-rule-then-Decision markdown template)
    - .planning/phases/06-registration-speedup/06-CONTEXT.md (D-06 worst-fitting section; D-07 quality-only keep/reject, time irrelevant, recorded either way; D-08 crop caveat)
    - $HOME/.claude/projects/-home-jflab-Analysis/memory/feedback_abba_tilt.md ("Crop-to-tissue pilot finding" block — the a-priori-rule + Decision idiom already used once)
  </read_first>
  <files>.planning/phases/06-registration-speedup/06-REG05-FINDINGS.md</files>
  <action>
    Write `06-REG05-FINDINGS.md` stating the D-07 a-priori rule UP FRONT (before the trial), then a
    blank trial-record block for plan 06-05 to fill. Structure per PATTERNS.md:

    Header + a-priori rule (locked now): "Trial the masked-elastix prototype on the WORST-FITTING
    section (D-06) — the one whose DeepSlice+angle+BigWarp overlay fits worst at the LA/BA boundary.
    KEEP elastix ONLY IF the operator judges its LA/BA + ventral-edge atlas-tissue fit VISIBLY BETTER
    than DeepSlice+BigWarp on that section (D-07). Time is IRRELEVANT to the decision (quality-first).
    A merely-equal fit → REJECT (BigWarp stays default). The decision is recorded EITHER WAY. Respect
    the D-08 caveat: on already-tight sections the crop trims only a few percent and may not by itself
    rescue elastix."

    Then a blank "Trial record (filled in plan 06-05)" block with labelled placeholders: worst-fitting
    section id `<...>`; fixed image = atlas plate at `<AP mm>` via extract_atlas_plate.py; moving =
    crop_to_tissue.py output, DAPI channel index 2; params = Par_Affine.txt + Par_BSpline.txt, masked
    both sides; operator visual comparison at LA/BA + ventral edge vs BigWarp-only `<description>`; and
    a final bolded `**Decision: KEEP / REJECT**` line with a one-line justification tied to the
    quality-only rule. Include a note that any elastix param retune (A2) tried before deciding should
    be recorded so a REJECT reflects a genuine loss, not an under-tuned first run.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-registration-speedup/06-REG05-FINDINGS.md && grep -c 'Decision: KEEP / REJECT' .planning/phases/06-registration-speedup/06-REG05-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and contains the bolded `**Decision: KEEP / REJECT**` placeholder (`grep -c 'Decision: KEEP / REJECT' ...` >= 1)
    - The a-priori rule is stated before the trial record: `grep -c 'worst-fitting' ...` >= 1 AND `grep -c 'Time is' ...` >= 1 (quality-only, time-irrelevant rule captured)
    - `grep -c 'index 2' ...` >= 1 (DAPI channel index pinned for the moving image) AND `grep -c 'extract_atlas_plate' ...` >= 1 (fixed-image path named)
  </acceptance_criteria>
  <done>06-REG05-FINDINGS.md records the D-07 a-priori keep/reject rule before the trial and leaves a labelled trial-record block + bolded Decision line for plan 06-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored record → downstream operator action | The pinned parameters (channels=2, spacing, a-priori rule) in these records govern the operator's later GUI actions; a wrong pinned value would misdirect the registration |

No software attack surface — this plan writes three static text/CSV files and runs no external
tool. The only concern is data-integrity: the SOP must pin `channels=2` (not 0) and the findings
record must state the a-priori D-07 rule BEFORE the trial so keep/reject cannot become a post-hoc
rationalisation. Both are content-correctness gates, verified by grep above.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering (data integrity) | wrong channel index pinned in SOP (0 vs DAPI=2) | low | mitigate | SOP pins `channels=2` verbatim + warns index 0 throws "Missing channel" (RESEARCH Pitfall 3); grep-gated |
| T-06-02-02 | Tampering (data integrity) | keep/reject rationalised post-hoc instead of a-priori | low | mitigate | 06-REG05-FINDINGS.md states the D-07 quality-only rule before the trial; grep-gated |
| T-06-SC | Tampering | package installs | n/a | n/a | No installs — doc/template authoring only |
</threat_model>

<verification>
- All three scaffolds exist with the pinned content (positive greps above).
- 06-REG03-SOP.md pins the full native-command parameter set + D-04/D-05 steps.
- bigwarp_effort_log.csv header is the exact 6-column schema.
- 06-REG05-FINDINGS.md states the a-priori D-07 rule before the (later) trial.
- These are scaffolds; the recorded outcomes are filled by operator plans 06-03/04/05 (manual-only, VALIDATION.md).
</verification>

<success_criteria>
- 06-REG03-SOP.md, bigwarp_effort_log.csv, 06-REG05-FINDINGS.md exist with pinned parameters / schema / a-priori rule.
- run_deepslice.py is NOT created (REG-03 uses the native ABBA command; D-01's reproducibility met by the SOP record).
</success_criteria>

<output>
Create `.planning/phases/06-registration-speedup/06-02-SUMMARY.md` when done.
</output>
