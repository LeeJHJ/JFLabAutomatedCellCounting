---
phase: 06-registration-speedup
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - .planning/phases/06-registration-speedup/06-REG03-SOP.md
autonomous: false
requirements: [REG-03]
user_setup: []

must_haves:
  truths:
    - "The native ABBA 'DeepSlice Registration (Local)' command produces AP + candidate shared angle for all 5 wBA1-3 sections in one pass, and the operator confirms the registered atlas overlay tracks tissue on each of the 5 sections (REG-03, SC#1)"
    - "The D-04 angle decision is recorded: the propagated candidate DV/ML angle was compared against a manual Review-Mode angle on a well-fitting section and whichever fit better was adopted (REG-03, D-04)"
    - "Any per-section DV/ML tilt override (D-05 outlier) is documented with which sections deviated and why (REG-03, D-05)"
  artifacts:
    - ".planning/phases/06-registration-speedup/06-REG03-SOP.md (filled: per-section run record + D-04 Decision + D-05 outlier notes)"
    - "5 registered ABBA slice states for wBA1-3_s1..s5 (AP + shared/overridden angle stamped), ready for the BigWarp escalation in plan 06-04"
  key_links:
    - "channels=2 (DAPI) in the DeepSlice-Local dialog — index 0 throws ABBA's 'Missing channel in selected slice(s)' error on these AF568-T2/AF488-T3/DAPI-T4 MIPs (RESEARCH Pitfall 3)"
    - "The adopted shared angle + per-section AP the operator records here are the inputs plan 06-05 needs (the DeepSlice-estimated AP of the worst-fitting section feeds extract_atlas_plate.py)"
    - "No ground-truth registration metric exists — overlay fit is operator visual judgment against tissue anatomy (established doctrine, feedback-abba-tilt)"
---

<objective>
Operator-run GUI plan (autonomous: false): the human at the microscope executes the native ABBA
"DeepSlice Registration (Local)" command on the 5 wBA1-3 MIPs per the pinned SOP (06-REG03-SOP.md),
confirms the registered atlas overlay tracks tissue on each of the 5 sections, performs the D-04
belt-and-suspenders angle comparison (propagated candidate vs a manual Review-Mode angle, adopting
whichever fits better), and documents any D-05 per-section tilt overrides. The executor cannot click
ABBA menus — its role is to surface the SOP, confirm prerequisites, and record the operator's
verbatim outcomes into 06-REG03-SOP.md.

Purpose: This satisfies REG-03 Success Criterion 1 — one-pass batch DeepSlice + angle propagation
with per-section operator overlay confirmation, materially less manual effort than v1.0's
per-section-from-scratch pass. It also produces the per-section AP + adopted angle that plan 06-04
(BigWarp) escalates from and plan 06-05 (elastix trial) consumes.

Output: filled `06-REG03-SOP.md` (per-section run record, D-04 Decision line, D-05 outlier notes) +
5 registered ABBA slice states.

This plan is Wave 2 — it depends on plan 06-02 having authored the SOP scaffold, and on the 5 MIPs
from Phase 5 existing. Per checkpoint-separation convention it carries only the blocking operator
gate + the executor's record-keeping, no script authoring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-registration-speedup/06-CONTEXT.md
@.planning/phases/06-registration-speedup/06-RESEARCH.md
@.planning/phases/06-registration-speedup/06-VALIDATION.md
@.planning/phases/06-registration-speedup/06-REG03-SOP.md
</context>

<artifacts_this_phase_produces>
This plan creates no new source symbols. It FILLS `06-REG03-SOP.md` (scaffolded by plan 06-02) with
the per-section run record, the D-04 angle Decision, and D-05 outlier notes, and produces 5 registered
ABBA slice states. `scripts/run_deepslice.py` is explicitly NOT built (06-CONTEXT ⟳ RESOLUTION).
Full Phase 6 symbol list is in 06-01-PLAN.md and 06-02-PLAN.md.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator runs native DeepSlice-Local on the 5 sections, confirms overlay fit per section, resolves D-04 angle + D-05 outliers</name>
  <read_first>
    - .planning/phases/06-registration-speedup/06-REG03-SOP.md (the pinned procedure the operator follows — authored by plan 06-02)
    - .planning/phases/06-registration-speedup/06-RESEARCH.md (Pattern 2 parameter table lines 226-236; Pitfall 2 saturation; Pitfall 3 channel index; A3 atlas-resolution reconciliation)
    - .planning/phases/06-registration-speedup/06-VALIDATION.md (Manual-Only Verifications table: DeepSlice overlay fit; propagated vs manual angle)
    - $HOME/.claude/projects/-home-jflab-Analysis/memory/feedback_abba_tilt.md (DV/ML tilt distrust; Review-Mode angle workflow)
  </read_first>
  <files>.planning/phases/06-registration-speedup/06-REG03-SOP.md (executor records the operator's verbatim outcomes here after sign-off; writes no code)</files>
  <action>
    STOP for human execution + verification — do not auto-approve (blocking-human GUI gate; ABBA menu
    actions cannot be automated by the executor per CLAUDE.md "GUI-only → hand back to the human").
    The executor's role: (a) confirm the 5 MIPs exist at
    `Automated Cell Counting/wBA Sungmo/wBA1-3_s1..s5_MIP.ome.tiff` (NOT the 32 GB `..._Merged.ome.tiff`);
    (b) surface the pinned SOP (06-REG03-SOP.md) to the operator; (c) present the how-to-verify steps
    below and WAIT for the operator's explicit signal; (d) after sign-off, transcribe the operator's
    per-section run record (AP mm, angle source, overrides, overlay-fit OK), the D-04 Decision line, and
    D-05 outlier notes VERBATIM into 06-REG03-SOP.md. If the operator reports the DeepSlice fit is
    unusable on one or more sections, record it — plan 06-04's BigWarp is the escalation, so this gate
    still passes with the deviations noted (it does not silently drop a bad section).
  </action>
  <what-built>
    Plan 06-02 authored 06-REG03-SOP.md pinning the native ABBA "DeepSlice Registration (Local)"
    command parameters (channels=2 DAPI, model=mouse, section_numbers=true,
    post_processing=KEEP_ORDER_SET_SPACING + operator spacing, propagate_angles=true, ensemble=true),
    the one-time DeepSlice env path set, the percentile-B&C-on-DAPI-first step, and the D-04/D-05 steps.
    The 5 identity-verified MIPs come from Phase 5. No standalone run_deepslice.py exists — the native
    command runs the identical offline predict()+propagate_angles() internally.
  </what-built>
  <how-to-verify>
    1. In Fiji, confirm the one-time `Edit > Configuration > Set DeepSlice Env Path` →
       `/home/jflab/miniforge3/envs/deepslice`.
    2. Load the 5 MIPs into ABBA's "Multi Image To Atlas" window. Set a PERCENTILE-based B&C on the
       DAPI channel (avoids the ~50% over-saturation failure mode).
    3. Select all 5; run `Plugins > BIOP > Atlas > Multi Image To Atlas > Align > ABBA - DeepSlice
       Registration (Local)` with the SOP's pinned values — CONFIRM `channels=2` (DAPI). If ABBA reports
       "Missing channel in selected slice(s)", the channel index is wrong (you used 0) — fix to 2.
    4. For EACH of the 5 sections, inspect the registered atlas overlay in Review Mode: confirm region
       boundaries track tissue (especially the LA/BA amygdala region and ventral edge). Record per
       section: DeepSlice AP (mm), overlay fit OK? (yes / needs-BigWarp), notes.
    5. D-04: note the propagated candidate shared DV/ML angle; independently find a good DV/ML tilt
       manually in Review Mode on one well-fitting section; compare both overlays; ADOPT WHICHEVER FITS
       BETTER and record which won on the SOP's Decision line.
    6. D-05: apply the adopted shared angle to all 5, but override the DV/ML tilt per-section for any
       poorly-fitting section (fold/damage/oblique cut); record which sections deviated and why.
    7. Do not run ABBA's Affine/Spline (out of scope; degrades unmasked) — export state is set by
       DeepSlice + adopted angle only at this stage; BigWarp escalation is plan 06-04.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 sections have a recorded overlay-fit outcome, the D-04 angle Decision is made, and any D-05 outliers are noted; or describe the blocking issue (e.g. channel-index error, unusable fit) so it is recorded/resolved.</resume-signal>
  <verify>
    <human-check>Operator confirms the registered atlas overlay tracks tissue on each of the 5 sections and states the adopted angle source — no automated command substitutes for visual overlay-vs-tissue judgment (VALIDATION.md Manual-Only Verifications). Recorded artifact: filled 06-REG03-SOP.md.</human-check>
    <automated>grep -c 'Decision:' .planning/phases/06-registration-speedup/06-REG03-SOP.md</automated>
  </verify>
  <acceptance_criteria>
    - Operator has explicitly approved (or recorded specific deviations for) all 5 sections' overlay fit
    - 06-REG03-SOP.md per-section run record has a filled row for each of s1..s5 (AP mm + overlay-fit outcome), NOT left blank
    - The D-04 angle-source Decision line is filled (propagated vs manual, which was adopted): `grep -c 'Decision:' .planning/phases/06-registration-speedup/06-REG03-SOP.md` >= 1 and the line is non-placeholder
    - Any D-05 per-section override is documented with the section id + reason (or an explicit "no outliers" note)
  </acceptance_criteria>
  <done>Operator has run native DeepSlice-Local on all 5 sections, confirmed per-section overlay fit, resolved the D-04 angle source, and the outcomes (incl. D-05 outliers) are recorded verbatim in 06-REG03-SOP.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DeepSlice AP/angle estimate → registered slice state | An unseen-but-wrong DV/ML angle (operator's documented distrust) propagates into every downstream ROI unless visually validated |
| registration fit → downstream classification | A silently-misregistered section produces locally plausible but wrong per-region counts in Phase 8 |

No software attack surface — this plan runs no executor code beyond confirming file presence and
recording outcomes. The "threat" is data-integrity: a misregistration presented as correct.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering (data integrity) | silent misregistration / unseen DeepSlice angle presented as correct | low | mitigate | Operator visual QC of overlay-vs-tissue on each of the 5 sections (established doctrine); D-04 compares propagated vs manual angle — never adopts an unseen angle; D-05 documents outliers |
| T-06-03-CH | Tampering (data integrity) | wrong moving-channel index (0 vs DAPI=2) | low | mitigate | SOP pins channels=2; ABBA's "Missing channel" error surfaces index-0 misuse immediately (RESEARCH Pitfall 3) |
| T-06-SC | Tampering | package installs | n/a | n/a | No installs — GUI operator action; DeepSlice already installed |
</threat_model>

<verification>
- SC#1 (REG-03): operator confirms the registered atlas overlay fits tissue on each of the 5 sections (manual-only, VALIDATION.md).
- D-04: propagated vs manual angle compared, adopted angle recorded on the SOP Decision line.
- D-05: per-section outliers documented (or "no outliers" noted).
- Manual-only by design — no automated registration-fit metric exists; the automated grep only confirms the record artifact was filled.
</verification>

<success_criteria>
- All 5 sections registered via native DeepSlice-Local in one pass with operator-confirmed overlay fit.
- D-04 angle source adopted-by-comparison (not unseen) and recorded; D-05 outliers documented.
- 06-REG03-SOP.md filled — the per-section AP + adopted angle are ready for plans 06-04/06-05.
</success_criteria>

<output>
Create `.planning/phases/06-registration-speedup/06-03-SUMMARY.md` when done (record the operator's per-section overlay confirmations + D-04 Decision verbatim — plan 06-05 reads the worst-fitting section's AP from here).
</output>
