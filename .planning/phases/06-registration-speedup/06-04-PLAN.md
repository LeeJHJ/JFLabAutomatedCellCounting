---
phase: 06-registration-speedup
plan: 04
type: execute
wave: 3
depends_on: ["06-03", "06-02"]
files_modified:
  - scripts/bigwarp_effort_log.csv
autonomous: false
requirements: [REG-04]
user_setup: []

must_haves:
  truths:
    - "A reduced-landmark (~4) BigWarp pass on amygdala-relevant anchors (LA/BA lateral boundary, external capsule, optic tract, ventral brain edge — NOT hippocampal CA1/CA3/DG) is applied across all 5 sections, escalated from the DeepSlice+angle state only where residual misalignment remains (REG-04, SC#2)"
    - "bigwarp_effort_log.csv records operator wall-clock per section for all 5, evidencing a per-section effort target of ≈ ≤5 min/section — below v1.0's 5–15 min manual baseline (REG-04, D-context)"
    - "The worst-fitting section at the LA/BA boundary after DeepSlice+angle+BigWarp is identified and recorded (D-06) — the single section plan 06-05 trials masked-elastix on"
  artifacts:
    - "scripts/bigwarp_effort_log.csv (filled: 5 rows with start/end wall-clock, elapsed_min, landmark_count, notes)"
    - "5 BigWarp-refined ABBA slice states for wBA1-3_s1..s5, ready for ABBA export → 01_load_abba_rois.groovy downstream"
  key_links:
    - "Moving-channel index in the BigWarp dialog = 2 (DAPI) on these 3-channel MIPs, not 0 (RESEARCH Pitfall 3)"
    - "The worst-fitting section recorded here (D-06) + its DeepSlice AP (from 06-REG03-SOP.md) are the two inputs plan 06-05's elastix trial consumes"
    - "Effort is operator wall-clock evidence, not an assertion — REG-04's 'materially less effort' claim stands or falls on the logged elapsed_min values"
---

<objective>
Operator-run GUI plan (autonomous: false): the human applies a reduced ~4-landmark BigWarp pass on
amygdala-relevant anchors across all 5 wBA1-3 sections, escalating from the DeepSlice+angle state
(plan 06-03) only where residual misalignment remains, and logs per-section wall-clock into
bigwarp_effort_log.csv so REG-04's "materially less effort" is evidenced (target ≈ ≤5 min/section,
below v1.0's 5–15 min). The operator also identifies the worst-fitting section at the LA/BA boundary
(D-06) — the single section plan 06-05 trials masked-elastix on. The executor cannot place BigWarp
landmarks; its role is to surface the anchor set + effort-log template and record the operator's
outcomes.

Purpose: This satisfies REG-04 Success Criterion 2 — reduced-landmark BigWarp across all 5 sections
with a documented, below-baseline effort target — and hands plan 06-05 its worst-fitting section
(D-06) + that section's AP for the elastix fixed image.

Output: filled `scripts/bigwarp_effort_log.csv` (5 rows) + 5 BigWarp-refined ABBA slice states + a
recorded worst-fitting-section id.

This plan is Wave 3 — it depends on plan 06-03 (the registered DeepSlice+angle state to escalate
from) and plan 06-02 (the effort-log template). It carries only the blocking operator gate + the
executor's record-keeping.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-registration-speedup/06-CONTEXT.md
@.planning/phases/06-registration-speedup/06-RESEARCH.md
@.planning/phases/06-registration-speedup/06-VALIDATION.md
@.planning/phases/06-registration-speedup/06-REG03-SOP.md
@scripts/bigwarp_effort_log.csv
</context>

<artifacts_this_phase_produces>
This plan creates no new source symbols. It FILLS `scripts/bigwarp_effort_log.csv` (templated by plan
06-02) with 5 per-section rows and records the worst-fitting section (D-06). Produces 5 BigWarp-refined
ABBA slice states. Full Phase 6 symbol list is in 06-01-PLAN.md and 06-02-PLAN.md.
`scripts/run_deepslice.py` is explicitly NOT built.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator applies reduced-landmark amygdala BigWarp across all 5 sections, logs wall-clock, and identifies the worst-fitting section (REG-04, D-06)</name>
  <read_first>
    - scripts/bigwarp_effort_log.csv (the 6-column template to fill — from plan 06-02)
    - .planning/phases/06-registration-speedup/06-CONTEXT.md (Claude's Discretion lines 46-52: reduced ~4 amygdala anchors, ≤5 min/section target, moving-channel index 2; D-06 worst-fitting section)
    - .planning/phases/06-registration-speedup/06-VALIDATION.md (Manual-Only Verifications: BigWarp effort target row)
    - $HOME/.claude/projects/-home-jflab-Analysis/memory/feedback_abba_tilt.md (BigWarp is the trusted escalation; landmarks on real tissue features)
  </read_first>
  <files>scripts/bigwarp_effort_log.csv (executor records the operator's per-section wall-clock + worst-fitting-section note here after sign-off; writes no code)</files>
  <action>
    STOP for human execution + verification — do not auto-approve (blocking-human GUI gate; BigWarp
    landmark placement is inherently interactive with no scriptable substitute per CLAUDE.md and
    RESEARCH Architectural Responsibility Map). The executor's role: (a) surface the amygdala anchor set
    + the effort-log template; (b) confirm plan 06-03's 5 registered slice states exist to escalate
    from; (c) present the how-to-verify steps and WAIT for the operator's signal; (d) after sign-off,
    append one row per section (s1..s5) to bigwarp_effort_log.csv with the operator's start/end
    wall-clock, elapsed_min, landmark_count, and notes, and record the worst-fitting section id (D-06)
    in the notes + the SUMMARY. If any section exceeds the ≤~5 min target, record the actual time (the
    log is evidence, not a pass/fail gate — an honest higher value is recorded, not massaged).
  </action>
  <what-built>
    Plan 06-03 left 5 DeepSlice+angle-registered ABBA slice states. Plan 06-02 authored
    bigwarp_effort_log.csv with the 6-column schema (section,start_time,end_time,elapsed_min,
    landmark_count,notes) and the amygdala anchor set in the example row. BigWarp is the project's
    trusted nonlinear escalation (feedback-abba-tilt); this pass re-targets landmarks to the LA/BA
    amygdala ROI, not v1.0's hippocampal CA1/CA3/DG anchors.
  </what-built>
  <how-to-verify>
    1. For each of the 5 sections, in ABBA/BigWarp, start a wall-clock timer. Confirm the moving-channel
       index is 2 (DAPI) — NOT 0 — on these AF568-T2/AF488-T3/DAPI-T4 MIPs.
    2. Place a REDUCED set (~4) of landmarks on amygdala-relevant anchors: LA/BA lateral boundary,
       external capsule, optic tract, ventral brain edge. Do NOT use the hippocampal CA1/CA3/DG set from
       v1.0 (not the ROI here). Add landmarks only where residual DeepSlice+angle misalignment remains —
       a well-fitting section may need fewer than 4.
    3. Stop the timer at export-ready state. Record start_time, end_time, elapsed_min, landmark_count,
       and notes for that section. Aim for ≈ ≤5 min/section (below v1.0's 5–15 min); record the real
       time regardless.
    4. Across all 5, judge which section fits WORST at the LA/BA boundary + ventral edge after
       DeepSlice+angle+BigWarp (D-06). Record its id — this is the one section plan 06-05 trials
       masked-elastix on. Note its DeepSlice AP (from 06-REG03-SOP.md) so 06-05 can extract the atlas
       plate at that AP.
    5. Confirm the BigWarp-refined overlay tracks tissue on each of the 5 before this plan closes.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 sections have a BigWarp pass with a logged wall-clock row AND the worst-fitting section (D-06) is identified with its AP; or describe the blocking issue.</resume-signal>
  <verify>
    <human-check>Operator confirms the reduced-landmark BigWarp overlay tracks tissue on each of the 5 sections and states the worst-fitting section — no automated command judges registration fit (VALIDATION.md). Recorded artifact: filled bigwarp_effort_log.csv + worst-section note.</human-check>
    <automated>test "$(grep -c 'wBA1-3_s' scripts/bigwarp_effort_log.csv)" -ge 5 && echo "5 SECTION ROWS PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - bigwarp_effort_log.csv contains a filled row for each of s1..s5 with non-empty elapsed_min + landmark_count (`grep -c 'wBA1-3_s' scripts/bigwarp_effort_log.csv` >= 5)
    - The recorded per-section elapsed_min values are present as evidence for the ≤~5 min/section target (target documented; a higher honest value is recorded, not hidden)
    - The worst-fitting section (D-06) is recorded (id + its DeepSlice AP mm) in the log notes and/or SUMMARY — the single input plan 06-05 needs
    - Operator confirms the BigWarp overlay tracks tissue on all 5 sections
  </acceptance_criteria>
  <done>Reduced-landmark amygdala BigWarp is applied across all 5 sections with per-section wall-clock logged as REG-04 evidence, and the worst-fitting section (D-06) + its AP are recorded for plan 06-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BigWarp landmark placement → refined slice state | Mis-placed/wrong-channel landmarks warp the atlas away from tissue, silently degrading downstream region labels |
| effort log → REG-04 evidence claim | If wall-clock is not honestly recorded, the "materially less effort" claim is unsupported |

No software attack surface — the executor only records outcomes. The concern is data-integrity:
a misregistration or an unrecorded/massaged effort value.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Tampering (data integrity) | wrong moving-channel index (0 vs DAPI=2) in BigWarp dialog | low | mitigate | how-to-verify pins channel index 2; ABBA surfaces a missing-channel error on index-0 misuse (RESEARCH Pitfall 3) |
| T-06-04-02 | Repudiation (evidence integrity) | effort value not honestly logged | low | mitigate | Operator records real start/end wall-clock per section; log is evidence not a gate, so honest higher values are recorded |
| T-06-03 | Tampering (data integrity) | BigWarp overlay misregistered but accepted | low | mitigate | Operator visual QC of overlay-vs-tissue per section (established doctrine) before the plan closes |
| T-06-SC | Tampering | package installs | n/a | n/a | No installs — GUI operator action |
</threat_model>

<verification>
- SC#2 (REG-04): reduced-landmark BigWarp applied across all 5; bigwarp_effort_log.csv has 5 filled rows evidencing the ≤~5 min/section target (manual-only wall-clock, VALIDATION.md).
- D-06: worst-fitting section identified + AP recorded for plan 06-05.
- Manual-only by design — the automated check only confirms the 5 section rows exist; effort/fit are operator-recorded.
</verification>

<success_criteria>
- Reduced amygdala-anchor BigWarp across all 5 sections; effort logged per section (≈ ≤5 min target, below v1.0 baseline).
- Worst-fitting section (D-06) + its AP recorded — plan 06-05's single input.
- Moving-channel index 2 (DAPI) used throughout.
</success_criteria>

<output>
Create `.planning/phases/06-registration-speedup/06-04-SUMMARY.md` when done (record the worst-fitting section id + its DeepSlice AP — plan 06-05 reads them from here).
</output>
