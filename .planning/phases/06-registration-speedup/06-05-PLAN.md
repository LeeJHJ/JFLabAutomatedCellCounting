---
phase: 06-registration-speedup
plan: 05
type: execute
wave: 4
depends_on: ["06-04", "06-01", "06-02"]
files_modified:
  - .planning/phases/06-registration-speedup/06-REG05-FINDINGS.md
autonomous: false
requirements: [REG-05]
user_setup: []

must_haves:
  truths:
    - "The masked-elastix prototype is trialed on EXACTLY ONE section — the worst-fitting section from plan 06-04 (D-06) — via crop_to_tissue.py --dapi-channel 2 → extract_atlas_plate.py fixed image → elastix_trial_harness.py (masked -fMask/-mMask, Par_Affine then Par_BSpline) → transformix (REG-05, SC#3)"
    - "The operator compares the elastix LA/BA + ventral-edge fit against DeepSlice+BigWarp on that section and applies the a-priori D-07 rule (keep ONLY if visibly better; quality-only, time irrelevant; merely-equal → reject) (REG-05, D-07)"
    - "The keep/reject Decision is recorded either way in 06-REG05-FINDINGS.md (REG-05, D-07)"
  artifacts:
    - ".planning/phases/06-registration-speedup/06-REG05-FINDINGS.md (filled: trial inputs + bolded Decision: KEEP/REJECT + justification)"
    - "elastix trial outputs for the one worst-fitting section (TransformParameters + warped result + a side-by-side comparison overlay the operator judged)"
  key_links:
    - "Exactly one section (D-06) — the trial cannot balloon; the worst-fitting section + its AP come from plan 06-04 / 06-REG03-SOP.md"
    - "Masked both sides — never run elastix unmasked (RESEARCH Pitfall 4, the 2026-06-23 failure); -fMask from extract_atlas_plate.py plate mask, -mMask from the cropped-DAPI Otsu mask"
    - "D-07 is quality-only: time is irrelevant to keep/reject; the operator may retune params (A2) before deciding so a REJECT is a genuine loss not an under-tuned run"
---

<objective>
Trial the masked-elastix prototype on EXACTLY the one worst-fitting section (D-06) from plan 06-04,
then apply the a-priori D-07 keep/reject rule. The executor runs the scriptable pipeline
(crop_to_tissue.py → extract_atlas_plate.py → elastix_trial_harness.py → transformix — elastix is a
CPU CLI, not a GUI, so the executor CAN run it) to produce the elastix-registered result and a
side-by-side comparison overlay; the operator makes the quality-only visual judgment (elastix vs
DeepSlice+BigWarp at the LA/BA boundary + ventral edge) and records KEEP or REJECT either way in
06-REG05-FINDINGS.md.

Purpose: This satisfies REG-05 Success Criterion 3 — a single-section masked-elastix trial under the
pre-declared quality-only accept/reject rule, decision recorded either way. Scoped to ONE section so
it cannot balloon.

Output: filled `06-REG05-FINDINGS.md` (trial inputs + bolded Decision + justification) + the elastix
trial artifacts for the one section.

This plan is Wave 4 — it depends on plan 06-04 (the worst-fitting section id + its AP), plan 06-01
(the elastix scripts/params), and plan 06-02 (the findings scaffold). It is autonomous: false because
the D-07 keep/reject is a blocking operator judgment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-registration-speedup/06-CONTEXT.md
@.planning/phases/06-registration-speedup/06-RESEARCH.md
@.planning/phases/06-registration-speedup/06-VALIDATION.md
@.planning/phases/06-registration-speedup/06-REG05-FINDINGS.md
@scripts/crop_to_tissue.py
@scripts/extract_atlas_plate.py
@scripts/elastix_trial_harness.py
</context>

<artifacts_this_phase_produces>
This plan creates no new source symbols. It RUNS the REG-05 scripts (from plan 06-01) on the one
worst-fitting section and FILLS `06-REG05-FINDINGS.md` (scaffolded by plan 06-02) with the trial
inputs and the bolded Decision. Produces elastix trial outputs (TransformParameters, warped result,
comparison overlay) for that single section. `scripts/run_deepslice.py` is explicitly NOT built.
Full Phase 6 symbol list is in 06-01-PLAN.md and 06-02-PLAN.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the masked-elastix trial on the one worst-fitting section (D-06) and build the comparison overlay (REG-05)</name>
  <read_first>
    - scripts/crop_to_tissue.py (usage: DAPI selected by name/index; --dapi-channel 2; preserves calibration/channel order; braian env)
    - scripts/extract_atlas_plate.py (from plan 06-01: --ap-mm → atlas_plate.tif + mask; braian env)
    - scripts/elastix_trial_harness.py (from plan 06-01: --fixed/--moving/--fixed-mask/--moving-mask/--param/--out/--transformix-in; shell=False)
    - .planning/phases/06-registration-speedup/06-RESEARCH.md ("Code Examples: elastix masked invocation" lines 303-322; Pitfalls 4 & 5; Assumption A2 retune freedom)
    - .planning/phases/06-registration-speedup/06-04-SUMMARY.md and 06-REG03-SOP.md (the worst-fitting section id + its DeepSlice AP mm — operator-supplied inputs)
  </read_first>
  <files>.planning/phases/06-registration-speedup/06-REG05-FINDINGS.md (record the trial inputs/paths here; the elastix outputs land under a trial output dir)</files>
  <action>
    Read the worst-fitting section id + its DeepSlice AP (mm) from plan 06-04's SUMMARY / 06-REG03-SOP.md
    (these are the operator-supplied inputs from Wave 3; if absent, ask the operator before proceeding —
    the trial is on ONE specific section, never a guessed one).

    Then run the scriptable pipeline (all in the `braian` env; elastix is a CPU CLI the executor may run):
    1. Crop the worst section to tissue: `conda run -n braian python3 scripts/crop_to_tissue.py
       "Automated Cell Counting/wBA Sungmo/wBA1-3_s<N>_MIP.ome.tiff" --dapi-channel 2 -o <trial>/worst_cropped.ome.tiff`.
       Note the printed tissue-fill fraction (Pitfall 5: if already >0.9, expect a marginal crop — still
       proceed, the trial is on the worst section precisely because gain is most plausible there).
    2. Extract the DAPI plane (channel index 2) of the cropped MIP to a 2D `worst_dapi.tif`, and its Otsu
       tissue mask to `worst_dapi_mask.tif` (a few lines of tifffile + skimage.threshold_otsu — the
       moving image + -mMask for elastix). Extract the atlas fixed image + mask at the section's AP:
       `conda run -n braian python3 scripts/extract_atlas_plate.py --ap-mm <AP> -o <trial>/atlas_plate.tif
       --mask-output <trial>/atlas_plate_mask.tif`. Sanity-check the printed atlas.orientation/shape and
       eyeball the plate is coronal (A1/A2) before registering.
    3. Register (masked both sides): `conda run -n braian python3 scripts/elastix_trial_harness.py
       --fixed <trial>/atlas_plate.tif --moving <trial>/worst_dapi.tif --fixed-mask <trial>/atlas_plate_mask.tif
       --moving-mask <trial>/worst_dapi_mask.tif --out <trial>/elastix_out --transformix-in <trial>/worst_dapi.tif`
       (defaults to Par_Affine.txt then Par_BSpline.txt; transformix consumes TransformParameters.1.txt).
       NEVER run unmasked (Pitfall 4). If the fit is clearly under-tuned, retune Par_*.txt (grid spacing,
       iterations) per A2 and re-run — record what was tried.
    4. Build a side-by-side comparison overlay PNG: the elastix-warped section vs the atlas plate contour,
       alongside the existing DeepSlice+BigWarp overlay for the same section, focused on the LA/BA boundary
       + ventral edge, so the operator's D-07 judgment (Task 2) has a concrete artifact. Record all input
       paths + the AP used in 06-REG05-FINDINGS.md's trial-record block.
  </action>
  <verify>
    <automated>conda run -n braian python3 scripts/elastix_trial_harness.py --self-test && conda run -n braian python3 scripts/extract_atlas_plate.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - The trial ran on EXACTLY one section (the D-06 worst-fitting section) — its id + AP are recorded in 06-REG05-FINDINGS.md, not a guessed/second section
    - `<trial>/elastix_out/` contains `TransformParameters.0.txt` and `TransformParameters.1.txt` (Affine then BSpline stages ran, masked) and a transformix `result.tif`
    - A comparison overlay artifact (elastix vs DeepSlice+BigWarp at LA/BA + ventral edge) exists for the operator's Task 2 judgment
    - The tissue-fill fraction printed by crop_to_tissue.py is recorded (Pitfall 5 context); elastix was run masked (both -fMask and -mMask supplied), never unmasked
  </acceptance_criteria>
  <done>The masked-elastix pipeline has run end-to-end on the one worst-fitting section, producing the warped result + a LA/BA comparison overlay, with all trial inputs recorded in 06-REG05-FINDINGS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator applies the a-priori D-07 quality-only keep/reject rule and records the Decision either way (REG-05, D-07)</name>
  <read_first>
    - .planning/phases/06-registration-speedup/06-REG05-FINDINGS.md (the a-priori D-07 rule stated by plan 06-02 + the trial inputs recorded in Task 1)
    - .planning/phases/06-registration-speedup/06-CONTEXT.md (D-07: keep only if visibly better LA/BA + ventral-edge fit; time irrelevant; merely-equal → reject; recorded either way; D-08 crop caveat)
    - .planning/phases/06-registration-speedup/06-VALIDATION.md (Manual-Only Verifications: masked-elastix fit vs DeepSlice+BigWarp)
    - $HOME/.claude/projects/-home-jflab-Analysis/memory/feedback_abba_tilt.md ("Crop-to-tissue pilot finding" — a-priori-rule-then-Decision idiom; BigWarp stays default unless elastix demonstrably wins)
  </read_first>
  <files>.planning/phases/06-registration-speedup/06-REG05-FINDINGS.md (executor records the operator's verbatim Decision here after sign-off; writes no code)</files>
  <action>
    STOP for human judgment — do not auto-approve (blocking-human quality gate; the keep/reject is
    operator visual judgment with no ground-truth metric). The executor's role: (a) surface the Task 1
    comparison overlay + the a-priori D-07 rule; (b) present the how-to-verify steps and WAIT for the
    operator's explicit KEEP or REJECT; (c) transcribe the operator's Decision + one-line justification
    VERBATIM into 06-REG05-FINDINGS.md's bolded Decision line — recorded EITHER WAY. If the operator
    wants a param retune (A2) before deciding, reopen Task 1 for a re-run first, then record.
  </action>
  <what-built>
    Task 1 ran the masked-elastix trial on the one worst-fitting section (D-06): crop_to_tissue.py
    (--dapi-channel 2) → extract_atlas_plate.py fixed image at the section's AP → elastix (Par_Affine +
    Par_BSpline, masked both sides) → transformix, producing a warped result and a side-by-side
    comparison overlay vs the DeepSlice+BigWarp result at the LA/BA boundary + ventral edge. The a-priori
    D-07 rule was stated in 06-REG05-FINDINGS.md before the trial (plan 06-02).
  </what-built>
  <how-to-verify>
    1. Open the Task 1 comparison overlay: elastix-warped section + atlas plate vs the DeepSlice+BigWarp
       overlay for the same section, focused on the LA/BA amygdala boundary + ventral brain edge.
    2. Apply the a-priori D-07 rule: KEEP elastix ONLY IF its LA/BA + ventral-edge fit is VISIBLY BETTER
       than DeepSlice+BigWarp. Time is IRRELEVANT to the decision (quality-first). A merely-equal fit →
       REJECT (BigWarp stays the default). Respect D-08: on an already-tight section the crop trims only
       a few percent, so a marginal result is expected — do not over-read it.
    3. If the elastix run looks under-tuned (not a genuine shape improvement), request a Par_*.txt retune
       (A2) and re-run Task 1 before deciding, so a REJECT reflects a real loss, not an under-tuned run.
    4. State KEEP or REJECT with a one-line justification tied to the LA/BA + ventral-edge quality
       comparison. The decision is recorded EITHER WAY.
  </how-to-verify>
  <resume-signal>Type "keep" or "reject" (with a one-line LA/BA + ventral-edge justification); or request a param retune to re-run Task 1 first.</resume-signal>
  <verify>
    <human-check>Operator states KEEP or REJECT after visually comparing the elastix vs DeepSlice+BigWarp LA/BA + ventral-edge fit — no automated command judges registration quality (VALIDATION.md). Recorded artifact: filled Decision line in 06-REG05-FINDINGS.md.</human-check>
    <automated>grep -Eic 'Decision:.*(KEEP|REJECT)' .planning/phases/06-registration-speedup/06-REG05-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - The operator has explicitly stated KEEP or REJECT under the a-priori D-07 quality-only rule (time irrelevant)
    - 06-REG05-FINDINGS.md's bolded Decision line is resolved to a concrete KEEP or REJECT with a one-line LA/BA + ventral-edge justification (`grep -Eic 'Decision:.*(KEEP|REJECT)' 06-REG05-FINDINGS.md` >= 1, and the line is no longer the `KEEP / REJECT` placeholder)
    - The decision is recorded EITHER WAY (a REJECT is recorded as explicitly as a KEEP) — no silent drop
    - Any param retune (A2) attempted before the decision is noted, so a REJECT is a genuine loss not an under-tuned run
  </acceptance_criteria>
  <done>The operator has applied the a-priori D-07 keep/reject rule to the one worst-fitting section's elastix vs BigWarp fit, and the Decision + justification are recorded verbatim in 06-REG05-FINDINGS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args / recorded inputs → REG-05 scripts | worst-section id + AP + file paths feed crop_to_tissue.py / extract_atlas_plate.py / elastix_trial_harness.py on a single-operator desktop |
| REG-05 scripts → elastix/transformix CLI | filenames passed as subprocess argv lists into the elastix binaries |
| elastix result → keep/reject decision | a merely-equal or under-tuned result mistaken for a genuine elastix win/loss would set the wrong default |

Local, offline, single-operator pipeline — no auth/session/network surface. The real items are the
same low-severity injection/validation concerns as plan 06-01 (the scripts run here) plus the
data-integrity concern of a post-hoc keep/reject.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | elastix/transformix subprocess call | low | mitigate | elastix_trial_harness.py uses subprocess.run([...], shell=False) argv lists + validates input paths (built/verified in plan 06-01) |
| T-06-05-01 | Tampering (data integrity) | elastix run unmasked → 2026-06-23 failure reproduced | low | mitigate | Trial is masked both sides (-fMask atlas plate mask, -mMask cropped-DAPI Otsu mask); Task 1 acceptance requires both masks supplied (RESEARCH Pitfall 4) |
| T-06-03 | Tampering (data integrity) | keep/reject rationalised post-hoc / under-tuned run mistaken for a loss | low | mitigate | A-priori D-07 rule stated before the trial (plan 06-02); operator may retune (A2) before deciding; decision recorded either way |
| T-06-SC | Tampering | package installs | n/a | n/a | No installs — crop_to_tissue/extract_atlas_plate/elastix all already present (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- SC#3 (REG-05): masked-elastix trialed on EXACTLY one worst-fitting section; compared to DeepSlice+BigWarp; kept/rejected per the a-priori D-07 rule; Decision recorded either way (manual-only judgment, VALIDATION.md).
- The REG-05 scripts' self-tests are re-run green before the real trial (automated verify on Task 1).
- Elastix run masked both sides (Pitfall 4); one section only (cannot balloon).
</verification>

<success_criteria>
- Masked-elastix trialed on the single worst-fitting section (D-06) via the plan-06-01 scripts, masked both sides.
- Operator applies the a-priori quality-only D-07 rule (time irrelevant); KEEP or REJECT recorded either way in 06-REG05-FINDINGS.md.
- BigWarp remains default unless elastix demonstrably wins on LA/BA + ventral-edge fit.
</success_criteria>

<output>
Create `.planning/phases/06-registration-speedup/06-05-SUMMARY.md` when done (record the D-07 KEEP/REJECT decision + justification verbatim — this is the a-priori-rule outcome REG-05 requires).
</output>
