---
phase: 06.1-pipeline-generalization-per-region-readout-runbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline.yml
  - "M3 Hippocampus 20x 062926 3 plane/pipeline.yml"
  - "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml"
  - scripts/validate_pipeline_config.py
autonomous: true
requirements: [PIPE-01, PIPE-05]

must_haves:
  truths:
    - "Marker set, anchor channel, exclusions, k_robust and ring geometry are declared in one sidecar config file, separate from BraiAn.yml (D-01, D-02, D-09, D-14)"
    - "A single validator computes, from the config alone, the exact per-slice column set, the per-marker bg-sub measurement-key strings, and whether Double+ will be emitted — so every groovy/python consumer conforms to one contract (D-01, D-03, D-04)"
    - "On a TdT-only config the validator confirms Double+ is omitted and no Fos rows/columns are produced (D-03, D-04)"
  artifacts:
    - path: "pipeline.yml"
      provides: "Sidecar pipeline config: anchor{name,channel}, markers[]{name,channel,compartment}, exclude_acronyms[], k_robust, ring{gap_um,width_um}"
      contains: "compartment"
    - path: "scripts/validate_pipeline_config.py"
      provides: "Fail-loud structural validator + contract printer (per-marker bg-sub key + class vocabulary + emitted columns), with --self-test"
      contains: "def main"
  key_links:
    - from: "scripts/validate_pipeline_config.py"
      to: "pipeline.yml"
      via: "yaml.safe_load reads the sidecar config and asserts required keys/types"
      pattern: "safe_load"
  prohibitions:
    - "Marker names, channels, compartments, exclusion acronyms, k_robust, or ring dimensions MUST NOT be hardcoded in any groovy/python source — they live only in pipeline.yml"
    - "This config file MUST NOT contain BraiAnDetect detection params (sigma/minArea/histogramThreshold/etc.) — those stay in BraiAn.yml (D-14)"
    - "No 'v1'/'simplified'/'placeholder' anchor or marker handling — the anchor is fully config-declared, never hardwired to DAPI"
---

<objective>
Create the sidecar pipeline config (D-14) that declares the variable marker set (D-01), the detection anchor channel (D-02), the region exclusions and tuning literals migrated out of groovy source (D-09), plus a runnable validator that fixes the shared contract every downstream groovy/python consumer conforms to.

This plan is the CONTRACT FOUNDATION for the whole phase. The validator prints, from the config alone: (a) the per-marker background-subtracted measurement-key string each classifier will write/read, (b) the PathClass vocabulary, and (c) the per-slice table column set — so 06.1-02 (classify) and 06.1-03 (export) can be authored in parallel against one source of truth without measurement-key drift.

Purpose: make "the marker set is variable" (PIPE-01) real and enforceable, and give every human-in-the-loop GUI seam a fail-loud precondition input (PIPE-05).
Output: `pipeline.yml` (canonical + two deploy copies) and `scripts/validate_pipeline_config.py`.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `pipeline.yml` (repo-root canonical) — keys: `anchor.name`, `anchor.channel`, `markers[].name`, `markers[].channel`, `markers[].compartment`, `exclude_acronyms`, `k_robust`, `ring.gap_um`, `ring.width_um`.
- Deploy copies at `M3 Hippocampus 20x 062926 3 plane/pipeline.yml` and `wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml` (byte-identical, read by the groovy scripts via `getProject().getBaseDirectory()`).
- `scripts/validate_pipeline_config.py` — functions `load_config`, `derive_contract`, `main`; `--self-test` mode.

Shared contract this plan fixes (consumed by 06.1-02 and 06.1-03):
- Compartment→compartment-label map: `nuclear`→`Nucleus`, `cytoplasmic`→`Cytoplasm`.
- Per-marker bg-sub measurement key: `"<compartment-label>: <channel> mean (bg-sub)"`.
- PathClass vocabulary: `"<marker.name>+"` (exactly one non-anchor marker positive); `"Double+"` (only when ≥2 non-anchor markers declared AND ≥2 positive on the cell — D-03); `"Negative"`; `"Excluded"`.
- Long/tidy combined-CSV column schema (emitted by 06.1-03): `slice,config_tag,region,hemisphere,is_leaf,marker,class,count,density`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md
@CLAUDE.md
@wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/BraiAn.yml
@scripts/build_dapi_reference.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the sidecar pipeline config (pipeline.yml) + deploy copies</name>
  <files>pipeline.yml, M3 Hippocampus 20x 062926 3 plane/pipeline.yml, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml</files>
  <read_first>
    - wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/BraiAn.yml (format/comment-density convention to mirror; content model to NOT copy — D-14 keeps detection params out)
    - scripts/02_detect_classify.groovy (the literals being migrated out live here: EXCLUDE_ACRONYMS line 92, K_ROBUST line 286, GAP_UM/RING_WIDTH_UM lines 143-144; anchor DAPI-T4 + marker channels AF488-T3/AF568-T2)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md (section "pipeline.yml / markers.yml (new sidecar config)")
  </read_first>
  <action>
    Write `pipeline.yml` at the repo root as heavily-commented YAML mirroring BraiAn.yml's rationale-comment density (per D-14, format-only borrow). Declare exactly these top-level keys: `anchor` (a mapping with `name` and `channel`), `markers` (a list of mappings each with `name`, `channel`, `compartment`), `exclude_acronyms` (an inline list), `k_robust` (a float), and `ring` (a mapping with `gap_um` and `width_um`).

    Seed it with the current wBA working set as the documented default (mark these seed values as tunable per slice-set, not locked): anchor name DAPI / channel DAPI-T4 (D-02 — the anchor is DECLARED here, not hardwired); a Fos marker on the nuclear compartment (channel AF488-T3) and a TdT marker on the cytoplasmic compartment (channel AF568-T2) (D-01 — name→channel→compartment); the exclusion set currently living as the EXCLUDE_ACRONYMS literal in 02_detect_classify.groovy (D-09); k_robust 3.0 (migrated from the K_ROBUST literal, D-09); ring gap 1.0 µm and width 8.0 µm (migrated from GAP_UM/RING_WIDTH_UM, D-09). Add a header comment block stating the compartment semantics rule (nuclear markers measured on the nucleus compartment, cytosolic markers on the cytoplasmic expansion ring — nucleus-anchored per CLAUDE.md) and that Double+ is auto-emitted ONLY when ≥2 non-anchor markers are declared (D-03).

    Then hard-copy the file byte-identically to `M3 Hippocampus 20x 062926 3 plane/pipeline.yml` and `wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml` (dual-location deploy convention; the groovy scripts read it from the QuPath project base dir alongside BraiAn.yml). Do NOT add any BraiAnDetect detection parameter to this file.
  </action>
  <verify>
    <automated>conda run -n braian python -c "import yaml,sys; c=yaml.safe_load(open('pipeline.yml')); a=c['anchor']; assert 'name' in a and 'channel' in a; ms=c['markers']; assert all(set(m)>={'name','channel','compartment'} and m['compartment'] in ('nuclear','cytoplasmic') for m in ms); assert isinstance(c['exclude_acronyms'],list) and float(c['k_robust'])==c['k_robust'] and set(c['ring'])>={'gap_um','width_um'}; import pathlib; assert pathlib.Path('pipeline.yml').read_bytes()==pathlib.Path('M3 Hippocampus 20x 062926 3 plane/pipeline.yml').read_bytes()==pathlib.Path('wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml').read_bytes(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline.yml` parses as YAML and contains all required keys with correct types (anchor.name/channel strings; markers list where every entry has name/channel/compartment and compartment ∈ {nuclear, cytoplasmic}; exclude_acronyms a list; k_robust a float; ring.gap_um and ring.width_um numeric).
    - The two deploy copies are byte-identical to the canonical file.
    - The file contains NO BraiAnDetect detection param key (no `sigmaMicrons`, `minAreaMicrons`, `histogramThreshold`, `cellExpansionMicrons`) — those remain BraiAn.yml's concern (D-14).
  </acceptance_criteria>
  <done>The variable marker set, config-declared anchor, exclusions, k_robust and ring geometry exist in one sidecar file (D-01/D-02/D-09/D-14), deployed to both QuPath projects, with no detection params leaked in.</done>
</task>

<task type="auto">
  <name>Task 2: Author validate_pipeline_config.py — contract printer + fail-loud validator with --self-test</name>
  <files>scripts/validate_pipeline_config.py</files>
  <read_first>
    - scripts/build_dapi_reference.py (reuse verbatim the argparse/RawDescriptionHelpFormatter/epilog=__doc__/type=Path CLI idiom, lines 1-23, 64-77)
    - pipeline.yml (the file authored in Task 1 — the schema this validates)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-01..D-04 for the contract rules; D-03 Double+ gate)
  </read_first>
  <action>
    Create `scripts/validate_pipeline_config.py` (braian env, PyYAML 6.0.3 already present — no new dependency). Implement `load_config(path)` that `yaml.safe_load`s the sidecar config and fail-louds with a clear per-key message if any required key is missing, mistyped, or a compartment value is not nuclear/cytoplasmic. Implement `derive_contract(config)` that returns, for each declared non-anchor marker, the compartment-label (nuclear→Nucleus, cytoplasmic→Cytoplasm) and the exact bg-sub measurement-key string `"<compartment-label>: <channel> mean (bg-sub)"`; the PathClass vocabulary (per-marker `"<name>+"`, plus `"Double+"` only when the number of declared non-anchor markers is ≥2, per D-03); and the per-slice table column set built purely by iterating the declared markers (anchor column set plus per-marker count/density columns — D-04, never a fixed Fos/TdT list). `main()` prints the derived contract and exits non-zero on any validation failure.

    Add a `--self-test` flag that constructs two synthetic configs in a temp dir and asserts the contract: (1) a single-non-anchor-marker (TdT-only) config → Double+ is NOT in the class vocabulary and no Fos marker/column appears; (2) a two-non-anchor-marker (Fos+TdT) config → Double+ IS in the vocabulary and both marker columns appear. Self-test exits non-zero if either assertion fails. Use argparse with RawDescriptionHelpFormatter and epilog=__doc__ mirroring build_dapi_reference.py.
  </action>
  <verify>
    <automated>conda run -n braian python scripts/validate_pipeline_config.py --self-test && conda run -n braian python scripts/validate_pipeline_config.py --config pipeline.yml</automated>
  </verify>
  <acceptance_criteria>
    - `--self-test` passes: the TdT-only synthetic config yields a class vocabulary WITHOUT Double+ and a column set WITHOUT any Fos column; the Fos+TdT synthetic config yields Double+ and both marker columns.
    - Running against the real `pipeline.yml` prints, for each declared marker, its compartment-label and its `"<label>: <channel> mean (bg-sub)"` key, and the class vocabulary — and exits 0.
    - Corrupting a required key (manual spot check acceptable) makes the validator exit non-zero with a message naming the missing/invalid key (fail-loud, D-15 input side).
  </acceptance_criteria>
  <done>One validator fixes and prints the shared contract (per-marker bg-sub key, class vocabulary, emitted columns) from the config alone, and proves the D-03/D-04 variable-marker behavior on synthetic TdT-only vs Fos+TdT configs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sidecar config | operator hand-edits pipeline.yml per slice-set; a malformed/mistyped config is untrusted input to every groovy/python consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-01 | Tampering | pipeline.yml parse | low | mitigate | validate_pipeline_config.py fail-louds on missing/mistyped keys and invalid compartment values before any run |
| T-06.1-02 | Denial of Service | silent misparse → wrong marker/compartment | low | mitigate | contract printer surfaces the derived keys/columns so an operator sees a wrong channel/compartment before running the pipeline |
| T-06.1-SC | Tampering | package installs | low | accept | No new npm/pip/cargo installs this plan — PyYAML 6.0.3 already present in the braian env; no supply-chain surface added |

**ASVS L1 review conclusion:** Offline, single-user, CPU-only local scientific tool. No network/auth/multi-tenant surface. The only realistic surface is local-file/config-parse robustness, mitigated by the validator's fail-loud asserts. No high-severity threats.
</threat_model>

<verification>
- `conda run -n braian python scripts/validate_pipeline_config.py --self-test` passes.
- `conda run -n braian python scripts/validate_pipeline_config.py --config pipeline.yml` prints the contract and exits 0.
- pipeline.yml and both deploy copies are byte-identical and contain no BraiAn.yml detection params.
</verification>

<success_criteria>
The sidecar config exists and declares the variable marker set, config-driven anchor, exclusions, k_robust and ring geometry (PIPE-01, PIPE-05), separate from BraiAn.yml (D-14). A runnable validator fixes the shared measurement-key/class/column contract and proves TdT-only omits Double+/Fos (D-03/D-04).
</success_criteria>

<output>
Create `.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-01-SUMMARY.md` when done.
</output>
