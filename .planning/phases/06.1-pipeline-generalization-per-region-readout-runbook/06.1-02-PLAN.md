---
phase: 06.1-pipeline-generalization-per-region-readout-runbook
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - scripts/02_detect_classify.groovy
  - "M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy"
  - "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy"
  - scripts/classify_markers.groovy
  - "M3 Hippocampus 20x 062926 3 plane/scripts/classify_markers.groovy"
  - "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/classify_markers.groovy"
  - _archive/retired-scripts/06.1/classify_markers.groovy
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-05]

must_haves:
  truths:
    - "02_detect_classify.groovy reads the marker set, anchor channel, exclusions, k_robust and ring geometry from pipeline.yml — nothing marker-specific is hardcoded (D-01, D-02, D-09)"
    - "The classification loop derives a bg-sub measure + robust threshold per DECLARED marker, wiring compartment to ROI as nuclear->nucleus ROI / cytoplasmic->cell ROI (nucleus-anchored, CLAUDE.md)"
    - "Double+ is emitted only when >=2 non-anchor markers are declared AND >=2 are positive on the cell; a single-marker config degenerates to that-marker+/Negative with no code path naming an absent marker (D-03, D-04)"
    - "The script fail-loud aborts before work if ABBA ROIs are absent, detections are absent, or a config-declared channel is missing from the image (D-15)"
    - "Exactly one classification implementation remains — classify_markers.groovy is retired (D-06)"
  artifacts:
    - path: "scripts/02_detect_classify.groovy"
      provides: "Config-driven nucleus-anchored compound classification + atlas labeling + fail-loud boundary asserts"
      contains: "pipeline.yml"
    - path: "_archive/retired-scripts/06.1/classify_markers.groovy"
      provides: "Archived superseded standalone classifier (logic lives in 02)"
      contains: "classify"
  key_links:
    - from: "scripts/02_detect_classify.groovy"
      to: "pipeline.yml"
      via: "reads marker/anchor/exclusion/k/ring config from the QuPath project base dir"
      pattern: "pipeline.yml"
    - from: "scripts/02_detect_classify.groovy"
      to: "image channel metadata"
      via: "channel-matches-config fail-loud assert loops declared channels against image channels"
      pattern: "getChannel"
  prohibitions:
    - "NO proximity/overlap colocalization — colocalization stays nucleus-anchored (compound classes on the same anchor-derived object set), never a geometric-overlap merge"
    - "NO hardcoded marker names, channels, compartments, exclusion acronyms, k_robust, or ring dimensions in the groovy source — all come from pipeline.yml"
    - "NO second classification implementation — classify_markers.groovy and its deploy copies are removed, not left alongside"
    - "NO 'v1'/'simplified' single-marker special-casing — the same config-driven loop handles 1..N markers"
---

<objective>
Generalize `02_detect_classify.groovy` from its two hardcoded markers (Fos on the nuclear compartment / AF488-T3, TdT on the cytoplasmic compartment / AF568-T2) into a config-driven marker loop reading `pipeline.yml` (D-01/D-02/D-09), generalize the compound classification to the D-03 Double+ gate, add the D-15 fail-loud boundary asserts, and retire the duplicate `classify_markers.groovy` (D-06) so exactly one classification implementation exists.

The KEEP/RETIRE split is deliberate (D-05): `run_braian_detection.groovy` (standalone BraiAnDetect pass) and `02_detect_classify.groovy` (classify + label + report) stay as the two canonical steps; only the redundant `classify_markers.groovy` is retired because its core already lives verbatim inside `02` (compare its lines 56-71 with 02's lines 400-413).

Purpose: make the marker set variable at classification (PIPE-01), collapse the two-implementation redundancy (PIPE-02), and guard the human-in-the-loop GUI seams with fail-loud preconditions (PIPE-05).
Output: a config-driven `02_detect_classify.groovy` (+ deploy copies) and an archived `classify_markers.groovy`.
</objective>

<artifacts_this_phase_produces>
This plan modifies/produces:
- `scripts/02_detect_classify.groovy` — reads `pipeline.yml`; new blocks: a line-block config reader, fail-loud precondition guards (ABBA-ROIs / detections / channel-match), a per-marker bg-sub + robust-threshold derivation loop, and a generalized compound-classification loop producing the D-03 class vocabulary.
- Deploy copies at both QuPath project `scripts/` dirs (byte-identical).
- `_archive/retired-scripts/06.1/classify_markers.groovy` (git mv of the canonical file); the two deploy copies removed.

Contract consumed from 06.1-01: compartment-label map (nuclear->Nucleus, cytoplasmic->Cytoplasm); bg-sub key `"<label>: <channel> mean (bg-sub)"`; class vocabulary (`"<name>+"`, `"Double+"` only when >=2 non-anchor markers, `"Negative"`, `"Excluded"`).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md
@CLAUDE.md
@scripts/02_detect_classify.groovy
@scripts/classify_markers.groovy
@scripts/01_load_abba_rois.groovy
@pipeline.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config reader + D-15 fail-loud boundary asserts to 02_detect_classify.groovy</name>
  <files>scripts/02_detect_classify.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy</files>
  <read_first>
    - scripts/02_detect_classify.groovy (whole file; entry-confirmation line 89, exclusion literal line 92, zero-detection guard lines 116-118, pixel calibration lines 126-137, ring constants lines 143-146, k_robust literal line 286)
    - scripts/01_load_abba_rois.groovy (ABBA-registration-exists guard lines 27-40 to mirror)
    - scripts/export_region_dapi_reference.groovy (the "no YAML lib in QuPath, parse the text" regex-grab idiom lines 30-39)
    - pipeline.yml (the config being parsed)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md (Shared Patterns -> Boundary-assert guard; "New D-15 assert needed" channel-match)
  </read_first>
  <action>
    After the entry-confirmation println, add a line-block reader that loads `pipeline.yml` from the QuPath project base dir via getProject().getBaseDirectory() (the same base-dir idiom the script already uses for classifier JSON reads). Parse without a YAML library (QuPath's Groovy has none — the same constraint that forces the regex-grab in export_region_dapi_reference.groovy): iterate lines, open a new marker block on each list-item name line, and collect the anchor name+channel, the marker list (name/channel/compartment), the exclusion acronym set, k_robust, and ring gap/width into local variables that REPLACE the hardcoded exclusion literal (line 92), the k_robust literal (line 286), and the ring geometry literals (lines 143-144) per D-09. If any required key is missing or empty, abort with a message naming the missing key.

    Add three D-15 boundary asserts, each aborting cleanly with an actionable message:
    1. ABBA-ROIs-loaded — mirror 01_load_abba_rois.groovy: if AtlasTools.getAvailableAtlasRegistration(getCurrentImageData()) is empty, tell the operator to run the ABBA export from Fiji first, then return.
    2. Detections-present — keep the existing zero-detection guard: if getDetectionObjects() is empty, tell the operator to run BraiAnDetect first, then return.
    3. Channels-match-config (NEW) — loop each config-declared channel (anchor channel plus every marker channel) against the image channel names read from the server metadata channels; if any declared channel is absent, abort naming the specific missing channel and listing the channels the image actually exposes.
  </action>
  <verify>
    <automated>grep -q "pipeline.yml" scripts/02_detect_classify.groovy && grep -q "getAvailableAtlasRegistration" scripts/02_detect_classify.groovy && grep -Eq "getChannel|getChannels" scripts/02_detect_classify.groovy && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The script references `pipeline.yml` and reads anchor/markers/exclusions/k_robust/ring from it (positive grep passes).
    - All three fail-loud guards are present: ABBA-registration check (getAvailableAtlasRegistration), zero-detection check, and a channel-name-vs-config check that reads the image's channel metadata.
    - The old hardcoded exclusion, k_robust, and ring literals no longer drive behavior — they are sourced from the parsed config (operator/code-review confirms the literal assignments are gone).
  </acceptance_criteria>
  <done>The classify entry reads its full parameterization from pipeline.yml and refuses to run on a mis-registered / un-detected / channel-mismatched entry with a clear message (D-09, D-15).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize per-marker measurement + compound classification to the config marker set (D-01/D-02/D-03)</name>
  <files>scripts/02_detect_classify.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy</files>
  <read_first>
    - scripts/02_detect_classify.groovy (localBackgroundSubtractedMean closure lines 171-208; the two hardcoded calls lines 217-256; robust-threshold closures lines 268-357; compound-classification core lines 400-413)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-01/D-02/D-03; specifics on compartment semantics)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md (section "02_detect_classify.groovy" — the calling-loop generalization; the D-03 exact gate)
    - scripts/validate_pipeline_config.py (the contract: bg-sub key format + class vocabulary the export will read)
  </read_first>
  <behavior>
    - Given a 2-non-anchor-marker config (Fos nuclear, TdT cytoplasmic): a cell positive for both is classed Double+; positive for exactly one is classed that-marker+; positive for neither is Negative; a cell whose centroid is in an excluded region is Excluded and never marker-classified.
    - Given a 1-non-anchor-marker config: Double+ is never assigned (no code path references it); cells are that-marker+ or Negative (or Excluded). No measurement key or class string names an absent marker.
    - The bg-sub key written per marker is exactly "<compartment-label>: <channel> mean (bg-sub)" (nuclear->Nucleus base ROI = nucleus ROI; cytoplasmic->Cytoplasm base ROI = cell ROI) — matching the 06.1-01 contract the export reads.
    - Each marker's positive-call threshold is derived independently via the existing robust median + k_robust*1.4826*MAD cut on that marker's bg-sub population, with k_robust from config.
  </behavior>
  <action>
    Replace the two hardcoded localBackgroundSubtractedMean calls (lines 232/236) with a loop over the config marker list: for each marker select the base ROI by compartment (nuclear -> the detection's nucleus ROI, cytoplasmic -> the detection's cell ROI — the nucleus-anchored wiring in CLAUDE.md), compute the bg-sub value via the existing marker-agnostic closure (it already takes channelName + baseRoi + selfDetection — do not rewrite it), and write it under the contract key "<compartment-label>: <channel> mean (bg-sub)". Replace the two fixed finite-value counter blocks (lines 243-248) with per-marker accumulation so the self-diagnosing counts still print for every declared marker.

    Replace the fixed threshold blocks (lines 327-357) with a per-marker map: collect each marker's finite bg-sub population, run the existing robustThreshold closure with the config k_robust, and run the existing selfCheck closure per marker. Keep writing/reading the per-marker bg-sub classifier JSONs through the existing writeBgsubClassifierSpec/readSpec round-trip so the safe-write guard is preserved, but drive the filenames/measurement keys from the config marker list rather than the two literal names.

    Generalize the compound-classification core (lines 400-413): compute a per-cell boolean map of positive calls keyed by marker name; count the positives; set PathClass to "Double+" ONLY when the config declares >=2 non-anchor markers AND >=2 are positive on the cell (D-03 exact gate), else "<marker.name>+" when exactly one is positive, else "Negative"; keep the Excluded branch. Keep the resolveHierarchy-based atlas labeling (regionOf/regionLabel) and the SC2/SC3 print blocks intact — they are already marker-agnostic. Do NOT alter the per-region rollup here; that heuristic (lines 483-507) is superseded by the consolidated export in 06.1-03. Update the class-breakdown console print to iterate the declared markers rather than the fixed marker lines.
  </action>
  <verify>
    <automated>grep -q "Double+" scripts/02_detect_classify.groovy && grep -Eq "markers|marker\\." scripts/02_detect_classify.groovy && grep -q "bg-sub" scripts/02_detect_classify.groovy && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The classification no longer contains two hardcoded marker calls; a single loop over the config markers drives the bg-sub measure, threshold derivation, and positive calls (operator/code-review confirms the loop; no fixed two-value threshold pair remains).
    - The Double+ assignment is gated on both (a) >=2 non-anchor markers declared and (b) >=2 positive on the cell — verifiable by reading the classification branch.
    - The bg-sub key strings and class vocabulary match `scripts/validate_pipeline_config.py --config pipeline.yml` output (the 06.1-01 contract), so the 06.1-03 export reads them without drift.
    - Nucleus-anchored wiring preserved: nuclear markers measured on the nucleus ROI, cytoplasmic on the cell ROI; no geometric-overlap/proximity step introduced.
  </acceptance_criteria>
  <done>Classification is fully config-driven: 1..N markers, D-03 Double+ gate, nucleus-anchored compartment wiring, thresholds re-derived per marker with config k_robust (PIPE-01).</done>
</task>

<task type="auto">
  <name>Task 3: Retire classify_markers.groovy (D-06) — archive canonical, remove deploy copies</name>
  <files>scripts/classify_markers.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/classify_markers.groovy, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/classify_markers.groovy, _archive/retired-scripts/06.1/classify_markers.groovy</files>
  <read_first>
    - scripts/classify_markers.groovy (the retire target — confirm its classification core lines 56-71 is the subset already inside 02_detect_classify.groovy lines 400-413)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (Claude's Discretion: delete vs archive; follow the workspace archive convention; grep for QuPath project references before moving)
    - _archive/README.md (archive convention)
  </read_first>
  <action>
    Confirm via grep that there is no runtime dependency on classify_markers.groovy beyond the two byte-identical deploy copies and historical .planning docs (the header comment in 02_detect_classify.groovy that names it as its base is documentation only, not an import). Create `_archive/retired-scripts/06.1/`, then `git mv scripts/classify_markers.groovy _archive/retired-scripts/06.1/classify_markers.groovy`, and `git rm` the two deploy copies under the M3 and wBA project `scripts/` dirs. Add a one-line supersession note (top of the archived file or a sibling README) recording it is replaced by 02_detect_classify.groovy per D-06. Leave the .planning/ historical references untouched.
  </action>
  <verify>
    <automated>test ! -f scripts/classify_markers.groovy && test ! -f "M3 Hippocampus 20x 062926 3 plane/scripts/classify_markers.groovy" && test ! -f "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/classify_markers.groovy" && test -f _archive/retired-scripts/06.1/classify_markers.groovy && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/classify_markers.groovy` and both QuPath-project deploy copies no longer exist.
    - `_archive/retired-scripts/06.1/classify_markers.groovy` exists with a supersession note.
    - No executable script imports/evaluates classify_markers.groovy (grep confirms only doc/comment references remain).
  </acceptance_criteria>
  <done>The two-implementation redundancy is gone — one classification implementation (02_detect_classify.groovy) remains (D-06, PIPE-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> pipeline.yml | operator-authored local config drives which channels/compartments/exclusions classification uses |
| ABBA/BraiAnDetect GUI -> classify script | registration + detections are produced by human-in-the-loop GUI steps upstream; the script consumes their on-disk state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-03 | Tampering | pipeline.yml line-block parse in groovy | low | mitigate | fail-loud abort naming the missing/empty key; channel-match assert catches a wrong channel before any classification runs |
| T-06.1-04 | Spoofing | wrong/un-registered entry under "Run for project" | low | mitigate | ABBA-ROIs-loaded + detections-present guards abort cleanly on a mis-prepared entry (D-15) |
| T-06.1-05 | Repudiation | silent mis-classification from stale bg-sub JSONs | low | mitigate | per-marker thresholds re-derived + safe-write round-trip preserved; class-breakdown printed each run for operator audit |
| T-06.1-SC | Tampering | package installs | low | accept | No new package installs — groovy runs in QuPath's bundled JRE; no dependency added |

**ASVS L1 review conclusion:** Local offline single-user tool; no network/auth surface. Realistic surface is config-parse robustness and running on a mis-prepared entry — both covered by the D-15 fail-loud guards. No high-severity threats.
</threat_model>

<verification>
- `grep pipeline.yml scripts/02_detect_classify.groovy` matches (config-driven).
- The three D-15 guards are present (getAvailableAtlasRegistration, zero-detection, channel-match).
- `scripts/classify_markers.groovy` and both deploy copies are gone; the archived copy exists.
- Behavior verification (marker loop, D-03 gate, nucleus-anchored wiring) is confirmed at the operator end-to-end run in 06.1-06 (no headless QuPath CI exists).
</verification>

<success_criteria>
The classify entry is config-driven for 1..N markers with the D-03 Double+ gate and nucleus-anchored compartment wiring (PIPE-01), guarded by fail-loud preconditions (PIPE-05), and the duplicate classifier is retired leaving one implementation (PIPE-02).
</success_criteria>

<output>
Create `.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-02-SUMMARY.md` when done.
</output>
