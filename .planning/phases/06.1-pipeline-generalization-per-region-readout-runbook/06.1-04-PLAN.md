---
phase: 06.1-pipeline-generalization-per-region-readout-runbook
plan: 04
type: execute
wave: 3
depends_on: ["06.1-02", "06.1-03"]
files_modified:
  - scripts/build_dapi_reference.py
  - scripts/verify_export_integrity.py
  - scripts/val01_metrics.py
  - scripts/03_export_val01_metrics.groovy
  - scripts/export_region_dapi_reference.groovy
  - "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy"
  - "M3 Hippocampus 20x 062926 3 plane/scripts/export_region_dapi_reference.groovy"
  - "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/03_export_val01_metrics.groovy"
  - _archive/retired-scripts/06.1/03_export_val01_metrics.groovy
  - _archive/retired-scripts/06.1/export_region_dapi_reference.groovy
autonomous: true
requirements: [PIPE-02, PIPE-04]

must_haves:
  truths:
    - "The Python aggregator reads the long/tidy combined CSV and groups per (config_tag, marker, class, region) — aggregation-ready for BraiAnalyse/pandas (D-13)"
    - "A runnable zero-leak re-proof confirms, from the combined CSV alone, that summed leaf counts equal the root rollup per (slice, marker, class) (D-10)"
    - "The export-integrity checker validates the new per-slice consolidated export filenames without cross-slice clobbering (D-12)"
    - "The bioplausibility metrics tolerate a variable <marker>_bgsub column set and skip absent-marker metrics on a TdT-only slice (D-04)"
    - "The two export scripts folded into the consolidated exporter are retired, leaving one export implementation (D-07)"
  artifacts:
    - path: "scripts/build_dapi_reference.py"
      provides: "Long/tidy combined-CSV aggregator + --check-zero-leak re-proof"
      contains: "marker"
    - path: "scripts/verify_export_integrity.py"
      provides: "Per-slice non-clobbering checker for the consolidated export filenames"
      contains: "__region_table.tsv"
    - path: "_archive/retired-scripts/06.1/export_region_dapi_reference.groovy"
      provides: "Archived superseded DAPI-only region exporter (folded into consolidated export)"
      contains: "region"
  key_links:
    - from: "scripts/build_dapi_reference.py"
      to: "reference/region_marker_combined.csv"
      via: "reads the long/tidy combined CSV emitted by 03_export_region_table.groovy"
      pattern: "region_marker_combined"
  prohibitions:
    - "NO second export implementation left alongside — export_region_dapi_reference.groovy and 03_export_val01_metrics.groovy are archived, not kept (D-07)"
    - "NO hardcoded two-column fos/tdt requirement in the Python consumers — the per-cell/combined schema is driven by whichever markers are present (D-04)"
    - "NO NA-filled marker rows in the aggregate — absent markers simply have no rows (D-13)"
---

<objective>
Bring the Python consumers to the consolidated export's schema and retire the two export scripts whose roles fold into it (D-07). Generalize `build_dapi_reference.py` to aggregate the long/tidy combined CSV (D-13) and add a `--check-zero-leak` re-proof of the D-10 parent rollup; update `verify_export_integrity.py`'s suffix constants to the new per-slice export filenames; and generalize `val01_metrics.py` to tolerate a variable `<marker>_bgsub` column set (so the D-17 bioplausibility spot-check runs on a TdT-only slice). Then archive `03_export_val01_metrics.groovy` and `export_region_dapi_reference.groovy` (+ deploy copies).

Purpose: complete the consolidation (PIPE-02) and make the growing combined CSV aggregation-ready with a machine-checkable zero-leak invariant (PIPE-04, D-10).
Output: three generalized Python consumers and two archived groovy exporters.
</objective>

<artifacts_this_phase_produces>
This plan modifies/produces:
- `scripts/build_dapi_reference.py` — generalized `load`/`aggregate`/`flag_image` to the long/tidy schema (`slice,config_tag,region,hemisphere,is_leaf,marker,class,count,density`), default `--ref-csv` = `reference/region_marker_combined.csv`, new `--check-zero-leak` mode.
- `scripts/verify_export_integrity.py` — `PERCELL_SUFFIX = "__percell_export.tsv"`, `REGION_SUFFIX = "__region_table.tsv"`.
- `scripts/val01_metrics.py` — variable `<marker>_bgsub` column tolerance; absent-marker metrics skipped; defaults repointed to the consolidated export filenames.
- `_archive/retired-scripts/06.1/03_export_val01_metrics.groovy` and `_archive/retired-scripts/06.1/export_region_dapi_reference.groovy` (git mv); deploy copies git-rm'd (M3 has both; wBA has 03 only).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md
@scripts/build_dapi_reference.py
@scripts/verify_export_integrity.py
@scripts/val01_metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize build_dapi_reference.py to the long/tidy combined CSV + --check-zero-leak (D-13/D-10)</name>
  <files>scripts/build_dapi_reference.py</files>
  <read_first>
    - scripts/build_dapi_reference.py (whole file; load lines 25-36, aggregate lines 39-47, flag_image lines 50-61, CLI lines 64-101)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-10, D-13)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md (build_dapi_reference.py generalization: groupby + drop_duplicates add marker/class)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-03-PLAN.md (the combined CSV schema this reads)
  </read_first>
  <action>
    Generalize `build_dapi_reference.py` in place to the long/tidy schema `slice,config_tag,region,hemisphere,is_leaf,marker,class,count,density`. In `load`: default `--ref-csv` to `reference/region_marker_combined.csv`, coerce is_leaf to bool, fillna hemisphere, and change the drop_duplicates subset to `[config_tag, slice, region, marker, class]` (rows are now per region x marker x class, not one-per-region). In `aggregate`: change the groupby key to `[config_tag, marker, class, hemisphere, region]` and aggregate `density` (mean/sd/min/max/count-of-slices) plus sum of `count`, so stats are per marker/class combination. Update the docstring to say markers/classes, not DAPI.

    Add a `--check-zero-leak` mode: for each `(slice, config_tag, marker, class)` group, assert that the sum of `count` over is_leaf==True rows equals the maximum `count` among is_leaf==False rows (the atlas-root rollup contains every leaf, so its count is that maximum) — this re-proves the D-10 leaf-summed invariant from the CSV alone. Print PASS/FAIL per group and exit non-zero on any mismatch. Include a `--self-test` that builds a synthetic combined CSV (a small tree: two leaves under one root, for a TdT-only marker set) and asserts (a) the aggregate groups by marker/class, (b) --check-zero-leak passes when root==sum(leaves) and fails when a leaf count is corrupted.
  </action>
  <verify>
    <automated>conda run -n braian python scripts/build_dapi_reference.py --self-test && grep -Eq "marker" scripts/build_dapi_reference.py && grep -q "region_marker_combined" scripts/build_dapi_reference.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `--self-test` passes: aggregate groups by marker/class; --check-zero-leak passes on a consistent synthetic tree and fails on a corrupted-leaf tree.
    - Default input is `reference/region_marker_combined.csv`; drop_duplicates + groupby include marker and class.
    - The aggregate never fabricates rows for absent markers (a TdT-only synthetic input yields only DAPI/TdT groups).
  </acceptance_criteria>
  <done>The aggregator consumes the long/tidy combined CSV and re-proves the zero-leak parent rollup from the CSV alone (D-13, D-10).</done>
</task>

<task type="auto">
  <name>Task 2: Update verify_export_integrity.py suffixes + generalize val01_metrics.py to variable markers (D-04)</name>
  <files>scripts/verify_export_integrity.py, scripts/val01_metrics.py</files>
  <read_first>
    - scripts/verify_export_integrity.py (whole file; suffix constants lines 38-39; pairing/non-clobber lines 84-142)
    - scripts/val01_metrics.py (PERCELL_EXPECTED_COLS + targets lines 43-56; load_percell lines 90-99; compute_* lines 108-186)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-03-PLAN.md (the new per-cell/region-table filenames + per-marker <marker>_bgsub columns)
  </read_first>
  <action>
    In `verify_export_integrity.py`, rename the two suffix constants to the consolidated export's filenames: PERCELL_SUFFIX = "__percell_export.tsv" and REGION_SUFFIX = "__region_table.tsv". Leave the pairing / even-count / non-clobbering assertions unchanged (they key off the suffixes) and update the module docstring's example filenames.

    In `val01_metrics.py`, generalize the per-cell schema handling: instead of requiring the fixed fos_bgsub/tdt_bgsub columns, detect the `<marker>_bgsub` columns actually present and coerce those to numeric; repoint DEFAULT_PERCELL_TSV / DEFAULT_REGION_TSV to the consolidated export's filenames. Make the Fos/Double-dependent metrics (double/tdt ratio, Fos control rate) skip gracefully with a printed "marker absent — metric skipped" note when their marker column is not present, while the marker-agnostic metrics (DAPI density per region, nucleus-area peak) still run — so the D-17 bioplausibility spot-check works on a TdT-only slice. Keep the existing target bands.
  </action>
  <verify>
    <automated>grep -q "__region_table.tsv" scripts/verify_export_integrity.py && grep -q "__percell_export.tsv" scripts/verify_export_integrity.py && conda run -n braian python -c "import ast; ast.parse(open('scripts/val01_metrics.py').read()); ast.parse(open('scripts/verify_export_integrity.py').read()); print('OK')" && grep -Eq "_bgsub|bgsub" scripts/val01_metrics.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - verify_export_integrity.py keys off `__percell_export.tsv` / `__region_table.tsv`; both files still parse.
    - val01_metrics.py detects present `<marker>_bgsub` columns rather than hard-requiring fos+tdt, and skips Fos/Double metrics when absent while still computing DAPI density + nucleus-area peak.
    - Both scripts import/parse cleanly (ast.parse OK).
  </acceptance_criteria>
  <done>The per-slice non-clobber checker and the bioplausibility metrics work against the consolidated export on a variable marker set, including TdT-only (D-04, D-12).</done>
</task>

<task type="auto">
  <name>Task 3: Retire export_region_dapi_reference.groovy + 03_export_val01_metrics.groovy (D-07 fold-in)</name>
  <files>scripts/03_export_val01_metrics.groovy, scripts/export_region_dapi_reference.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy, M3 Hippocampus 20x 062926 3 plane/scripts/export_region_dapi_reference.groovy, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/03_export_val01_metrics.groovy, _archive/retired-scripts/06.1/03_export_val01_metrics.groovy, _archive/retired-scripts/06.1/export_region_dapi_reference.groovy</files>
  <read_first>
    - scripts/export_region_dapi_reference.groovy and scripts/03_export_val01_metrics.groovy (confirm their roles are fully covered by 03_export_region_table.groovy from 06.1-03)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-07; workspace archive convention; grep for QuPath refs before moving)
  </read_first>
  <action>
    Confirm via grep that no active script imports/evaluates these two exporters (only doc/comment references and their deploy copies exist). git mv the two canonical files into `_archive/retired-scripts/06.1/`, and git rm their deploy copies: "M3 Hippocampus 20x 062926 3 plane/scripts/03_export_val01_metrics.groovy", "M3 Hippocampus 20x 062926 3 plane/scripts/export_region_dapi_reference.groovy", and "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/03_export_val01_metrics.groovy" (wBA has no export_region_dapi_reference copy — do not invent one). Add a one-line supersession note recording they are folded into 03_export_region_table.groovy per D-07. Leave the historical reference/dapi_region_reference.csv and .planning/ references untouched.
  </action>
  <verify>
    <automated>test ! -f scripts/03_export_val01_metrics.groovy && test ! -f scripts/export_region_dapi_reference.groovy && test -f _archive/retired-scripts/06.1/03_export_val01_metrics.groovy && test -f _archive/retired-scripts/06.1/export_region_dapi_reference.groovy && test ! -f "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/03_export_val01_metrics.groovy" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both canonical export scripts and their deploy copies no longer exist under scripts/ or the project scripts/ dirs.
    - Both are archived under `_archive/retired-scripts/06.1/` with a supersession note.
    - The only remaining export implementation is `scripts/03_export_region_table.groovy` (grep confirms one export entry).
  </acceptance_criteria>
  <done>One export implementation remains; the DAPI-only and split per-cell/region exporters are folded in and archived (D-07, PIPE-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combined CSV / results TSVs -> Python consumers | on-disk export artifacts (operator-produced via GUI) parsed by pandas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-09 | Repudiation | undetected parent-rollup leak in the combined CSV | low | mitigate | --check-zero-leak re-proves summed leaves == root rollup from the CSV, independent of the groovy assertion |
| T-06.1-10 | Tampering | cross-slice clobbering of per-slice TSVs | low | mitigate | verify_export_integrity.py pairing + non-clobber checks on the new suffixes |
| T-06.1-11 | Denial of Service | consumer crash on absent marker columns | low | mitigate | column-presence detection instead of hard-required fos/tdt; absent-marker metrics skipped |
| T-06.1-SC | Tampering | package installs | low | accept | No new package installs — pandas/numpy/scipy/pyyaml already in the braian env |

**ASVS L1 review conclusion:** Local offline analysis scripts over operator-owned files; the integrity surface is the combined CSV, independently re-proved by --check-zero-leak. No high-severity threats.
</threat_model>

<verification>
- `conda run -n braian python scripts/build_dapi_reference.py --self-test` passes (aggregate + zero-leak re-proof).
- verify_export_integrity.py + val01_metrics.py parse and key off the new filenames / variable marker columns.
- The two superseded export scripts and their deploy copies are archived; one export implementation remains.
</verification>

<success_criteria>
The Python consumers read the consolidated export's long/tidy schema, re-prove the D-10 zero-leak invariant, and tolerate a variable marker set (PIPE-04); the two folded-in export scripts are retired leaving one export implementation (PIPE-02).
</success_criteria>

<output>
Create `.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-04-SUMMARY.md` when done.
</output>
