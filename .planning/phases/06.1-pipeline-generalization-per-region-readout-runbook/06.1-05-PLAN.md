---
phase: 06.1-pipeline-generalization-per-region-readout-runbook
plan: 05
type: execute
wave: 4
depends_on: ["06.1-01", "06.1-02", "06.1-03", "06.1-04"]
files_modified:
  - RUNBOOK.md
  - docs/runbook/01-registration.md
  - docs/runbook/02-detection.md
autonomous: true
requirements: [PIPE-06]

must_haves:
  truths:
    - "A single RUNBOOK.md is an operator checklist covering czi->MIP -> ABBA register -> BraiAnDetect -> classify -> export -> per-region table, each step tagged GUI-vs-scriptable with exact commands and QuPath/ABBA click-paths (D-16)"
    - "The pipeline.yml marker-config block is documented in the runbook (name->channel->compartment, anchor, exclusions), including how to declare a TdT-only slice set (D-16, D-01)"
    - "The dual-location deploy convention (author in canonical scripts/, hard-copy into the QuPath project scripts/) is documented (D-16)"
    - "Per-stage detail docs back the two confusing GUI stages: ABBA registration and BraiAnDetect detection (D-16)"
    - "The runbook reflects the final consolidated script inventory (03_export_region_table.groovy; classify_markers/export_region_dapi_reference/03_export_val01_metrics retired)"
  artifacts:
    - path: "RUNBOOK.md"
      provides: "Reusable operator checklist for the whole per-region-readout pipeline"
      contains: "pipeline.yml"
    - path: "docs/runbook/01-registration.md"
      provides: "ABBA registration per-stage detail doc"
      contains: "ABBA"
    - path: "docs/runbook/02-detection.md"
      provides: "BraiAnDetect detection per-stage detail doc"
      contains: "BraiAnDetect"
  key_links:
    - from: "RUNBOOK.md"
      to: "scripts/03_export_region_table.groovy"
      via: "documents the export step + its output files (per-cell TSV, per-region table, combined CSV)"
      pattern: "03_export_region_table"
  prohibitions:
    - "NO references to retired scripts as live steps — classify_markers.groovy, export_region_dapi_reference.groovy, 03_export_val01_metrics.groovy are archived, not runbook steps"
    - "NO GUI step presented as scriptable or vice versa — each step is explicitly tagged"
---

<objective>
Capture the consolidated pipeline as a single reusable operator checklist `RUNBOOK.md` (D-16), each stage tagged GUI-vs-scriptable with exact commands and QuPath/ABBA click-paths, the `pipeline.yml` marker-config block documented (including the TdT-only declaration), the dual-location deploy convention spelled out, and the final consolidated script inventory reflected. Back the two confusing GUI stages (ABBA registration, BraiAnDetect detection) with per-stage detail docs.

Purpose: make the generalized pipeline reusable for any new slice set by any operator (PIPE-06).
Output: `RUNBOOK.md` plus `docs/runbook/01-registration.md` and `docs/runbook/02-detection.md`.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `RUNBOOK.md` (repo root) — the operator checklist: stages czi->MIP, ABBA register, BraiAnDetect, classify (02_detect_classify.groovy), export (03_export_region_table.groovy), aggregate/verify (build_dapi_reference.py, verify_export_integrity.py); each tagged [GUI] or [SCRIPTABLE]; the pipeline.yml marker-config block; the dual-location deploy step; the retired-scripts note.
- `docs/runbook/01-registration.md` — ABBA registration detail (Fiji click-paths, DeepSlice->angle->export, the ABBA-Transform/ABBA-RoiSet outputs).
- `docs/runbook/02-detection.md` — BraiAnDetect detection detail (BraiAn.yml vs pipeline.yml split, run_braian_detection.groovy, the re-detect/re-classify tuning loop).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md
@CLAUDE.md
@scripts/01_load_abba_rois.groovy
@scripts/run_braian_detection.groovy
@pipeline.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author RUNBOOK.md — the single operator checklist</name>
  <files>RUNBOOK.md</files>
  <read_first>
    - scripts/01_load_abba_rois.groovy (docstring header REQUIREMENTS/USAGE convention + the exact ABBA export click-path lines 36-38)
    - scripts/run_braian_detection.groovy (USAGE block lines 15-21 — the detect/tune loop, dual-location deploy note)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-16 exact scope; established patterns: GUI-vs-scriptable seams; dual-location deploy)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-PATTERNS.md (RUNBOOK section — structural precedents + dual-location deploy paths)
    - pipeline.yml; scripts/03_export_region_table.groovy (the export step + output files to document)
  </read_first>
  <action>
    Write `RUNBOOK.md` at the repo root as an operator checklist with one numbered section per stage in pipeline order: (1) czi->MIP [SCRIPTABLE] (czi_mip.py, the channel-order override note), (2) ABBA atlas registration [GUI] (Fiji click-path, link to docs/runbook/01-registration.md), (3) load ABBA ROIs [SCRIPTABLE] (01_load_abba_rois.groovy via "Run for project"), (4) BraiAnDetect detection [GUI] (run_braian_detection.groovy, link to docs/runbook/02-detection.md), (5) classify [SCRIPTABLE-authored/GUI-run] (02_detect_classify.groovy "Run for project"), (6) export per-region table [SCRIPTABLE-authored/GUI-run] (03_export_region_table.groovy -> per-cell TSV + per-region table + combined CSV), (7) aggregate + verify [SCRIPTABLE] (build_dapi_reference.py, verify_export_integrity.py). Tag every step [GUI] or [SCRIPTABLE] explicitly and give exact commands (conda run -n braian ... for Python; Automate > Run for project for groovy) and QuPath/ABBA menu click-paths.

    Include a "Marker config (pipeline.yml)" section documenting the anchor and name->channel->compartment marker block, the exclude_acronyms/k_robust/ring keys, and a worked TdT-only example showing the config produces DAPI+/TdT+ only (no Fos/Double). Include a "Deploy" section documenting the dual-location convention (author canonical scripts/, hard-copy byte-identically into the QuPath project scripts/ + copy pipeline.yml into the project base dir next to BraiAn.yml before "Run for project"). Add a short "Retired scripts" note pointing to _archive/retired-scripts/06.1/ so operators don't run the superseded scripts.
  </action>
  <verify>
    <automated>test -f RUNBOOK.md && grep -q "pipeline.yml" RUNBOOK.md && grep -q "03_export_region_table" RUNBOOK.md && grep -Eq "\\[GUI\\]" RUNBOOK.md && grep -Eq "\\[SCRIPTABLE\\]" RUNBOOK.md && grep -Eiq "TdT-only|TdT only" RUNBOOK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - RUNBOOK.md has one section per pipeline stage (czi->MIP through aggregate/verify), each tagged [GUI] or [SCRIPTABLE] with exact commands/click-paths.
    - The pipeline.yml marker-config block is documented including a worked TdT-only example.
    - The dual-location deploy convention and the retired-scripts note are present; the export step names 03_export_region_table.groovy and its three output files.
    - No retired script (classify_markers / export_region_dapi_reference / 03_export_val01_metrics) is presented as a live step.
  </acceptance_criteria>
  <done>A single reusable operator checklist documents the whole config-driven pipeline with GUI/scriptable tagging and the marker-config block (D-16).</done>
</task>

<task type="auto">
  <name>Task 2: Author the two per-stage detail docs (registration, detection)</name>
  <files>docs/runbook/01-registration.md, docs/runbook/02-detection.md</files>
  <read_first>
    - scripts/01_load_abba_rois.groovy (ABBA requirements + export click-path)
    - scripts/run_braian_detection.groovy (BraiAnDetect topology + tuning loop + the ImageChannelTools bug note)
    - wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/BraiAn.yml (detection params that stay in BraiAn.yml, NOT pipeline.yml)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-14 split: BraiAn.yml = detection params; pipeline.yml = markers/anchor/exclusions)
  </read_first>
  <action>
    Create `docs/runbook/01-registration.md`: the ABBA registration stage in detail — prerequisites (QuPath ABBA extension, elastix 5.2.0, image imported FROM the QuPath project first per the known pitfall), the DeepSlice -> manual angle -> export workflow, the exact Fiji click-path to export registrations to the QuPath project, and the on-disk outputs (ABBA-Transform-*.json, ABBA-RoiSet-*.zip in data/<N>/) that 01_load_abba_rois.groovy consumes. Note this is the [GUI] seam the D-15 ABBA-ROIs assert guards.

    Create `docs/runbook/02-detection.md`: the BraiAnDetect detection stage in detail — the BraiAn.yml (detection params: sigma/area/histogramThreshold/cellExpansion) vs pipeline.yml (markers/anchor/exclusions) split (D-14), running run_braian_detection.groovy, the re-detect/re-classify tuning loop (delete detections, edit BraiAn.yml, re-run), and that classification is a separate step (02_detect_classify.groovy) reading pipeline.yml. Note this is the [GUI] seam the D-15 detections-present + channel-match asserts guard.
  </action>
  <verify>
    <automated>test -f docs/runbook/01-registration.md && test -f docs/runbook/02-detection.md && grep -qi "ABBA" docs/runbook/01-registration.md && grep -qi "BraiAnDetect" docs/runbook/02-detection.md && grep -q "BraiAn.yml" docs/runbook/02-detection.md && grep -q "pipeline.yml" docs/runbook/02-detection.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both per-stage docs exist and cover their GUI stage in operator detail with exact click-paths / commands.
    - The detection doc states the BraiAn.yml vs pipeline.yml responsibility split (D-14).
    - Each doc notes which D-15 boundary assert guards its seam.
  </acceptance_criteria>
  <done>The two confusing GUI stages have backing detail docs the runbook links to (D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> runbook | documentation only; no code execution, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-12 | Repudiation | operator runs a retired script by mistake | low | mitigate | runbook explicitly lists the final script inventory + a retired-scripts note pointing at _archive/ |
| T-06.1-SC | Tampering | package installs | low | accept | Documentation only — no installs, no executable surface |

**ASVS L1 review conclusion:** Documentation artifact with no runtime surface. No high-severity threats.
</threat_model>

<verification>
- RUNBOOK.md and both per-stage docs exist with the required sections/markers (positive greps pass).
- The runbook names the final scripts and no retired script as a live step.
</verification>

<success_criteria>
The pipeline is captured as a single reusable operator RUNBOOK.md with GUI/scriptable tagging, the marker-config block, the dual-location deploy convention, and per-stage detail docs for registration and detection (PIPE-06, D-16).
</success_criteria>

<output>
Create `.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-05-SUMMARY.md` when done.
</output>
