---
phase: 06.1-pipeline-generalization-per-region-readout-runbook
plan: 06
type: execute
wave: 5
depends_on: ["06.1-05"]
files_modified:
  - pipeline.tdt-only.yml
  - docs/runbook/VALIDATION-tdt-only.md
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05, PIPE-06]

must_haves:
  truths:
    - "The consolidated pipeline runs end-to-end on the incoming TdT-only slice set following RUNBOOK.md, exercising the variable-marker path for the first time (D-17)"
    - "The per-region table is schema-correct and non-empty for the target region(s), with Fos+/Double+ columns AND rows ABSENT (not NA) — the D-04/D-13 omit-by-construction behavior (D-17)"
    - "The zero-leak invariant re-proves on the real output: summed leaf counts equal the root rollup per marker/class (D-10)"
    - "A spot bioplausibility check confirms DAPI+ densities land in the ~Phase-2 band (500-2000/mm2) with sane counts (D-17)"
  artifacts:
    - path: "pipeline.tdt-only.yml"
      provides: "TdT-only validation config (anchor DAPI + single TdT marker) proving Double+ omission"
      contains: "compartment"
    - path: "docs/runbook/VALIDATION-tdt-only.md"
      provides: "Recorded end-to-end validation outcome (table schema, zero-leak, density band)"
      contains: "TdT"
  key_links:
    - from: "pipeline.tdt-only.yml"
      to: "scripts/validate_pipeline_config.py"
      via: "validator confirms single-marker config omits Double+ and Fos"
      pattern: "validate_pipeline_config"
  prohibitions:
    - "NO acceptance via 'looks correct' — the table schema, zero-leak, and density band are the concrete gates"
    - "NO NA/placeholder Fos or Double+ columns/rows on the TdT-only output — absence is by construction"
---

<objective>
Validate the consolidated, config-driven pipeline end-to-end on the incoming TdT-only slice set (D-17) — the first real exercise of the variable-marker path. This is a human-in-the-loop GUI validation: the executor prepares and validates the TdT-only config and runs the scriptable post-checks, while the operator at the monitor runs the ABBA registration + BraiAnDetect + classify + export GUI steps following RUNBOOK.md.

Acceptance bar (D-17): a schema-correct, non-empty per-region table for the target region(s) with Fos+/Double+ columns and rows correctly ABSENT, the zero-leak invariant re-proved on the real output, and a spot bioplausibility check (DAPI+ densities in the ~Phase-2 band).

Purpose: prove the whole pipeline (PIPE-01..05) actually works on variable markers and lock it as reusable (PIPE-06).
Output: `pipeline.tdt-only.yml` (validation config) and `docs/runbook/VALIDATION-tdt-only.md` (recorded outcome).
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `pipeline.tdt-only.yml` (repo root) — validation config: anchor DAPI + a single TdT marker (cytoplasmic), exclude_acronyms/k_robust/ring as usual. No Fos marker.
- `docs/runbook/VALIDATION-tdt-only.md` — the recorded end-to-end validation outcome (table row counts, omitted columns confirmed, zero-leak PASS numbers, DAPI+ density band result, any deviations).
- GUI-run outputs (in the validation QuPath project's results/ and the reference/ combined CSV) — produced by the operator; typically untracked microscopy-project artifacts.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md
@CLAUDE.md
@RUNBOOK.md
@pipeline.yml
@scripts/validate_pipeline_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + validate the TdT-only validation config</name>
  <files>pipeline.tdt-only.yml</files>
  <read_first>
    - pipeline.yml (the wBA-default Fos+TdT config to derive the TdT-only variant from)
    - scripts/validate_pipeline_config.py (the validator that proves Double+ omission)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-17 acceptance bar; D-01..D-04)
  </read_first>
  <action>
    Create `pipeline.tdt-only.yml` at the repo root: same schema as pipeline.yml but with the markers list containing ONLY the TdT marker (name TdT, its cytoplasmic channel, compartment cytoplasmic), the DAPI anchor unchanged, and exclude_acronyms/k_robust/ring unchanged. Add a header comment stating this is the TdT-only validation config for D-17 (single non-anchor marker -> Double+ auto-omitted per D-03; per-slice table emits DAPI+/TdT+ only per D-04). Run validate_pipeline_config.py against it and confirm the derived contract has no Double+ class and no Fos column.
  </action>
  <verify>
    <automated>conda run -n braian python scripts/validate_pipeline_config.py --config pipeline.tdt-only.yml && conda run -n braian python -c "import yaml; c=yaml.safe_load(open('pipeline.tdt-only.yml')); ms=c['markers']; assert len(ms)==1 and ms[0]['compartment']=='cytoplasmic'; assert not any(m['name'].lower().startswith('fos') for m in ms); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline.tdt-only.yml` declares exactly one non-anchor marker (TdT, cytoplasmic) and the DAPI anchor.
    - validate_pipeline_config.py against it reports a class vocabulary WITHOUT Double+ and a column set WITHOUT any Fos column.
  </acceptance_criteria>
  <done>The TdT-only validation config exists and is proven, config-side, to omit Double+/Fos (D-03/D-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the GUI pipeline end-to-end on the TdT-only slice set</name>
  <files>(operator-produced, typically untracked) validation QuPath project results/*__region_table.tsv + reference/region_marker_combined.csv</files>
  <read_first>
    - RUNBOOK.md (the operator checklist to follow for the validation run)
    - docs/runbook/01-registration.md and docs/runbook/02-detection.md (the two GUI stages)
    - pipeline.tdt-only.yml (the config to deploy into the validation project base dir)
  </read_first>
  <action>
    Human-in-the-loop GUI step — the executor CANNOT automate this (no headless QuPath). The operator at the monitor deploys the scripts + pipeline.tdt-only.yml to the validation QuPath project and runs register -> load ROIs -> BraiAnDetect -> classify -> export per RUNBOOK.md, then confirms the per-region table schema and the zero-leak PASS as described below. The executor waits at this blocking checkpoint and resumes on the operator's signal.
  </action>
  <what-built>
    The full config-driven pipeline: 02_detect_classify.groovy (config-driven classify) and 03_export_region_table.groovy (consolidated export), driven by pipeline.tdt-only.yml, with fail-loud D-15 asserts. All authoring and the config-side proof are done; only the GUI-run stages remain, which the executor cannot automate (no headless QuPath).
  </what-built>
  <how-to-verify>
    Following RUNBOOK.md on the incoming TdT-only slice set:
    1. Deploy: hard-copy the canonical scripts into the validation QuPath project's scripts/ and copy pipeline.tdt-only.yml into the project base dir as pipeline.yml (next to BraiAn.yml).
    2. [GUI] Register each slice in Fiji ABBA (DeepSlice -> angle -> export) and run 01_load_abba_rois.groovy.
    3. [GUI] Run run_braian_detection.groovy (BraiAnDetect) on each entry.
    4. [GUI] Run 02_detect_classify.groovy "for project" — confirm it does NOT abort (channels match the TdT-only config; ABBA ROIs + detections present) and the class breakdown shows only TdT+/Negative/Excluded (no Fos+/Double+).
    5. [GUI] Run 03_export_region_table.groovy "for project".
    6. Open the per-region table results/<stem>__region_table.tsv and confirm: it is non-empty for the target region(s); it has DAPI+ and TdT+ count/density columns; it has NO Fos or Double+ columns (absent, not NA). Confirm the console printed "zero-leak PASS".
    Record the results/ and reference/ directory paths and the observed row/column set.
  </how-to-verify>
  <acceptance_criteria>
    - 02_detect_classify.groovy runs without aborting and classifies only TdT+/Negative/Excluded on the TdT-only set.
    - The per-region table is non-empty for the target region(s) and has DAPI+/TdT+ columns with Fos+/Double+ columns ABSENT (not NA).
    - The export console shows a zero-leak PASS.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the results/ + reference/ paths, or describe what aborted / what the table shows.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Scriptable post-checks + record the validation outcome</name>
  <files>docs/runbook/VALIDATION-tdt-only.md</files>
  <read_first>
    - scripts/verify_export_integrity.py (per-slice non-clobber checker)
    - scripts/build_dapi_reference.py (--check-zero-leak re-proof over the combined CSV)
    - scripts/val01_metrics.py (DAPI density + nucleus-area peak bioplausibility)
    - .planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-CONTEXT.md (D-17 acceptance bar: ~Phase-2 density band, sane counts)
  </read_first>
  <action>
    Against the operator-produced outputs (paths recorded in Task 2), run the scriptable post-checks: verify_export_integrity.py --results &lt;validation results dir&gt; (pairing + per-slice non-clobber); build_dapi_reference.py --ref-csv &lt;validation reference combined CSV&gt; --check-zero-leak (re-prove summed leaves == root rollup per marker/class, D-10); val01_metrics.py on the per-cell + region-table outputs for the DAPI density (500-2000/mm2 band) and nucleus-area-peak metrics (the marker-agnostic bioplausibility, since Fos/Double metrics are absent on TdT-only). Write `docs/runbook/VALIDATION-tdt-only.md` recording: the per-region table row/column set (Fos/Double omission confirmed), the zero-leak PASS numbers, the DAPI+ density band result, and any deviations. If any check fails, record it as a gap for a follow-up (do not silently pass).
  </action>
  <verify>
    <automated>conda run -n braian python scripts/build_dapi_reference.py --self-test && test -f docs/runbook/VALIDATION-tdt-only.md && grep -Eiq "zero.?leak" docs/runbook/VALIDATION-tdt-only.md && grep -Eiq "densit" docs/runbook/VALIDATION-tdt-only.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - verify_export_integrity.py passes on the validation outputs (pairing + non-clobber); build_dapi_reference.py --check-zero-leak passes (summed leaves == root rollup); val01_metrics reports DAPI+ densities in/near the ~Phase-2 band.
    - `docs/runbook/VALIDATION-tdt-only.md` records the table schema (Fos/Double omitted), the zero-leak PASS numbers, and the DAPI+ density band result.
    - Any failing check is recorded as an explicit gap, not silently passed.
  </acceptance_criteria>
  <done>The variable-marker pipeline is validated end-to-end on the TdT-only set against the D-17 bar and the outcome is recorded (PIPE-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator GUI runs -> on-disk outputs | registration/detection/classify/export produced via human-in-the-loop GUI; post-checks validate their integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-13 | Repudiation | a subtly wrong table passed as "looks correct" | low | mitigate | concrete gates: schema (Fos/Double absent), zero-leak re-proof, DAPI density band — recorded in VALIDATION-tdt-only.md |
| T-06.1-14 | Tampering | wrong channel index / mis-registered slice silently classified | low | mitigate | D-15 channel-match + ABBA-ROIs asserts abort before classification on a mis-prepared entry |
| T-06.1-SC | Tampering | package installs | low | accept | No new installs — existing envs; GUI tools are the pinned QuPath 0.6.0 / elastix 5.2.0 |

**ASVS L1 review conclusion:** Local offline validation; the surface is output integrity, covered by the re-provable zero-leak + density-band gates and the upstream D-15 asserts. No high-severity threats.
</threat_model>

<verification>
- pipeline.tdt-only.yml validates with Double+/Fos omitted (config-side).
- Operator confirms the per-region table is schema-correct, non-empty, Fos/Double absent, with zero-leak PASS.
- Scriptable post-checks (non-clobber, zero-leak re-proof, DAPI density band) pass and are recorded in VALIDATION-tdt-only.md.
</verification>

<success_criteria>
The consolidated variable-marker pipeline is validated end-to-end on the incoming TdT-only slice set against the D-17 bar — schema-correct non-empty per-region table with omitted Fos+/Double+, re-proved zero-leak, and DAPI+ densities in the ~Phase-2 band — and captured as a reusable, documented pipeline (PIPE-06).
</success_criteria>

<output>
Create `.planning/phases/06.1-pipeline-generalization-per-region-readout-runbook/06.1-06-SUMMARY.md` when done.
</output>
