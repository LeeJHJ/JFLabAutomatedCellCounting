---
plan: 06.1-06
status: complete
requirements: [PIPE-06]
completed: 2026-07-27
---

# 06.1-06 SUMMARY — TdT-only end-to-end validation (D-17)

## Outcome

The generalized, config-driven pipeline was validated end-to-end on the incoming **TdT-only** slice set (wBA1-2_2-1, 5 sections) — the first real exercise of the variable-marker path. It runs clean, degrades correctly to a single marker (no Fos/Double+ anywhere, D-17), and produces biologically plausible whole-cell engram fractions in the amygdala.

Full record: **`docs/runbook/VALIDATION-tdt-only.md`**.

## Tasks

| Task | What | Result |
|---|---|---|
| 1 | Author + validate `pipeline.tdt-only.yml` | ✓ (commit 5e19a4c) — validates to TdT+/Negative/Excluded, no Double+ |
| 2 | Operator GUI run (ABBA → BraiAnDetect → classify → export) | ✓ done at the monitor (5 sections) |
| 3 | Scriptable post-checks + `VALIDATION-tdt-only.md` | ✓ integrity PASS, zero-leak PASS (Groovy authoritative), k-sweep + LA/BA readout, D-17 confirmed |

## Key results

- **D-17 PASS:** combined CSV markers = {DAPI, TdT}, classes = {DAPI+, TdT+} — zero Fos/Double+ rows/columns.
- **Integrity PASS:** 5 paired stems, no clobbering.
- **Zero-leak PASS:** `03_export`'s `leaf + unresolved == total` assertion holds every slice.
- **Engram:** ~4.3% (k=3) → 8% (k=2) TdT+ overall; LA/BA — BMA > BLA > LA, present in s1/s2/s4.
- **Robust:** fractions/pattern stable across whole-cell-TdT and dim-DAPI methodology changes.

## Deviations / findings (surfaced during validation, drove fixes/decisions)

- **Zero-leak assertion off-by-nUnresolved** (03_export): edge cell outside all atlas regions → fixed to reconcile unresolved (commit e712989).
- **Whole-cell TdT** requested by operator (TdTomato fills the cell) → added `whole-cell` compartment (quick 260725-w88); two propagation gaps fixed (guard GString 7c1d7eb, 03 validator c40cb95).
- **Dim-DAPI detection** tuned to `peakProminence: 45` (operator visual on LA).
- **k-sweep tool** formalized (`scripts/k_sweep_readout.py`, quick 260725-npx).
- **LABEL-01:** ~26% non-leaf assignment on amygdala — carried to Phase 8.
- **Python `--check-zero-leak`** over-asserts leaf-partition → deferred redesign (Groovy authoritative).

## Key files
- created: `docs/runbook/VALIDATION-tdt-only.md`, `pipeline.tdt-only.yml`
- readout artifacts: `<project>/qc_LA-BA_wholecell_ksweep.png`, `<project>/ksweep_wholecell_tidy.csv`

## Self-Check: PASSED
All Task-3 post-checks ran; D-17/integrity/zero-leak all green; validation outcome recorded.
