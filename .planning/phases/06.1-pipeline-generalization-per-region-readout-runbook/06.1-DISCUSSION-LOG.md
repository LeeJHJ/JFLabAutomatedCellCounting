# Phase 06.1: Pipeline Generalization + Per-Region Readout Runbook - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-23
**Phase:** 06.1-pipeline-generalization-per-region-readout-runbook
**Areas discussed:** Variable marker set, Canonical spine + parameterization, Output table + runbook, Config/combined-format/QC-gates

---

## Variable marker set

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit config block | markers: list (name→channel→compartment) in config | ✓ |
| Auto-detect from channels | Parse channel names to a built-in marker map | |
| Script-top constant | MARKERS map edited at top of script each run | |

**User's choice:** Explicit config block.

| Option | Description | Selected |
|--------|-------------|----------|
| Omit columns | Per-slice schema adapts to declared marker set | ✓ |
| Always-present, NA | Fixed columns, NA for absent markers | |
| Always-present, 0 | Fixed columns, 0 for absent markers | |

**User's choice:** Omit columns. **Notes:** the "aggregation must union schemas" consequence is resolved by the later long/tidy combined-CSV choice.

| Option | Description | Selected |
|--------|-------------|----------|
| DAPI anchor fixed; Double+ auto | DAPI mandatory anchor; Double+ only when ≥2 markers | |
| Anchor configurable too | Detection anchor declared in config, not hardwired | ✓ |

**User's choice:** Anchor configurable too. **Notes:** Double+ still auto-derived only when ≥2 non-anchor markers declared.

---

## Canonical spine + parameterization

| Option | Description | Selected |
|--------|-------------|----------|
| Keep split, retire duplicate | Keep run_braian_detection + 02; retire classify_markers | ✓ |
| Merge into one | Fold detection into 02 | |
| Keep all three | No consolidation | |

**User's choice:** Keep split, retire duplicate.

| Option | Description | Selected |
|--------|-------------|----------|
| Generalize region export; keep per-cell | Grow export_region_dapi_reference into the table; keep per-cell | |
| One export does everything | Single script emits per-cell + per-region table | ✓ |
| New third script | Add a new per-region script alongside existing | |

**User's choice:** One export does everything.

| Option | Description | Selected |
|--------|-------------|----------|
| All regions + config exclusions | Emit all regions; exclusions move to config | ✓ |
| Region-list parameter | Operator passes explicit acronym list | |
| Focus-ROI + all | All regions but mark a focus set | |

**User's choice:** All regions + config exclusions.

---

## Output table + runbook

| Option | Description | Selected |
|--------|-------------|----------|
| All regions, leaf-anchored counts | is_leaf flag; cells count to smallest leaf only | |
| Leaf regions only | Drop parent rows entirely | |
| All regions, parent rollups summed | Parent rows = sum of descendants | ✓ |

**User's choice:** All regions, parent rollups summed. **Notes:** reconciled with CR-01 — rollups MUST be leaf-anchored-then-summed up the tree, never independent parent-ROI containment (see CONTEXT D-10).

| Option | Description | Selected |
|--------|-------------|----------|
| Per-slice + append to combined | Per-slice table + growing combined CSV | ✓ |
| Per-slice only | One table per slice, nothing merged | |
| Single merged only | One multi-slice table | |

**User's choice:** Per-slice + append to combined.

| Option | Description | Selected |
|--------|-------------|----------|
| Single operator checklist RUNBOOK.md | One repo RUNBOOK.md, GUI-vs-scriptable marked | ✓ |
| Lightweight step index | Ordered list pointing to script headers | |
| Per-stage docs | Separate doc per stage | ✓ (supplementary) |

**User's choice:** Single operator checklist RUNBOOK.md **plus** per-stage detail docs for confusing steps.

---

## Config / combined-format / QC-gates

| Option | Description | Selected |
|--------|-------------|----------|
| New sidecar pipeline config | Separate pipeline.yml/markers.yml | ✓ |
| Extend BraiAn.yml | Add markers block to existing detection config | |
| Script-top config map | Config as a map in the scripts | |

**User's choice:** New sidecar pipeline config.

| Option | Description | Selected |
|--------|-------------|----------|
| Long/tidy format | One row per region×marker | ✓ |
| Wide with NA | Region-per-row, NA for absent markers | |
| Per-schema combined files | One combined CSV per marker-set schema | |

**User's choice:** Long/tidy format.

| Option | Description | Selected |
|--------|-------------|----------|
| Assert at boundaries, fail loud | Precondition checks at each scriptable entry | ✓ |
| Runbook documents checks only | Minimal script guards; operator eyeballs | |
| Full QC harness | Dedicated QC pass with recorded metrics | |

**User's choice:** Assert at boundaries, fail loud.

---

## Claude's Discretion

- Exact sidecar config filename/format, per-stage doc layout, precise assert messages.
- Retire `classify_markers.groovy` via delete vs move to `_archive/` (follow workspace archive convention; grep QuPath refs first).
- Validation target left to the inferred default (end-to-end on incoming TdT-only slices → schema-correct non-empty per-region table + spot bioplausibility), refinable at plan time.

## Deferred Ideas

- Section→animal BraiAnalyse aggregation — Phase 10 (AGG-01).
- Atlas-coordinate export + brainrender point cloud — EXP-03/04 (Phase 10).
- Area-based density readout for compact-nuclei regions — Phase 9 (AREA-01/02).
- Full QC harness (extended qc_detection_gates.groovy) — beyond this phase's targeted boundary asserts.
