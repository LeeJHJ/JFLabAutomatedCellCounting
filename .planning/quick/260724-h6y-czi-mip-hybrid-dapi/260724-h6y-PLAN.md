---
phase: quick-260724-h6y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/jflab/Analysis/czi_mip.py
autonomous: true
requirements: [QUICK-260724-h6y]
must_haves:
  truths:
    - "Per scene, the DAPI/anchor channel is built from ONE auto-selected sharpest Z plane (var-of-Laplacian), not a Z-MIP"
    - "Per scene, every non-anchor (marker) channel is a full-Z max projection over ALL planes"
    - "Sharpest-DAPI-plane selection is independent per scene and reuses the already-read DAPI stack (no second CZI read)"
    - "The chosen DAPI Z index and per-Z focus scores are printed per scene for provenance"
    - "The identity thumbnail consumes the single sharpest DAPI plane, not a DAPI MIP"
    - "A --self-test proves the projection logic without needing the 16 GB CZI"
  artifacts:
    - /home/jflab/Analysis/czi_mip.py
  key_links:
    - "Anchor channel chosen via existing _dapi_index(names) — never hardcoded position"
    - "Output filename suffix stays _MIP.ome.tiff so the line-276 output-count glob still matches"
    - "Marker projection uses np.max over the FULL read stack (all n_z planes), not a Z0-2 sub-range"
---

<objective>
Port the validated hybrid projection from `czi_hybrid_mip.py` into the multi-scene
`czi_mip.py`, applied PER SCENE: the DAPI/anchor channel becomes a single
auto-selected sharpest Z plane (var-of-Laplacian), while every marker channel stays
a full-Z max projection across all planes.

Purpose: The current per-channel loop max-projects EVERY channel including DAPI
(line 235), which over-projects nuclei into blobs / phantom-faint doublets (memory
`hybrid-imaging-dapi`). The single-scene reference already solved this; this brings
the fix into the series path without touching scene isolation, calibration, or the
export-integrity assertions.

Output: Modified `/home/jflab/Analysis/czi_mip.py` with a pure, unit-testable hybrid
projection helper, per-scene provenance printing, and a `--self-test` that proves the
behavior on synthetic arrays (no CZI required).
</objective>

<context>
@/home/jflab/Analysis/CLAUDE.md
@/home/jflab/Analysis/.claude/CLAUDE.md
@/home/jflab/Analysis/.planning/STATE.md

# Files being modified / referenced
@/home/jflab/Analysis/czi_mip.py
@/home/jflab/Analysis/czi_hybrid_mip.py

# Project --self-test idiom to match (synthetic fixtures, assert-based, prints PASSED)
@/home/jflab/Analysis/scripts/build_dapi_reference.py

Key facts already established during planning (do not re-derive):
- `czi_mip.py` exists ONLY at repo root — `find` confirmed no deploy copies in QuPath
  project dirs, so there is nothing to propagate.
- `_dapi_index(names)` (line 142) already returns the anchor index and is already
  bound to `dapi_idx` at line 215 — reuse it, do NOT hardcode a channel position.
- `scipy.ndimage` (`from scipy import ndimage as ndi`) is the focus metric the
  reference uses and is present in the `braian` env — no new dependency.
- The final output-count assertion (line 276) globs
  `{args.animal_prefix}_s*_MIP.ome.tiff`. Keeping the `_MIP.ome.tiff` suffix means
  that glob keeps matching — do NOT change the suffix.
</context>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Replace per-channel max-projection with per-scene hybrid projection</name>
  <files>/home/jflab/Analysis/czi_mip.py</files>
  <action>
Add `from scipy import ndimage as ndi` to the imports (matching czi_hybrid_mip.py's focus metric; scipy is in the braian env — CPU-only, no new heavy dependency).

Add two pure helpers (place them near the other `_` helpers, e.g. just below `_dapi_index`):

1. `_sharpest_plane_from_stack(stack)` — takes a list of already-read (Y, X) planes and returns `(best_z, scores)`. Compute a var-of-Laplacian focus score per plane by applying `ndi.laplace` to a strided-downsampled float32 copy of each plane (e.g. `p[::4, ::4]`) and taking `.var()`; this bounds memory and mirrors czi_hybrid_mip.py's low-res focus read WITHOUT a second CZI read. `best_z = int(np.argmax(scores))`. Return scores as plain Python floats.

2. `_hybrid_scene_projection(channel_stacks, dapi_idx)` — pure function taking a list (one entry per channel) of Z-plane lists, returning `(out_channels, dapi_z, dapi_scores)`. For the anchor index (`dapi_idx`): pick `dapi_z` via `_sharpest_plane_from_stack` and emit that single plane. For every other channel: emit `np.max(stack, axis=0)` over the FULL read stack (all planes — locked decision #2; deliberately NOT the Z0-2 sub-range czi_hybrid_mip.py used, to capture the observed 2-4 µm axial offset between the DAPI-sharp plane and marker signal peak). Preserve physical channel order in `out_channels`. This function performs NO CZI read, so it is unit-testable from synthetic arrays.

Rewrite the per-scene projection block (current lines ~226-237): keep reading all Z-planes per channel exactly as now, but accumulate each channel's squeezed planes into a `channel_stacks` list instead of max-projecting inline. After the read loop, call `out_channels, dapi_z, dapi_scores = _hybrid_scene_projection(channel_stacks, dapi_idx)`. Then `mip = np.stack(out_channels, axis=0)` (the shape/bbox assertion downstream is unchanged). Reuse the existing `dapi_idx` bound at line 215 — do not recompute or hardcode.

Add provenance printing right after projection (satisfies locked decision #1): print the per-Z focus scores (e.g. `Z0=..., Z1=...`) and a line naming the chosen sharpest DAPI plane with both `scene_idx` and the 1-based `s{N}` label.

Feed the identity thumbnail the single sharpest DAPI plane: change the `_save_identity_thumbnail(...)` call to pass `out_channels[dapi_idx]` (which is now the single plane, not a MIP) and update the adjacent comment (currently "Reuse the DAPI channel's already-computed MIP plane") to say single sharpest plane.

Encode hybrid provenance in the file itself without changing the filename: give `_build_ome_xml` an extra `dapi_z: int` parameter and inject an XML comment inside the OME description recording that the anchor channel is a single plane at that Z while markers are full-Z MIP (BioFormats ignores comments, so the PhysicalSizeX round-trip assertion at line 269-273 is unaffected). Pass `dapi_z` at the call site. KEEP the output filename suffix `_MIP.ome.tiff` unchanged so the line-276 output-count glob keeps matching — this is a deliberate concern flagged in the task: changing the suffix would falsely fail that assertion.

Update the module docstring: the summary currently says "stitches tiles, max-projects over Z per channel". Change it to describe the hybrid behavior (anchor/DAPI = single sharpest plane per scene via var-of-Laplacian; markers = full-Z MIP). Leave the scene-isolation / bbox / off-by-one / pitfalls prose intact.

Do NOT touch: `_preflight_scenes` / `--check-scenes`, the bbox-region read (`region=`, never `S=`), `_build_ome_xml`'s PhysicalSizeX calibration, the shape==bbox assertion, the OME round-trip assertion, or the final output-count assertion (beyond it continuing to match the unchanged suffix). The `--channels` physical read-order contract is unchanged.
  </action>
  <verify>
    <automated>conda run -n braian python /home/jflab/Analysis/czi_mip.py --help</automated>
    <automated>grep -q "_hybrid_scene_projection" /home/jflab/Analysis/czi_mip.py && grep -q "_sharpest_plane_from_stack" /home/jflab/Analysis/czi_mip.py && grep -q "from scipy import ndimage" /home/jflab/Analysis/czi_mip.py && echo HELPERS_OK</automated>
    <automated>grep -q "out_channels\[dapi_idx\]" /home/jflab/Analysis/czi_mip.py && echo THUMB_SINGLE_PLANE_OK</automated>
    <automated>grep -q "_s\*_MIP.ome.tiff" /home/jflab/Analysis/czi_mip.py && echo SUFFIX_GLOB_MATCHES</automated>
    <automated>test $(find /home/jflab/Analysis -name czi_mip.py -not -path '*/.git/*' | wc -l) -eq 1 && echo NO_DEPLOY_COPIES</automated>
  </verify>
  <done>
`--help` runs clean (module imports, incl. scipy, in the braian env; argparse builds).
Both new helpers and the scipy import are present. The identity thumbnail is fed
`out_channels[dapi_idx]` (single sharpest plane). The output glob/suffix still reads
`_MIP.ome.tiff`. Exactly one `czi_mip.py` exists on disk (no deploy copies to sync).
DAPI is no longer max-projected; markers remain full-Z MIP.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --self-test proving the hybrid projection on synthetic arrays</name>
  <files>/home/jflab/Analysis/czi_mip.py</files>
  <action>
Add a `--self-test` flag matching the project idiom (see `scripts/build_dapi_reference.py` / `scripts/validate_pipeline_config.py`): a synthetic, assert-based check that needs no CZI and prints a `PASSED` line.

In `parse_args`, add `p.add_argument("--self-test", action="store_true", ...)`. Because `--self-test` must run without input, relax `--czi` to `required=False` and, mirroring the existing conditional validation at lines 62-64, `p.error(...)` if `--czi` is missing when NOT `--self-test`. Keep the existing rule that `--outdir` is required unless `--check-scenes` (and also skip that requirement under `--self-test`). At the top of `main()`, short-circuit: `if args.self_test: _self_test(); return` — before `aicspylibczi.CziFile(...)` is opened.

Implement `_self_test()` that builds small synthetic channel stacks with numpy (use a seeded `np.random.default_rng(0)` for determinism) and asserts three properties via the pure helpers from Task 1:

(a) Sharpest-plane selection: build a 3-plane DAPI stack where exactly one plane is high-frequency noise (high Laplacian variance) and the others are near-constant (variance ~0); assert `_sharpest_plane_from_stack` returns the known-sharp index.

(b) Hybrid output shape: run `_hybrid_scene_projection` on a 3-channel synthetic scene (mark one index as anchor). Assert the anchor output is byte-identical to `stack[dapi_z]` (a single plane, `np.array_equal`), and each marker output is byte-identical to `np.max(stack, axis=0)` over the full stack.

(c) Per-scene independence: call `_hybrid_scene_projection` twice with two synthetic scenes whose DAPI stacks put the sharp plane at DIFFERENT Z indices; assert the two returned `dapi_z` values differ (selection is per-scene, not a shared/global index).

Print a `self-test PASSED: ...` line summarizing the three proven properties, consistent with the wording style of `build_dapi_reference.py`. Add `import numpy as np` is already present; do not add tempfile unless you use it.
  </action>
  <verify>
    <automated>conda run -n braian python /home/jflab/Analysis/czi_mip.py --self-test; test $? -eq 0 && echo SELFTEST_EXIT_0</automated>
    <automated>conda run -n braian python /home/jflab/Analysis/czi_mip.py --self-test | grep -q "PASSED" && echo SELFTEST_PASSED</automated>
    <automated>conda run -n braian python /home/jflab/Analysis/czi_mip.py 2>&1 | grep -qi "czi" && echo CZI_STILL_REQUIRED_WITHOUT_SELFTEST</automated>
  </verify>
  <done>
`--self-test` exits 0 and prints PASSED, proving (a) the var-of-Laplacian selector picks
the known-sharp plane, (b) anchor output = a single plane while markers = full-Z max, and
(c) selection is independent per scene — all without the 16 GB CZI. Running with neither
`--self-test` nor `--czi` still errors on the missing `--czi` (contract preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CZI file → script | Operator-supplied Zeiss CZI on local disk; trusted input, single local operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h6y-01 | Tampering | silent scene truncation / wrong DAPI plane | low | mitigate | Existing per-scene bbox non-overlap + output-count assertions unchanged; new per-scene DAPI-Z provenance is printed and embedded in OME-XML for audit |
| T-h6y-SC | Tampering | package installs | low | accept | No installs — scipy/numpy/tifffile already present in the braian env; no npm/pip/cargo step in this plan |
</threat_model>

<verification>
- `conda run -n braian python /home/jflab/Analysis/czi_mip.py --self-test` exits 0 and prints PASSED (synthetic proof; no CZI).
- `--help` imports cleanly (scipy present) and argparse builds.
- Static: hybrid helpers + scipy import present; thumbnail fed the single DAPI plane; `_MIP.ome.tiff` suffix/glob unchanged; exactly one `czi_mip.py` on disk.
- Regression-guard (by inspection during review): `_preflight_scenes`/`--check-scenes`, bbox `region=` read, `_build_ome_xml` PhysicalSizeX calibration + round-trip assertion, shape==bbox assertion, and final output-count assertion are all unchanged; `--channels` read-order contract preserved; `_dapi_index` reused (anchor not hardcoded); markers max-projected over the FULL Z stack.
</verification>

<success_criteria>
- DAPI/anchor channel is a single auto-selected sharpest Z plane per scene; markers are full-Z MIP.
- Sharpest-plane selection reuses the already-read stack (no second CZI read) and is per-scene independent.
- Chosen DAPI Z + focus scores printed per scene; provenance embedded in OME-XML; filename suffix unchanged.
- Identity thumbnail consumes the single sharpest DAPI plane.
- `--self-test` proves (a) selector correctness, (b) single-plane-DAPI vs full-Z-marker output, (c) per-scene independence — all without the 16 GB CZI.
- All pre-existing scene-isolation, calibration, and export-integrity assertions preserved.
</success_criteria>

<output>
Quick task — no SUMMARY file required. Commit the modified
`/home/jflab/Analysis/czi_mip.py` atomically.
</output>
