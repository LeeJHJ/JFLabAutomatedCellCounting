---
phase: quick-260725-w88
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QUICK-w88-whole-cell-tdt]
files_modified:
  - scripts/02_detect_classify.groovy
  - scripts/validate_pipeline_config.py
  - pipeline.yml
  - pipeline.tdt-only.yml
  - CLAUDE.md
  - .claude/CLAUDE.md
  - .planning/STATE.md
  - M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy
  - M3 Hippocampus 20x 062926 3 plane/pipeline.yml
  - wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy
  - wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml
  - wBA/wBA1-2_2-1-tdt-only 072026 project/scripts/02_detect_classify.groovy
  - wBA/wBA1-2_2-1-tdt-only 072026 project/pipeline.yml
autonomous: true

must_haves:
  truths:
    - "validate_pipeline_config.py accepts `compartment: whole-cell` and derives the measurement key `Cell: <channel> mean (bg-sub)` for such a marker."
    - "02_detect_classify.groovy accepts whole-cell markers (its own config-validator no longer rejects them), routes them to QuPath's area-weighted whole-cell `Cell: <channel> mean` measure, and FAILS LOUD (aborts, never all-Negative) if that key is absent on the detections."
    - "pipeline.yml and pipeline.tdt-only.yml declare TdT as `compartment: whole-cell`; Fos stays `nuclear`; cytoplasmic remains a valid, working option for other markers."
    - "CLAUDE.md (+ .claude/CLAUDE.md + STATE.md) record the TdT whole-cell revision as a deliberate, dated (2026-07-25) operator domain call with rationale, preserving nucleus-anchored colocalization and keeping the cytoplasmic ring available."
    - "All deploy copies (3 groovy, 2 pipeline.yml, 1 tdt-only) are byte-identical to their sources; downstream consumers (k_sweep_readout.py, build_dapi_reference.py, val01_metrics.py, 03_export_region_table.groovy) are byte-unchanged."
  artifacts:
    - scripts/02_detect_classify.groovy
    - scripts/validate_pipeline_config.py
    - pipeline.yml
    - pipeline.tdt-only.yml
    - CLAUDE.md
  key_links:
    - "COMPARTMENT_LABELS whole-cell->Cell must agree EXACTLY between the groovy (line ~226) and the python validator (_COMPARTMENT_LABELS) — divergence produces mismatched measurement keys."
    - "The groovy's OWN compartment-validator (line ~211, the `in [\"nuclear\", \"cytoplasmic\"]` check) MUST also accept whole-cell — otherwise the config is rejected before COMPARTMENT_LABELS is ever consulted."
    - "The whole-cell fail-loud guard must run at TOP script scope (not inside `dets.each`) so `return` aborts the run rather than skipping one detection."
---

<objective>
Add a third measurement compartment, `whole-cell` (label `Cell`, QuPath's area-weighted whole-cell mean = nucleus + cytoplasm over all cell pixels), to the config-driven classifier and switch TdT to it, because the operator confirmed TdTomato fills the whole cell — measuring it on the cytoplasmic ring alone under-counts it. The change is small and symmetric: the measurement loop is already compartment-agnostic; the core is adding one map entry, accepting the value in both validators, guarding against a missing `Cell:` key (fail-loud, D-15 style), flipping the TdT config declaration, documenting the revision in CLAUDE.md, and byte-propagating to all deploy copies.

Purpose: Correct TdT intensity measurement (whole-cell area-weighted, not ring-only) while keeping nuclear + cytoplasmic compartments fully working (additive), and capture the revision of a CLAUDE.md "non-negotiable" rule as a documented operator domain call — never silently.
Output: Updated classifier + validator + two pipeline configs + three doc files, all deploy copies propagated byte-identical, downstream scripts proven unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md
@scripts/02_detect_classify.groovy
@scripts/validate_pipeline_config.py
@pipeline.yml
@pipeline.tdt-only.yml

# Confirmed by reading source before planning:
# - The bg-sub measurement loop is already compartment-agnostic. For each marker it reads
#   QuPath's precomputed `"${COMPARTMENT_LABELS[compartment]}: ${channel} mean"` (rawKey),
#   subtracts a local-background annulus anchored outside the nucleus ROI (nuclear) or the
#   cell ROI (else), and writes `"${label}: ${channel} mean (bg-sub)"`. QuPath's
#   `"Cell: <ch> mean"` IS the area-weighted whole-cell mean. So whole-cell needs only a new
#   label entry + validator acceptance + a missing-key guard.
# - The baseRoi logic `(compartment == "nuclear") ? nucleusRoi : cellRoi` already routes
#   whole-cell to the cell-anchored annulus — correct, no change.
# - CRITICAL extra spot (not in the task pointer): the groovy has its OWN inline
#   compartment-value validator at lines 210-212 (`in ["nuclear", "cytoplasmic"]`). It must
#   also accept whole-cell or the run aborts before COMPARTMENT_LABELS is used.
# - Downstream (k_sweep_readout.py, build_dapi_reference.py, val01_metrics.py,
#   03_export_region_table.groovy) read the exported `<marker>_bgsub` column agnostically —
#   the column name stays `TdT_bgsub`; only the underlying compartment changes. No edits there.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add whole-cell compartment to the classifier + validator (source of truth)</name>
  <files>scripts/02_detect_classify.groovy, scripts/validate_pipeline_config.py</files>
  <action>
In `scripts/02_detect_classify.groovy` (the canonical copy at the Analysis root):

1. Inline compartment-value validator (~lines 211-212): extend the allowed-value list from `["nuclear", "cytoplasmic"]` to `["nuclear", "cytoplasmic", "whole-cell"]`, and update the fail-loud message tail from "must be nuclear or cytoplasmic" to "must be nuclear, cytoplasmic, or whole-cell". This is load-bearing: without it the new config is rejected before COMPARTMENT_LABELS is ever read.

2. COMPARTMENT_LABELS map (~line 226): add the entry `"whole-cell": "Cell"` so the map becomes the three-way `[nuclear: "Nucleus", cytoplasmic: "Cytoplasm", "whole-cell": "Cell"]` (the whole-cell key is hyphenated, so it MUST be quoted in the Groovy map literal). Update the adjacent comment (~line 225) to document the third option and its semantics: whole-cell -> Cell = QuPath's area-weighted whole-cell mean over all cell pixels (nucleus + cytoplasm), for markers that fill the whole cell (e.g. TdTomato). No change is needed to the baseRoi selector `(m.compartment == "nuclear") ? nucleusRoi : cellRoi` — whole-cell correctly resolves to the cell-anchored annulus.

3. Add a new top-level fail-loud guard, labeled in a comment "D-15 guard 4: whole-cell markers require QuPath's Cell-compartment mean", placed immediately BEFORE the `println "Computing local-background-subtracted measures (D-04)..."` line (~363), i.e. at script scope, NOT inside `dets.each`. Logic (directive prose, no fenced code): compute `wholeCellMarkers = markers.findAll { it.compartment == "whole-cell" }`; if it is non-empty, read `sampleKeys = dets.first().getMeasurements().keySet()` (dets is already guaranteed non-empty by guard 2), then `missingCellKeys = wholeCellMarkers.findAll { !sampleKeys.contains("Cell: ${it.channel} mean") }.collect { "Cell: ${it.channel} mean" }`. If missingCellKeys is non-empty: print an ERROR block that (a) names the missing key(s), (b) tells the operator BraiAnDetect must emit the Cell-compartment mean — re-run detection with makeMeasurements including nuclei AND a cell expansion so the Cell compartment exists, (c) prints the actual sampleKeys for diagnosis, then `return` to abort the run at script scope (mirrors D-15 guards 1-3). Rationale to put in the comment: without this guard a missing `Cell:` key resolves rawKey -> null -> NaN -> every cell Negative — the exact silent failure the A5/A5b self-checks warn about. Fail-loud is chosen over the Nucleus×area+Cytoplasm×area fallback per the codebase's D-15 fail-loud convention.

4. Refresh the header docstring compartment notes (~lines 31 and 59) so they read as three-way (nuclear -> nucleus ROI, cytoplasmic -> expanded cytoplasm/ring, whole-cell -> whole cell / Cell-compartment area-weighted mean). Keep it a one-line-per-mention edit; do not restructure the docstring.

In `scripts/validate_pipeline_config.py`:

5. `_VALID_COMPARTMENTS` (line 39): add `"whole-cell"` -> `("nuclear", "cytoplasmic", "whole-cell")`.
6. `_COMPARTMENT_LABELS` (line 42): add `"whole-cell": "Cell"`; update its adjacent comment to note whole-cell -> Cell (area-weighted whole-cell mean).
7. Module docstring compartment-semantics note (near the top, the `<compartment-label>: <channel> mean (bg-sub)` explanation): add whole-cell -> Cell so the printed contract is self-documenting.
8. `self_test()`: add a THIRD synthetic proof (keep the existing TdT-only and Fos+TdT proofs intact). Build a synthetic config with a single whole-cell marker `("TdT", "AF568-T2", "whole-cell")`, run load_config + derive_contract, and assert its `measurement_keys["TdT"] == "Cell: AF568-T2 mean (bg-sub)"` (fail with a clear "SELF-TEST FAIL" message and set ok=False if not). On success, extend the printed summary to include the whole-cell key proof.

Constraints: CPU-only; project style (snake_case python, `def`/`println` groovy); additive — nuclear and cytoplasmic must remain fully working. Do NOT touch any downstream consumer.
  </action>
  <verify>
    <automated>PYTHONUNBUFFERED=1 /home/jflab/miniforge3/envs/braian/bin/python3 scripts/validate_pipeline_config.py --self-test</automated>
  </verify>
  <done>
`--self-test` exits 0 and its printed proof includes the whole-cell key `Cell: AF568-T2 mean (bg-sub)`; the existing TdT-only and Fos+TdT proofs still pass. In the groovy, `grep -n '"whole-cell": "Cell"' scripts/02_detect_classify.groovy` returns the map entry, `grep -n 'whole-cell' scripts/02_detect_classify.groovy` shows the entry in BOTH the inline validator list (~line 211) and COMPARTMENT_LABELS (~line 226) plus the guard, and `grep -n 'D-15 guard 4' scripts/02_detect_classify.groovy` returns the guard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip TdT to whole-cell in both configs + document the revision</name>
  <files>pipeline.yml, pipeline.tdt-only.yml, CLAUDE.md, .claude/CLAUDE.md, .planning/STATE.md</files>
  <action>
1. `pipeline.yml`: change the TdT marker's `compartment: cytoplasmic` to `compartment: whole-cell` (Fos stays `nuclear`). Update the TdT marker's inline comment and the top-of-file "Compartment semantics" comment block: describe the third option — whole-cell = the marker is measured on the whole cell (nucleus + cytoplasmic ring), area-weighted mean (QuPath's `Cell: <channel> mean`), for markers whose signal fills the whole cell (TdTomato here, operator's confirmed domain observation 2026-07-25). State that the wiring stays fixed code (nuclear -> Nucleus, cytoplasmic -> Cytoplasm ring, whole-cell -> Cell area-weighted); cytoplasmic remains available for strictly-cytosolic markers.

2. `pipeline.tdt-only.yml`: same TdT `compartment: cytoplasmic` -> `compartment: whole-cell`; update its TdT comment to match (whole-cell / area-weighted, engram marker TdTomato).

3. `CLAUDE.md` (Analysis-correctness rules, ~line 37): revise the "DAPI nuclear / TdTomato cytosolic mismatch" hard rule so it reads that TdT is now measured on the WHOLE CELL (nucleus + cytoplasmic ring), AREA-WEIGHTED mean, because the operator confirmed (2026-07-25) TdTomato fills the whole cell in this line/prep; the cytoplasmic-ring compartment REMAINS available (config option) for strictly-cytosolic markers; classify Fos on the nuclear compartment (unchanged). Preserve the nucleus-anchored-colocalization principle explicitly: detection is still DAPI-nucleus-anchored; this only changes which compartment's intensity is measured for the TdT call. Frame it as a deliberate, documented REVISION of the former "non-negotiable cytoplasmic-ring-only" wording — not a deletion of the safeguard.

4. `.claude/CLAUDE.md` (Constraints, ~line 15 "Colocalization rule"): update "cytoplasmic ring for TdTomato" to "whole-cell (nucleus + ring, area-weighted) for TdTomato — operator domain call 2026-07-25; cytoplasmic ring still available for strictly-cytosolic markers", keeping the "Nucleus-anchored only ... no proximity/overlap heuristics" wording intact.

5. `.planning/STATE.md` (Key Decisions locked table, the "Cytoplasmic expansion ring for TdTomato / Locked" row, ~line 101): revise it to record that TdT is now measured whole-cell (nucleus + ring, area-weighted; QuPath `Cell: <ch> mean`) per operator domain call 2026-07-25 (TdTomato fills the whole cell); note the cytoplasmic-ring compartment remains available for strictly-cytosolic markers, and that this revises the earlier CLAUDE.md "non-negotiable" wording. Keep the row's Rationale/Status columns coherent (Status e.g. "Revised 2026-07-25 — whole-cell, operator domain call").

Constraint: this is analysis-correctness-sensitive — the whole-cell measure is the AREA-WEIGHTED whole-cell mean (QuPath `Cell: mean`), never max/unweighted. Do not weaken the nucleus-anchored colocalization principle anywhere.
  </action>
  <verify>
    <automated>PYTHONUNBUFFERED=1 /home/jflab/miniforge3/envs/braian/bin/python3 scripts/validate_pipeline_config.py --config pipeline.tdt-only.yml && PYTHONUNBUFFERED=1 /home/jflab/miniforge3/envs/braian/bin/python3 scripts/validate_pipeline_config.py --config pipeline.yml</automated>
  </verify>
  <done>
`--config pipeline.tdt-only.yml` prints the TdT bg-sub measurement key as `Cell: AF568-T2 mean (bg-sub)` and PathClass vocabulary `['TdT+', 'Negative', 'Excluded']` (exit 0). `--config pipeline.yml` prints TdT key `Cell: AF568-T2 mean (bg-sub)`, Fos key `Nucleus: AF488-T3 mean (bg-sub)`, and a vocabulary including `Double+` (exit 0). `grep -n 'whole-cell' CLAUDE.md .claude/CLAUDE.md .planning/STATE.md` shows the revision, and `grep -n '2026-07-25' CLAUDE.md` shows the dated rationale.
  </done>
</task>

<task type="auto">
  <name>Task 3: Propagate deploy copies byte-identical + prove downstream unchanged</name>
  <files>M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy, M3 Hippocampus 20x 062926 3 plane/pipeline.yml, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy, wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml, wBA/wBA1-2_2-1-tdt-only 072026 project/scripts/02_detect_classify.groovy, wBA/wBA1-2_2-1-tdt-only 072026 project/pipeline.yml</files>
  <action>
Copy the edited source files over their deploy copies, byte-for-byte (same convention as prior tasks — plain `cp`, then confirm with `diff -q`). Do NOT hand-edit the copies; overwrite them from the canonical source so they are guaranteed identical.

1. `scripts/02_detect_classify.groovy` -> each of the three project script dirs:
   - `M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy`
   - `wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy`
   - `wBA/wBA1-2_2-1-tdt-only 072026 project/scripts/02_detect_classify.groovy`

2. `pipeline.yml` (the 2-marker Fos+TdT config) -> the two full-series projects:
   - `M3 Hippocampus 20x 062926 3 plane/pipeline.yml`
   - `wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml`

3. `pipeline.tdt-only.yml` -> the TdT-only project, deployed under the fixed filename `pipeline.yml` (the groovy reads a fixed `pipeline.yml`):
   - `wBA/wBA1-2_2-1-tdt-only 072026 project/pipeline.yml`
   (This is the operator's live deployed config, so their next re-run of 02_detect_classify picks up whole-cell.)

Note the source/target asymmetry: the tdt-only project receives `pipeline.tdt-only.yml` (renamed to `pipeline.yml` on deploy), NOT the 2-marker `pipeline.yml`. `validate_pipeline_config.py` has NO deploy copies (it runs from the Analysis root). Do NOT modify any downstream consumer.
  </action>
  <verify>
    <automated>cd /home/jflab/Analysis && diff -q "scripts/02_detect_classify.groovy" "M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy" && diff -q "scripts/02_detect_classify.groovy" "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy" && diff -q "scripts/02_detect_classify.groovy" "wBA/wBA1-2_2-1-tdt-only 072026 project/scripts/02_detect_classify.groovy" && diff -q "pipeline.yml" "M3 Hippocampus 20x 062926 3 plane/pipeline.yml" && diff -q "pipeline.yml" "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml" && diff -q "pipeline.tdt-only.yml" "wBA/wBA1-2_2-1-tdt-only 072026 project/pipeline.yml" && test -z "$(git status --porcelain -- scripts/k_sweep_readout.py scripts/build_dapi_reference.py scripts/val01_metrics.py scripts/03_export_region_table.groovy)" && echo ALL_IDENTICAL_AND_DOWNSTREAM_UNCHANGED</automated>
  </verify>
  <done>
All six `diff -q` checks report no differences (deploy copies byte-identical to their sources: the three groovy copies match the source groovy; the two full-series pipeline.yml copies match the source pipeline.yml; the tdt-only project's pipeline.yml matches pipeline.tdt-only.yml). `git status --porcelain` for the four downstream consumers is empty (byte-unchanged). Command prints `ALL_IDENTICAL_AND_DOWNSTREAM_UNCHANGED`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BraiAnDetect measurement output -> classifier | Measurement key set produced upstream (in QuPath) is consumed by 02_detect_classify.groovy; a missing `Cell:` key is untrusted-shape input that must not be silently coerced. |
| pipeline.yml config -> classifier/validator | Operator-authored config declares compartments; an invalid value must be rejected loudly, not silently mis-routed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w88-01 | Tampering (silent data corruption) | 02_detect_classify.groovy whole-cell path | high | mitigate | D-15 guard 4: fail loud + abort if `Cell: <ch> mean` is absent on detections, instead of NaN -> all-Negative. Guard runs at top scope so `return` aborts the run. |
| T-w88-02 | Tampering (compartment mis-routing) | inline config-validator (line ~211) + COMPARTMENT_LABELS + python `_VALID_COMPARTMENTS`/`_COMPARTMENT_LABELS` | medium | mitigate | whole-cell added to BOTH validators AND both label maps in lockstep; validator self-test asserts the derived key equals `Cell: AF568-T2 mean (bg-sub)`. |
| T-w88-03 | Repudiation (undocumented rule change) | CLAUDE.md "non-negotiable" TdT rule | medium | mitigate | Revision recorded with date (2026-07-25) + operator-domain rationale in CLAUDE.md, .claude/CLAUDE.md, STATE.md; framed as documented revision, not deletion. |
| T-w88-SC | Tampering | package installs | low | accept | No new packages installed (edits to existing tracked scripts/configs/docs only); no legitimacy gate needed. |
</threat_model>

<verification>
Phase-level checks (no headless QuPath available — groovy is structurally verified, then confirmed live by the operator's next re-run):
- `validate_pipeline_config.py --self-test` exits 0 with the whole-cell proof; existing TdT-only + Fos+TdT proofs still pass.
- `validate_pipeline_config.py --config pipeline.tdt-only.yml` and `--config pipeline.yml` both print TdT key `Cell: AF568-T2 mean (bg-sub)` (and Fos `Nucleus: AF488-T3 mean (bg-sub)` for the 2-marker config).
- Groovy: `"whole-cell": "Cell"` present in COMPARTMENT_LABELS; `whole-cell` accepted in the inline validator (line ~211); `D-15 guard 4` present — in the source AND all three deploy copies (via `diff -q` identity).
- All deploy copies byte-identical; the four downstream consumers byte-unchanged (`git status --porcelain` empty for them).
- LIVE confirmation (operator, next run): re-run 02_detect_classify -> 03_export in QuPath; the A5b key-set print confirms `Cell: <ch> mean` exists on the detections, and TdT is classified on the whole-cell area-weighted measure.
</verification>

<success_criteria>
- whole-cell is a first-class compartment across the classifier and validator (additive; nuclear + cytoplasmic still work).
- TdT is declared and measured whole-cell (area-weighted `Cell: <ch> mean`) in both pipeline configs; Fos stays nuclear.
- A missing `Cell:` key aborts the run loudly (never all-Negative).
- The revision of the former CLAUDE.md "non-negotiable" rule is documented with date + rationale in CLAUDE.md, .claude/CLAUDE.md, and STATE.md; cytoplasmic ring remains available.
- All deploy copies byte-identical to source; downstream scripts proven byte-unchanged.
</success_criteria>

<output>
Create `.planning/quick/260725-w88-whole-cell-tdt/260725-w88-SUMMARY.md` when done.
</output>