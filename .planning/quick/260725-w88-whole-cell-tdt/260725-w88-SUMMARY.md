---
phase: quick-260725-w88
plan: 01
subsystem: pipeline
tags: [qupath, groovy, python, pipeline-config, tdtomato, classification]

requires:
  - phase: 06.1
    provides: config-driven per-marker classifier (COMPARTMENT_LABELS nuclear/cytoplasmic), validate_pipeline_config.py contract printer
provides:
  - Third measurement compartment `whole-cell` (label `Cell`) in both the classifier groovy and the python validator, additive to nuclear/cytoplasmic
  - D-15 guard 4: fail-loud abort (never silent all-Negative) when a whole-cell marker's QuPath `Cell: <ch> mean` key is absent
  - TdT declared and measured whole-cell (area-weighted `Cell: <ch> mean`) in pipeline.yml and pipeline.tdt-only.yml; Fos unchanged (nuclear)
  - Dated, documented revision of the former CLAUDE.md "cytoplasmic-ring-only, non-negotiable" TdT rule
affects: [phase-08-la-ba-classification, phase-09-area-based-density-readout, quick-tasks-touching-pipeline-yml]

tech-stack:
  added: []
  patterns:
    - "whole-cell compartment: nuclear -> Nucleus, cytoplasmic -> Cytoplasm, whole-cell -> Cell (QuPath's area-weighted whole-cell mean), symmetric across groovy classifier and python validator"
    - "D-15 fail-loud guard for a missing expected QuPath measurement key, run at top script scope so `return` aborts the whole run"

key-files:
  created: []
  modified:
    - scripts/02_detect_classify.groovy
    - scripts/validate_pipeline_config.py
    - pipeline.yml
    - pipeline.tdt-only.yml
    - CLAUDE.md
    - .claude/CLAUDE.md
    - .planning/STATE.md
    - "M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy"
    - "M3 Hippocampus 20x 062926 3 plane/pipeline.yml"
    - "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy"
    - "wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml"
    - "wBA/wBA1-2_2-1-tdt-only 072026 project/scripts/02_detect_classify.groovy"
    - "wBA/wBA1-2_2-1-tdt-only 072026 project/pipeline.yml"

key-decisions:
  - "TdTomato is now measured whole-cell (nucleus + cytoplasmic ring, area-weighted QuPath `Cell: <ch> mean`) instead of cytoplasmic-ring-only, per operator domain call 2026-07-25 that TdTomato fills the whole cell in this line/prep"
  - "cytoplasmic remains a fully working compartment option for any future strictly-cytosolic marker; nuclear/cytoplasmic/whole-cell are additive, not a replacement"
  - "A missing `Cell: <ch> mean` key for a whole-cell marker aborts the run loudly (D-15 guard 4) rather than silently degrading to all-Negative"

patterns-established:
  - "Adding a new compartment requires touching exactly 4 spots in lockstep: groovy inline validator, groovy COMPARTMENT_LABELS, python _VALID_COMPARTMENTS, python _COMPARTMENT_LABELS -- divergence between groovy and python produces mismatched measurement keys"

requirements-completed: [QUICK-w88-whole-cell-tdt]

coverage:
  - id: D1
    description: "whole-cell compartment added to 02_detect_classify.groovy (inline validator + COMPARTMENT_LABELS) and validate_pipeline_config.py (_VALID_COMPARTMENTS + _COMPARTMENT_LABELS), additive to nuclear/cytoplasmic"
    requirement: "QUICK-w88-whole-cell-tdt"
    verification:
      - kind: unit
        ref: "PYTHONUNBUFFERED=1 /home/jflab/miniforge3/envs/braian/bin/python3 scripts/validate_pipeline_config.py --self-test"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-15 guard 4: fail-loud abort at top script scope if a whole-cell marker's `Cell: <ch> mean` key is absent on detections"
    requirement: "QUICK-w88-whole-cell-tdt"
    verification:
      - kind: other
        ref: "grep -n 'D-15 guard 4' scripts/02_detect_classify.groovy (structural proof; no headless QuPath available to exercise it live)"
        status: pass
    human_judgment: true
    rationale: "The guard's runtime behavior (aborting on a real QuPath detection set missing the Cell key) can only be exercised live in QuPath -- no headless QuPath is available in this environment. Structurally verified via grep/code-read; live confirmation deferred to the operator's next QuPath run."
  - id: D3
    description: "pipeline.yml and pipeline.tdt-only.yml declare TdT as compartment: whole-cell; Fos stays nuclear; both validate and print the Cell-compartment TdT key"
    requirement: "QUICK-w88-whole-cell-tdt"
    verification:
      - kind: unit
        ref: "PYTHONUNBUFFERED=1 /home/jflab/miniforge3/envs/braian/bin/python3 scripts/validate_pipeline_config.py --config pipeline.tdt-only.yml && ...--config pipeline.yml"
        status: pass
    human_judgment: false
  - id: D4
    description: "CLAUDE.md, .claude/CLAUDE.md, STATE.md record the whole-cell revision as a dated (2026-07-25), documented operator domain call, preserving nucleus-anchored colocalization and the cytoplasmic-ring option"
    requirement: "QUICK-w88-whole-cell-tdt"
    verification:
      - kind: other
        ref: "grep -n 'whole-cell' CLAUDE.md .claude/CLAUDE.md .planning/STATE.md; grep -n '2026-07-25' CLAUDE.md"
        status: pass
    human_judgment: false
  - id: D5
    description: "All six deploy copies (3 groovy, 2 pipeline.yml, 1 tdt-only-as-pipeline.yml) byte-identical to source; four downstream consumers byte-unchanged"
    requirement: "QUICK-w88-whole-cell-tdt"
    verification:
      - kind: other
        ref: "diff -q (x6) + git status --porcelain on k_sweep_readout.py/build_dapi_reference.py/val01_metrics.py/03_export_region_table.groovy -> printed ALL_IDENTICAL_AND_DOWNSTREAM_UNCHANGED"
        status: pass
    human_judgment: false

duration: ~15min
completed: 2026-07-26
status: complete
---

# Quick Task 260725-w88: Whole-Cell TdT Compartment Summary

**Added a third, additive `whole-cell` measurement compartment (QuPath's area-weighted `Cell: <ch> mean`) to the config-driven classifier and validator, and switched TdTomato from cytoplasmic-ring-only to whole-cell per the operator's confirmed 2026-07-25 domain observation, with a fail-loud guard against a silently-missing `Cell:` key.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-26
- **Tasks:** 3/3
- **Files modified:** 13 (2 source scripts, 2 pipeline configs, 3 doc files, 6 deploy copies)

## Accomplishments

- `whole-cell` is now a first-class compartment across `02_detect_classify.groovy` and `validate_pipeline_config.py`, symmetric with `nuclear`/`cytoplasmic` and fully additive (both existing compartments still work unchanged).
- Added D-15 guard 4: a top-script-scope fail-loud abort if a whole-cell marker's QuPath `Cell: <channel> mean` measurement key is absent on detections -- prevents the silent `null -> NaN -> all-Negative` failure mode.
- TdT is now declared `compartment: whole-cell` in both `pipeline.yml` and `pipeline.tdt-only.yml`; Fos remains `nuclear`. Both configs validate and print the TdT measurement key as `Cell: AF568-T2 mean (bg-sub)`.
- The former CLAUDE.md "non-negotiable" cytoplasmic-ring-only rule for TdTomato is revised (not deleted) with a dated (2026-07-25), operator-attributed rationale in `CLAUDE.md`, `.claude/CLAUDE.md`, and `.planning/STATE.md`'s Key Decisions table; the cytoplasmic-ring compartment remains documented as available for any future strictly-cytosolic marker.
- All six deploy copies (three `02_detect_classify.groovy` project copies, two full-series `pipeline.yml` copies, and `pipeline.tdt-only.yml` deployed as `pipeline.yml` in the TdT-only project) are byte-identical (`diff -q`) to their canonical sources. The four downstream consumers (`k_sweep_readout.py`, `build_dapi_reference.py`, `val01_metrics.py`, `03_export_region_table.groovy`) are byte-unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add whole-cell compartment to the classifier + validator** - `65dc32c` (feat)
2. **Task 2: Flip TdT to whole-cell in both configs + document the revision** - `b0345e2` (feat)
3. **Task 3: Propagate deploy copies byte-identical + prove downstream unchanged** - `140c93f` (feat)

**Plan metadata:** handled by orchestrator (docs commit, not part of this task's per-task commits)

_Note: `.planning/STATE.md`'s Key Decisions row was edited in-tree as part of Task 2's documentation requirement, but per this quick task's constraints was left uncommitted here -- the orchestrator's docs commit picks it up together with the quick-task-table row and this SUMMARY.md._

## Files Created/Modified

- `scripts/02_detect_classify.groovy` - Inline compartment validator + `COMPARTMENT_LABELS` accept `whole-cell` -> `Cell`; new D-15 guard 4 fail-loud check; docstring updated to three-way compartment semantics
- `scripts/validate_pipeline_config.py` - `_VALID_COMPARTMENTS` / `_COMPARTMENT_LABELS` accept `whole-cell` -> `Cell`; `self_test()` adds a third synthetic proof for the whole-cell measurement key
- `pipeline.yml` - TdT `compartment: cytoplasmic` -> `whole-cell`; compartment-semantics comment block updated (Fos unchanged, nuclear)
- `pipeline.tdt-only.yml` - Same TdT compartment flip, comment updated to match
- `CLAUDE.md` - "DAPI nuclear / TdTomato cytosolic mismatch" hard rule revised to whole-cell measurement, dated 2026-07-25, framed as a documented revision (not a deletion) of the prior rule
- `.claude/CLAUDE.md` - Colocalization-rule constraint bullet updated to whole-cell for TdTomato, operator domain call 2026-07-25, cytoplasmic ring noted as still available
- `.planning/STATE.md` - Key Decisions locked table: "Cytoplasmic expansion ring for TdTomato" row revised to record the whole-cell change and rationale (edited, not committed by this task -- see note above)
- `M3 Hippocampus 20x 062926 3 plane/scripts/02_detect_classify.groovy`, `M3 Hippocampus 20x 062926 3 plane/pipeline.yml` - byte-identical deploy copies
- `wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/scripts/02_detect_classify.groovy`, `wBA 1-3 2-1 072026/wBA_1-3_2-1_072026/pipeline.yml` - byte-identical deploy copies
- `wBA/wBA1-2_2-1-tdt-only 072026 project/scripts/02_detect_classify.groovy`, `wBA/wBA1-2_2-1-tdt-only 072026 project/pipeline.yml` - byte-identical deploy copies (the latter deployed from `pipeline.tdt-only.yml`, renamed on deploy per the fixed-filename convention)

## Decisions Made

- TdTomato is measured whole-cell (area-weighted `Cell: <ch> mean`), never max/unweighted, per the plan's explicit constraint.
- The whole-cell fail-loud guard (D-15 guard 4) runs at top script scope (not inside `dets.each`), so `return` aborts the entire run rather than skipping a single detection -- consistent with the existing D-15 guards 1-3.
- CLAUDE.md's revision is framed explicitly as a REVISION of a "non-negotiable" rule, with the operator-attributed rationale and date, rather than silently rewriting or deleting the prior wording.

## Deviations from Plan

None - plan executed exactly as written. All four `must_haves.truths` and both `key_links` constraints were satisfied without needing to deviate from the plan's specified action steps.

## Issues Encountered

None. Two of the three deploy targets under `wBA/wBA1-2_2-1-tdt-only 072026 project/` were pre-existing on disk but not yet tracked in git (the whole `wBA/` directory was untracked before this task); the two files this task modifies (`pipeline.yml`, `scripts/02_detect_classify.groovy`) were staged and committed explicitly by path, without pulling in any of that directory's other untracked files (BraiAn.yml, data/, classifiers/, etc., which are out of this task's scope).

## User Setup Required

None - no external service configuration required. No headless QuPath run was performed (per plan constraints); the operator's next live re-run of `02_detect_classify.groovy` in QuPath on the TdT-only slice set (or the full-series projects) will exercise the whole-cell path and D-15 guard 4 live, per the plan's `<verification>` LIVE-confirmation item.

## Next Phase Readiness

The whole-cell compartment change is fully propagated and validator-proven; the operator's next QuPath re-run on the TdT-only project (already flagged as an open blocker in STATE.md awaiting the incoming slice set) will pick up the new `pipeline.yml` and exercise the D-15 guard 4 fail-loud path live. No blockers introduced by this task.

---
*Phase: quick-260725-w88*
*Completed: 2026-07-26*

## Self-Check: PASSED

All 12 modified/created files (source scripts, pipeline configs, doc files, 6 deploy copies) verified present on disk. All 3 task commits (`65dc32c`, `b0345e2`, `140c93f`) verified present in `git log --oneline --all`.
